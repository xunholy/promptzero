---
type: reference
created: 2026-04-25T17:00
tags: [catalog, gap-analysis, v0.8, specs]
related: [[firmware]] [[apps]] [[attacks]] [[hardware]] [[v0.8-team-audit]]
---

# Gap analysis — catalogs vs PromptZero Specs

Synthesises the four researcher catalogs (`firmware.md`, `apps.md`,
`attacks.md`, `hardware.md`) against the current Spec inventory and the
existing v0.8 roadmap (`docs/refactor/v0.8-team-audit.md`). Goal: surface
**what the prior audit missed**, not re-list what it already covered.

## Inputs (snapshot 2026-04-25)

| Source | Lines | Inventory |
|---|---|---|
| `docs/catalog/firmware.md` | 748 | 11 firmwares |
| `docs/catalog/apps.md` | 470 | 233 apps surveyed; ~190 with hw primitives |
| `docs/catalog/attacks.md` | 614 | 34 attack entries (2024-2026) |
| `docs/catalog/hardware.md` | 338 | 38 hardware entries |
| `docs/refactor/v0.8-team-audit.md` | 257 | existing roadmap (the floor) |
| `internal/tools/` registry | — | **230 Spec `Name:` registrations / 221 unique** (verified by `grep -rohE 'Name:\s+"[a-z0-9_]+"' --exclude="*_test.go" \| sort -u`) |

## Headline finding

The v0.8 audit covers the **strategic surface** (Phase 1 architecture,
Phase 2 capability themes) very well. The four researcher catalogs
broaden the picture in exactly two ways:

1. **Tactical Spec gaps** the audit didn't enumerate — defensive
   classifiers, forensic-side decoders, and a handful of widely-shipped
   Flipper apps (NFC relay, MagSpoof, Sentry Safe, POCSAG, AVR ICSP /
   SWD).
2. **Two net-new hardware backends** beyond audit §2c —
   **Proxmark3 (Iceman)** and **CatSniffer V3 / Sniffle dongle**.

That is the entire delta. **Diminishing returns set in fast.** Below ~30
items the new findings tail off into stuff already implied by the audit
or out-of-scope by policy. The "Top-30" list at §3 is therefore the
honest ceiling, not a quota.

One **factual correction to a researcher claim**: `firmware.md` §4.2
lists `subghz_chat` as missing. It is **already a Spec**
(`internal/tools/subghz.go:123`). Treat that as a stale finding; the
remaining four firmware-catalog gaps stand.

The **Cuyler36** entry from the original task #7 spec was confirmed
**not a Flipper firmware** by the firmware researcher (their §2.11). It
**does not appear in `docs/refactor/v0.8-team-audit.md`** — the typo
lives only in the task system / earlier draft notes, so no patch to the
audit is required. Recommendation: drop it from any future spec drafts;
no PromptZero detection branch should be added.

---

## 1. Coverage matrix

Every primitive identified in the four catalogs as a row. Columns:

- **Native** — Spec exists today in `internal/tools/`
- **Container-bridge** — wraps a containerised tool (mfoc/mfcuk/hashcat
  pattern) — counts as covered if the bridge is wired
- **Federation** — reachable via `mcpfed` prefix (e.g. `pm3.*`,
  `secsec.*`)
- **Gap** — not covered by any of the above today

✅ = present · ⚠️ = partial / variant · ❌ = missing.
"§2a"/"§2b"/"§2c" cells reference the corresponding row in the v0.8
audit (`docs/refactor/v0.8-team-audit.md`).

### 1.1 NFC / RFID

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| Mifare Classic mfoc / mfcuk / mfkey32 | apps + audit baseline | ✅ `mfoc_attack`, `mfcuk_attack`, `mfkey32_recover` | ✅ | ✅ via pm3 | — |
| Mifare hardnested host-bridge | attacks + audit Q2 | ⚠️ `mifare_hardnested_host` (bridge) | ✅ | ✅ | — |
| Mifare FM11RF08(S) backdoor | attacks #2 | ❌ | — | — | **§2a — already on roadmap** |
| Mifare Classic on-device nested (FlipperNested) | apps top-20 #7 | ❌ | — | — | **§2b** ⟶ `nfc_flippernested_run` |
| Mifare Plus SL1 read | apps top-20 #8 | ❌ | — | — | New gap |
| Mifare Ultralight C 3DES brute (`ulc_brute`) | apps NFC | ❌ | — | — | New gap |
| Mifare Classic UID enumeration brute | apps `uid_brute_smarter` | ❌ | — | — | New gap |
| iClass loclass key recovery | attacks + audit | ✅ `iclass_loclass_recover` | — | ✅ pm3 | — |
| iClass dummy-MAC emulate | attacks #9 | ❌ | — | ✅ pm3 | **NEW vs audit** |
| iClass SE/SEOS downgrade | attacks | ❌ | — | ✅ pm3 | Federation-only by policy |
| HID iClass SE / DESFire via SAM (Seader) | apps top-20 #4 | ⚠️ `loader_seader` (loader-only) | — | — | **§2b** ⟶ `nfc_seader_credential_read` |
| Saflok / dormakaba forgery | attacks + apps top-20 #5 | ❌ | — | — | **§2a** ⟶ `nfc_unsaflok_forge` |
| Metroflip transit cards | apps top-20 #3 | ❌ | — | — | **§2b** ⟶ `nfc_metroflip_*` |
| NFC Magic write Gen1A/2/4 | apps + capabilities | ✅ `loader_nfc_magic` | — | — | — |
| NFC APDU single-frame | baseline | ✅ `nfc_apdu` | — | — | — |
| NFC APDU **script** runner (sequence files) | apps top-20 #14 | ⚠️ `nfc_apdu` (1 frame) | — | — | **NEW vs §2b** ⟶ `nfc_apdu_script_run` |
| NFC raw frame TX | baseline | ✅ `nfc_raw_frame` | — | — | — |
| NFC sniffer (raw-bit) | apps `nfc_sniffer` | ⚠️ `nfc_raw_frame` (synth-only) | — | ✅ pm3 | **NEW** small gap |
| NFC relay (two-Flipper proxy) | apps top-20 #13 | ❌ | — | — | **NEW vs audit** ⟶ `nfc_relay_start/stop` |
| ULC / SEOS BLE-tunnel relay | apps `ulc_relay` | ❌ | — | — | New gap |
| ISO15693-3 writer | firmware §4.2 #4 | ❌ | — | — | **NEW** small gap |
| EMV parse (visa/mc) | firmware §4.2 #3 | ✅ `nfc_emv_decode` (+ `nfc_emv_encode`) — BER-TLV walker + ~80-tag dictionary; `nfc_emv_track2_decode` v0.414 cracks tag 57 (PAN/expiry/service code, Luhn-gated); `nfc_emv_dol_decode` v0.415 walks PDOL/CDOL/DDOL/TDOL (tag,length) lists; `nfc_emv_afl_decode` v0.416 expands tag 94 (SFI/record ranges → READ RECORD list); `nfc_emv_cvm_decode` v0.426 cracks tag 8E (X/Y amounts + CVM method/condition rules) | — | — | shipped — BER-TLV + Track-2 + DOL + AFL + CVM-List field decode. Cryptogram/online-auth flow deliberately out of scope (needs issuer keys). |
| Wiegand D0/D1 capture + replay | apps top-20 #6 + attacks #6 | ❌ | — | — | **§2b** ⟶ `gpio_wiegand_capture/replay` |
| HID Prox / EM4xxx PACS decode | apps top-20 #15 | ⚠️ `rfid_raw_analyze` | — | ✅ pm3 | **NEW** ⟶ `rfid_pacs_decode` |
| LF EM4100 / T5577 read+write | baseline | ✅ `rfid_*`, `loader_t5577_multiwriter`; offline `em4100_decode` (ID forms) + `em4100_encode` (64-bit frame) + `em4100_frame_decode` v0.417 (parity-validating frame→ID inverse) | — | — | — |
| FDX-B / DCF77 / niche LF synth | apps NFC | ⚠️ `rfid_build` covers EM4100 only; **FDX-B offline *decode* shipped v0.464.0 (`fdxb_decode`)**, DCF77 telegram-synth v0.375 | — | — | Low-priority gaps (LF *synth*/build side remains EM4100-only) |
| UHF EPC Gen2 (M6E-Nano) | apps `uhf_rfid` | ⚠️ | — | — | Adjacent-HW gap for *reading*; **offline EPC *decode* shipped v0.468.0 (`epc_decode`, SGTIN-96)** |
| Mag-stripe wireless emulation (MagSpoof) | apps top-20 #9 | ⚠️ partial | — | — | offline half ✅ `magstripe_decode` (v0.453 — ISO 7813 Track 1/2 ASCII swipe parser); the wireless-coil TX emulation remains a hardware loader step |

### 1.2 Sub-GHz

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| Sub-GHz read/transmit/decode/bruteforce/sweep | baseline | ✅ `subghz_*` family + `loader_*` | — | — | — |
| Sub-GHz protocol classify (ProtoView) | baseline + apps | ✅ `subghz_classify`, `loader_protoview` | — | — | — |
| KeeLoq (decrypt / brute / dictionary) | baseline | ✅ `keeloq_*` family | — | — | — |
| URH decode bridge | baseline | ✅ `urh_decode_sub` | ✅ | — | — |
| RollBack RKE replay (offensive) | attacks + audit Q5 | — by policy — | — | — | Intentional skip |
| RollBack capture-only detect | attacks #5 | ✅ `subghz_rollback_detect` | — | — | shipped v0.386 — offline defensive sequence analyser: flags non-consecutive duplicate rolling codes (key-free replay/RollBack signature; consecutive burst repeats excluded) + counter regressions when decrypted counters are supplied. Observation-not-verdict, no RF/TX. |
| Sub-GHz `chat` verb | firmware §4.2 #2 | ✅ `subghz_chat` | — | — | **researcher claim was stale** |
| TPMS decode (Schrader/Citroën/Renault/Toyota/Ford) | attacks #1 + apps top-20 #2 | ✅ `subghz_tpms_decode` | — | — | shipped v0.360 — Manchester (both conventions/alignments) + CRC-8 disambiguation + 32-bit sensor ID |
| TPMS synth | attacks + apps | ✅ `subghz_tpms_synth` | — | — | shipped v0.377 — offline inverse of `subghz_tpms_decode` ([sensor ID][payload][CRC-8] Manchester frame, round-trip-verified; generation only, no TX). Per-model pressure/temp scaling left to the caller (unverifiable). |
| Tesla VCSEC TPMS anomaly detect | attacks #15 | ⚠️ partial | — | — | `tpms_anomaly_detect` shipped v0.367 — Sub-GHz-side sequence analyser (excess unique sensor IDs vs wheel count + CRC-invalid frames, observation-not-verdict framing). The Tesla VCSEC **BLE-side** malformed-cert angle (CVE-2025-2082) is a separate, still-unshipped primitive. |
| Weather-station 433 MHz decode (LaCrosse/Acurite/Oregon) | apps `weather_station` | ✅ `subghz_weather_decode` (+ `subghz_weather_synth` v0.378, the inverse generator, round-trip-verified) | — | — | shipped v0.361 — LaCrosse TX141TH-Bv2 + Acurite 609TXC (fixed-40-bit, checksum-gated); Oregon/5n1 deferred |
| POCSAG paging decode | apps top-20 #11 | ✅ `subghz_pocsag_decode` (+ `subghz_pocsag_synth` v0.379, the inverse generator with real BCH(31,21), round-trip + idle-codeword verified) | — | — | shipped — sync/idle framing, numeric + alphanumeric, parity check |
| Sub-GHz playlist / scheduler / remote | apps | ✅ `loader_subghz_playlist` | — | — | — (`subghz_scheduler` low-priority) |
| Spectrum analyzer / freq sweep | baseline + apps | ✅ `subghz_freq_sweep`, `loader_spectrum_analyzer` | — | — | — |
| Sub-GHz signal generator | apps | ✅ `loader_signal_generator` | — | — | — |
| LoRa SX126x bridge | apps `LORA_term` | ⚠️ `bruce_lora_scan` (Bruce only) | — | — | New gap if Flipper-LoRa target |
| Sub-GHz jammer-detect | apps `subghz_jammer_detect` | ❌ | — | — | New gap (pairs with rollback_detect) |

### 1.3 BLE / Bluetooth

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| BLE Spam (Marauder backend) | apps + audit | ✅ `wifi_ble_spam` | — | — | — |
| BLE Spam (Flipper-native FAP) | firmware §4.2 #1 | ❌ | — | — | **NEW** small gap (capability bit `HasBLESpam` detected but no Flipper-side handler) |
| BLE FindMy / AirTag emulation | attacks #10 + apps top-20 #1 | ❌ | — | — | **§2b** ⟶ `ble_findmy_emulate` (nRootTag is the strong PoC) |
| nRootTag advertisement spoof | attacks | ❌ | — | — | Subsumed by `ble_findmy_emulate` |
| Apple Continuity classifier (defensive) | attacks #8 | ⚠️ `defense_classify_advertisement` | — | — | **NEW vs audit** ⟶ `ble_continuity_classify` |
| BLE proximity-tracking audit (passive long-running) | attacks BLE | ❌ | — | — | New gap (defensive) |
| BLE address-type / RPA classification | scan post-processing | ✅ `ble_addr_classify` v0.425 — random subtype from the top 2 bits (static / resolvable-private RPA / non-resolvable / reserved) or public OUI; the BLE counterpart of `mac_classify`, the privacy/tracking-resistance signal over scan-result addresses | — | — | shipped |
| BLE 5 connection follow (PHY/CSA hops) | hardware §4 (Sniffle) | ❌ | — | — | Needs CatSniffer/Sniffle backend |
| Stealtooth forced-pairing | attacks | — research-only — | — | — | Out of scope |
| BadKB (Bluetooth-HID BadUSB) | firmware + apps | ✅ via `badusb_*` + `bruce_badusb_run` | — | — | — |

### 1.4 802.11 / WiFi

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| WiFi scan / deauth / beacon-spam / probe / evil-portal | baseline | ✅ Marauder `wifi_*` family (≈70 Specs) | — | — | — |
| WiFi PMKID capture (Marauder) | baseline | ✅ `wifi_sniff_pmkid` | — | — | — |
| MAC randomization / admin-bit classify | scan post-processing | ✅ `mac_classify` v0.423 — I/G (multicast) + U/L (locally-administered → randomized/privacy-MAC signal) + broadcast, plus the known-attack-OUI cross-check; offline analysis over scan-result MACs | — | — | shipped |
| WiFi PMKID → hashcat 22000 pipeline | attacks #7 | ✅ native `.hc22000` PMKID writer `wifi_pmkid_hc22000` (v0.390 — pure-Go `WPA*01*…` line builder, anchored on hashcat's published example; removes the hcxpcapngtool shell-out for the clientless-PMKID case) | ✅ hashcat | — | EAPOL (type 02) pcap extraction still via hcxpcapngtool |
| WiFi SSID Confusion (Vanhoef WiSec'24) | attacks + audit §2a | ❌ | — | — | **§2a** ⟶ `wifi_ssid_confusion` |
| WiFi PEAP downgrade audit (CVE-2023-52160) | attacks #13 | ❌ | — | — | **NEW vs audit** ⟶ `wifi_peap_downgrade_audit` |
| WiFi FragAttacks audit (Vanhoef WiSec'25) | attacks #14 | ❌ | ⚠️ via Pineapple+container | — | **NEW vs audit** ⟶ `wifi_fragattacks_audit` |
| Pineapple REST surface | hardware §7 + audit §2c #3 | ❌ | — | — | **§2c #3 — backend pending** |
| GhostESP backend (Apple-spam, Pwnagotchi-friend, RGB) | hardware §1 + audit §2c #4 | ❌ | — | — | **§2c #4** |
| ESP flash / bring-up (`esp_flasher`) | apps WiFi | ❌ | — | — | New gap |

### 1.5 USB / BadUSB / HID

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| BadUSB run / validate / DuckyScript corpus | baseline | ✅ `badusb_*`, `bruce_badusb_run` | — | — | — |
| BadUSB **forensic classifier** (DuckyScript reconstruct from usbmon pcap) | attacks #11 | ✅ `usb_badusb_classify` | — | — | shipped — HID Boot-Protocol report decode → DuckyScript; v0.366 added raw Linux usbmon-capture ingestion (auto-strips per-URB framing). USBPcap (Windows) framing still deferred. |
| USB Rubber Ducky compile-and-drop | hardware §8 + audit §2c #5 | ❌ | — | — | **§2c #5** |
| O.MG Cable / Plug push | hardware §8 | ❌ | — | — | New gap (low priority) |
| Mass storage / MTP / MIDI emulate | apps BadUSB | ❌ | — | — | Niche; low priority |

### 1.6 GPIO / hardware bridges / chip-dump

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| GPIO read/set | baseline | ✅ `gpio_read`, `gpio_set` | — | — | — |
| 1-Wire search | baseline | ✅ `onewire_search` | — | — | — |
| iButton read/write/emulate | baseline | ✅ `ibutton_*` | — | — | host-side `ibutton_decode` (Dallas ROM dissector) + `ibutton_encode` (v0.385 — offline ROM-ID builder, family + 48-bit serial + Maxim CRC-8, round-trip + Maxim AN-27 vector verified) close the offline clone-prep loop |
| Bus Pirate I²C / SPI / UART | baseline | ✅ `buspirate_*` family | — | — | — |
| AVR ICSP programmer / read | apps top-20 #16 | ❌ | — | — | **NEW vs audit** ⟶ `avr_isp_read` (block on `workflow_glitch_chip_dump`) |
| ARM SWD probe / dump | apps top-20 #16 | ❌ | — | — | **NEW vs audit** ⟶ `swd_dump` |
| CMSIS-DAP debug bridge | apps `dap_link` | ❌ | — | — | New gap |
| WCH SWIO flasher | apps GPIO | ❌ | — | — | Niche |
| 8-channel logic analyzer / oscilloscope | apps top-20 #17 | ❌ | — | — | **NEW** ⟶ `gpio_logic_capture` |
| Sentry Safe / Master Lock electronic safe replay | apps top-20 #10 | ❌ | — | — | **NEW vs audit** ⟶ `gpio_sentry_safe_open` |
| Faultier glitch (arm/fire/sweep/disarm) | baseline | ✅ `glitch_*` family | — | — | — (Phase 0 hotfixes #2, #4) |

### 1.7 NRF24

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| NRF24 mousejack / sniff / payload-build / list-targets | baseline | ✅ `nrf24_*` family | — | — | — |
| NRF24 channel scanner / batch / monitor / jammer | apps NRF24 | ⚠️ partial | — | — | Low-priority gaps |

### 1.8 CAN bus / automotive

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| CAN init/sniff/inject/replay/info | baseline | ✅ `canbus_*` family | — | — | Phase 0 hotfix #3 (input validation) |
| CAN-FD sniff | apps top-20 #18 | ❌ | — | — | **NEW** ⟶ `canbus_fd_sniff` |
| UDS-on-DoIP attacks | attacks Auto | ⚠️ via `canbus_replay`; native `uds_decode` (v0.397 — ISO 14229 service / NRC / sub-function / DID decode, offline) | ✅ python-uds | — | Workflow extension (transport/ISO-TP still external) |
| ISO 15118 EVCC / PLC | attacks Auto | ❌ | — | — | Out of scope (PLC HW) |
| VIN decode/validate (ISO 3779) | UDS DID F190 / OBD-II Mode 09 | ✅ `vin_decode` v0.421 — check-digit validation (the anchor) + region + model-year candidates + WMI/VDS/VIS split; offline complement to the UDS/OBD VIN read. Manufacturer-from-WMI lookup deferred (proprietary). | — | — | shipped |
| DroneID receive | attacks #12 + audit §2a #5 | ❌ | — | — | **§2a + §2c** (blocked on HackRF) |

### 1.9 Firmware introspection / fork detection

| Primitive | Anchor source | Native | Container | Federation | Gap |
|---|---|:---:|:---:|:---:|:---:|
| Per-fork capability bitmap | firmware catalog backbone | ✅ `firmware_introspect` | — | — | — |
| Firmware extract / blob inspect | baseline | ✅ `firmware_extract` (group fix in Phase 0 #5) | — | — | — |
| Adversarial CFW detection (Private-Unleashed 2.0) | firmware §2.12 | ⚠️ implicit via `subghz_rollback_detect` | — | — | Detection-only, intentional |

### 1.10 Hardware backends (existing + missing)

| Backend | Native? | Notes |
|---|:---:|---|
| Flipper (USB-CDC + BLE) | ✅ `internal/flipper/` | — |
| ESP32 Marauder | ✅ `internal/marauder/` | — |
| Bruce (ESP32) | ✅ `internal/bruce/` | — |
| Faultier | ✅ `internal/faultier/` | — |
| Bus Pirate 5 | ✅ `internal/buspirate/` | — |
| HackRF + PortaPack | ❌ | **§2c #1** |
| ChameleonUltra | ❌ | **§2c #2** |
| WiFi Pineapple Mark VII | ❌ | **§2c #3** |
| GhostESP-Revival | ❌ | **§2c #4** |
| USB Rubber Ducky (compile-only) | ❌ | **§2c #5** |
| **Proxmark3 (Iceman)** | ❌ | **NEW vs audit §2c — containerbridge** |
| **CatSniffer V3 / Sniffle dongle** | ❌ | **NEW vs audit §2c — BLE-5 sniffing gap** |
| ChipSHOUTER PicoEMP | ❌ | Honourable mention (complements Faultier) |
| Glasgow Interface Explorer | ❌ | Honourable mention (chip-dump union of BP+GoodFET) |
| CANable v2 (SocketCAN) | ❌ | New native backend would unlock CAN-FD |

---

## 2. De-duplication — gaps already covered by v0.8 audit

These are **already in `docs/refactor/v0.8-team-audit.md`** and are *not*
re-listed in the prioritised gap section below. References use the
audit's own §2a/§2b/§2c/§2d/Q-numbers for cross-link. The catalogs
*confirm* every one of them — none of the audit's Phase 2 picks looks
weakened on second pass.

| Spec / item | Audit anchor | Catalog confirmation |
|---|---|---|
| `mifare_fm11rf08_backdoor` | §2a row 1 | attacks #2 (Quarkslab Aug 2024 — confirmed strongest) |
| `nfc_unsaflok_forge` | §2a row 2 | attacks #3 + apps top-20 #5 |
| `subghz_rollback_detect` | §2a row 3 + Q5 | attacks #5 + firmware §2.12 (adversarial CFW) |
| `wifi_ssid_confusion` | §2a row 4 | attacks #4 (Vanhoef WiSec'24) |
| `dronid_receive` | §2a row 5 | attacks #12 (blocked on HackRF — sequencing right) |
| `nfc_metroflip_*` | §2b | apps top-20 #3 (Metroflip 2026-04 active) |
| `subghz_tpms_decode` + `_synth` | §2b | attacks #1 + apps top-20 #2 — **promote to first-in (S effort, biggest ROI)** |
| `nfc_seader_credential_read` | §2b | apps top-20 #4 |
| `ble_findmy_emulate` | §2b | attacks #10 (nRootTag is the strongest 2025 PoC backing it — stronger than the original audit citation) |
| `gpio_wiegand_capture` + `_replay` | §2b | apps top-20 #6 + attacks #6 |
| `nfc_apdu_run` | §2b | apps NFC — **note: §3 below splits the script-runner variant** |
| `flipperhttp_fetch` + `_post` | §2b | apps `flip_downloader`, `web_crawler`, `flip_telegram` |
| `nfc_flippernested_run` | §2b | apps top-20 #7 |
| HackRF + PortaPack backend | §2c #1 | hardware §3 + §11 #1 |
| ChameleonUltra backend | §2c #2 | hardware §2 + §11 #2 |
| WiFi Pineapple Mark VII backend | §2c #3 | hardware §7 + §11 #4 |
| GhostESP-Revival backend | §2c #4 | hardware §1 + §11 #5 |
| USB Rubber Ducky compile-and-drop | §2c #5 | hardware §8 + §11 #7 |
| `workflow_evil_twin_fullcap` | §2d | attacks 802.11 cluster |
| `workflow_glitch_chip_dump` | §2d | apps GPIO — **note: §3 below names the Specs that block it** |
| `workflow_canbus_replay_capture` | §2d | attacks Automotive |
| `workflow_iclass_pickup` | §2d | apps NFC + attacks iCLASS |
| `workflow_keeloq_capture_and_crack` | §2d | attacks KeeLoq |
| `workflow_apple_continuity_audit` | §2d | attacks BLE |

**Cuyler36** — the audit doc itself does **not** mention this, so no
audit patch is needed. The reference lives only in the task #7
description and the firmware researcher's §2.11 stand-down. Drop it
from any future spec drafts; no detection branch in PromptZero.

---

## 3. Prioritised gap list — top 30 missing capabilities

Scoring: `prevalence × adversarial-leverage / effort`. Effort tags:
**S** ≤ 1 week · **M** 1-3 weeks · **L** 3+ weeks · **XL** ≥ 2 months.
Items already in the audit (§2 above) are excluded — this list is the
**delta** the audit missed.

| # | Spec / capability | Source | Why it ranks | Effort | Pkg / extends |
|---|---|---|---|:---:|---|
| 1 | `nfc_relay_start` + `_stop` (two-Flipper ISO14443A proxy) | apps `nfc_relay`, `ulc_relay` | High adversarial leverage (corp-badge clone-at-distance); apps shipped widely in RM/M; complements `ble_findmy_emulate`. | M | `internal/tools/nfc.go` + dual-target |
| 2 | `gpio_sentry_safe_open` (Sentry / Master factory backdoor) | apps top-20 #10 (`H4ckd4ddy/flipperzero-sentry-safe-plugin`) | Real physical-pentest primitive; tiny GPIO/UART sequence. | S | new `internal/safe/` or `flipper.go` GPIO path |
| 3 | `magspoof_emulate` (mag-stripe T1/T2/T3 wireless coil) | apps top-20 #9 (`zacharyweiss/magspoof_flipper`) | Untouched by audit; complements NFC payment pentest; widely shipped Samy-Kamkar port. | M | new `internal/magstripe/` |
| 4 | `subghz_pocsag_decode` (paging dragnet) | apps top-20 #11, attacks (rtl_433-adjacent) | Universal European paging still alive; fits `subghz_classify` pipeline. | S | extend `subghz_classify` |
| 5 | `subghz_weather_decode` (LaCrosse / Acurite / Oregon 433 MHz) | apps `weather_station` | ✅ shipped v0.361 — LaCrosse TX141TH-Bv2 + Acurite 609TXC, checksum-gated; Oregon/5n1 deferred. | S | `internal/weather/` |
| 6 | `tpms_anomaly_detect` (Tesla VCSEC malformed certs, BLE side) | attacks #15 (CVE-2025-2082) | ⚠️ Sub-GHz-side analyser **shipped v0.367** (excess unique sensor IDs + CRC-invalid frames, on the same wire as `subghz_tpms_decode`). The Tesla VCSEC **BLE-side** malformed-cert primitive (CVE-2025-2082) is still unshipped — needs a BLE classifier. | M | `subghz` + BLE classifier |
| 7 | `wifi_pmkid_capture` (native `.hc22000` writer + hashcat federate) | attacks #7 (hcxdumptool / hashcat 22000) | Closes the loop on Marauder PMKID capture; pure Go, no new HW. | M | `marauder`, future `pineapple` |
| 8 | `ble_continuity_classify` (Apple Continuity dissector) | attacks #8 (furiousMAC) + AppleJuice | Pure decode; pairs with audit's §2d `workflow_apple_continuity_audit`. | M | `marauder` BT pcap, `defense.go` |
| 9 | `iclass_dummy_mac_emulate` (legacy iClass, no MAC keys) | attacks #9 (bettse/Flipper picopass app) | Small change in existing emulation path; opens lab/red-team flows currently PM3-only. | S | `internal/iclass/` |
| 10 | `usb_badusb_classify` (DuckyScript reconstruct from usbmon pcap) | attacks #11 (agentzex Wireshark dissector) | ✅ **shipped** — HID Boot-Protocol report → DuckyScript decode (`internal/usbhid`); v0.366 added raw Linux usbmon-capture ingestion. USBPcap (Windows) framing deferred. | M | `internal/usbhid` |
| 11 | `swd_dump` + `avr_isp_read` (chip-dump Specs) | apps top-20 #16 | **Blocks audit §2d `workflow_glitch_chip_dump`** — without these the workflow has no data path. | M | new `internal/swd/` or extend `buspirate` |
| 12 | `gpio_logic_capture` (8-channel logic analyzer / oscilloscope) | apps top-20 #17 | Pairs with hw_recon workflows; only device-internal scope primitive. | M | extend `buspirate` GPIO sample loop |
| 13 | `nfc_apdu_script_run` (sequence-file APDU runner) | apps top-20 #14 | Audit named `nfc_apdu_run` (single-frame); script-file variant is a separate Spec. | S | extend `nfc.go` |
| 14 | `wifi_peap_downgrade_audit` (CVE-2023-52160) | attacks #13 | Adjacent to SSID Confusion; same hostapd backend; net-new attack-class. | M | future `pineapple` |
| 15 | `wifi_fragattacks_audit` (Vanhoef WiSec'25 follow-up) | attacks #14 | Defensive coverage of FragAttacks remediation status. | L | future `pineapple`, container-bridge |
| 16 | `subghz_jammer_detect` (RSSI floor + dwell heuristic) | apps `subghz_jammer_detect` | ✅ shipped v0.514.0 — offline host-side RSSI-sequence analyser (the complement to the on-device Jammer Detect FAP `loader_subghz_jammer_detect`): stats (min/max/mean/median/std-dev/noise-floor/occupancy/longest-dwell) + observation-not-verdict flags (elevated_noise_floor / sustained_occupancy / long_dwell) with overridable, echoed thresholds. Deterministic, hand-vector-verified; sibling of `subghz_rollback_detect`. `internal/subghz.AnalyzeJamming`. | S | extend `subghz` |
| 17 | `canbus_fd_sniff` (CAN-FD framing) | apps top-20 #18 | ✅ offline-decode sibling `canbus_fd_decode` shipped v0.362 — candump grammar + CAN-FD DLC↔length + SAE J1939 PGN; live sniff still TODO. | M | extend `canbus` |
| 18 | `ble_proximity_audit` (long-running passive Find-My / SmartThings flagging) | attacks BLE (Liu et al. USENIX'25) | Defensive complement to `ble_findmy_emulate`; needs Sniffle/CatSniffer. | M | new `internal/sniffle/` |
| 19 | `rfid_pacs_decode` (HID Prox / EM4xxx PACS payload decode) | apps top-20 #15 + attacks #6 | ✅ shipped — decode + the inverse `rfid_pacs_encode` (v0.376: H10301/H10306; v0.420: H10304 + H10302 37-bit, round-trip-verified, generation-only). Closes the reader-cloning loop. Corporate-1000 (35/48-bit) encode still deferred — self-referential/proprietary parity the decoder validates only best-effort. | S | `internal/pacs` |
| 20 | `nfc_iso15693_writer` (HF tag-it / ICODE write) | firmware §4.2 #4 + apps `iso15693_nfc_writer` | Net-new write surface added by Momentum 2025-2026. | S | `nfc.go` |
| 21 | `nfc_emv_parse` (EMV co-branded card decode) | firmware §4.2 #3 + apps | Read-only decode; defensible primitive. | M | `nfc.go` parser |
| 22 | `ble_spam_flipper_native` (Flipper-side BLE spam handler) | firmware §4.2 #1 | Capability bit `HasBLESpam` is detected today but no Flipper-side dispatch — only Marauder side (`wifi_ble_spam`) exists. **Defensive scope: keep classify-only**, do not add offensive TX. | S | `bluetooth.go` (new) — defensive only |
| 23 | `nfc_mfp_sl1_read` (Mifare Plus SL1 read) | apps top-20 #8 | Bridges Classic→Plus SL1 deployments while audit still says "no PoC for SL3". | S | `mifare.go` |
| 24 | `nfc_ulc_brute` (Mifare Ultralight C 3DES brute) | apps NFC `ulc_brute` | Crypto1 sibling; existing `internal/crypto1/` infra applies. | M | `mifare.go` + 3DES |
| 25 | `subghz_lora_recv` (LoRa SX126x for Flipper LoRa add-on) | apps `LORA_term`, `loradar` | Bruce already has `bruce_lora_scan`; Flipper-side is unsupported; new attack class (LoRaWAN replays). | M | new `internal/lora/` |
| 26 | `pm3_*` containerbridge (Proxmark3 Iceman) | hardware §11 #3 — **NEW vs §2c** | Containerbridge exactly the way mfoc/mfcuk are wired today; closes LF-EM4x sniff and HID-downgrade gaps the Chameleon can't reach. | S | new `internal/containerbridge/pm3` |
| 27 | `catsniffer_*` / `sniffle_*` containerbridge | hardware §11 #6 — **NEW vs §2c** | Sole BLE 5 connection-following primitive; Ubertooth replacement; CatSniffer V3 ($95) bundles Sniffle+LoRa+Zigbee. | M | new `internal/sniffle/` |
| 28 | `picoemp_emfi_*` (ChipSHOUTER PicoEMP) | hardware §5 (Bus Pirate-shape) | Cheap EM-FI complement to Faultier ($133); different physics. | M | new `internal/picoemp/` |
| 29 | `glasgow_*` applets (JTAG/SWD/SPI flash dump) | hardware §6 | Best chip-dump union (BP + GoodFET + cheap JTAG); containerbridge to `glasgow` CLI. | L | `internal/containerbridge/glasgow` |
| 30 | `canable_*` (SocketCAN backend, mature `go.einride.tech/can`) | hardware §10 | Cheapest unlock for real automotive Spec set; native Go bindings exist. | M | new `internal/canable/` |

### Honourable mentions (rank 31-40)

Below the line — small gaps, niche surfaces, or duplicates of items
above pending evidence:

- `nfc_amiibo_clone` (RM `amiibo_toolkit`)
- `nfc_dicts_manager` (already covered by `corpora` Spec — keep as is)
- `dcf77_clock_spoof` (LF time-signal synth) — ⚠️ telegram-synth shipped v0.375 (`dcf77_synth`: offline 60-bit minute-telegram generator, BCD + even parity, round-trip-verified against `dcf77_decode`). The long-wave TX stage (actual spoof transmission) remains a separate loader step.
- `ipv6_eui64_recover` (IPv6 → MAC deanonymisation) — ✅ shipped v0.424 — recover the hardware MAC embedded in an IPv6 Modified-EUI-64 interface identifier (detect the FF:FE marker, strip it, flip the U/L bit). The offline complement to `ndp_decode` / `dhcpv6_decode` (which surface IPv6 addresses); chains the recovered MAC into `mac_classify` so a MAC-derived SLAAC address is deanonymised and a randomized one is flagged. Observation-framed (privacy/RFC-7217 IIDs carry no marker), no hardware (`internal/macaddr`).
- `hash_identify` AD-roasting coverage — ✅ v0.434 extended the existing hash-format identifier with the Active Directory roasting loot: Kerberos TGS-REP (`$krb5tgs$`, Kerberoast → hashcat 13100/19600/19700), AS-REP (`$krb5asrep$` → 18200), AS-REQ pre-auth (`$krb5pa$` → 7500), DCC2/mscash2 (`$DCC2$` → 2100), and NetNTLMv1/v2 (the `user::domain:…` Responder format → 5500/5600). Definitive `$`-prefixes (no confidently-wrong); the colon-prefix strip now skips `$`-structured and `::` NetNTLM hashes so they reach the identifier intact. Ties to `kerberos_decode`. v0.435 added WPA hashcat-22000 lines (`WPA*01*` PMKID / `WPA*02*` EAPOL — closing the coherence gap with `wifi_pmkid_hc22000`, which emits them) and Cisco-IOS type 8 (`$8$` → 9200) / type 9 (`$9$` → 9300) (`internal/tools/security.go`).
- Werkzeug **scrypt** (`scrypt:N:r:p$…`) — ✅ v0.459 extended `internal/webpass` (and so `pbkdf2_password` verify/compute + the `hash_crack` `werkzeug` mode) to cover Werkzeug's **modern default** password hash (Werkzeug ≥2.3 / current Flask), closing the scrypt deferral noted when pbkdf2_password shipped (v0.457). The `werkzeug` cracker now auto-detects pbkdf2 vs scrypt; compute via `scheme=werkzeug-scrypt` (N/r/p). `Wrap-vs-native: wrap` — `golang.org/x/crypto/scrypt` (already a dep; native port infeasible), with a 1 GiB memory-cost cap against hostile N/r and our own `scrypt:N:r:p$salt$hex` framing. Verified byte-for-byte against the reference Werkzeug library + Go's x/crypto/scrypt. No new tool. (`internal/webpass`)
- `phpass_password` (WordPress $P$ / phpBB $H$) + `hash_crack` phpass — ✅ shipped v0.458 — the phpass portable-hash format used by **WordPress** (the most-deployed CMS) and phpBB3 — among the most common user-DB offline-crack targets (hashcat 400). Compute or verify (constant-time), and `hash_crack_dictionary` gained a `phpass` dictionary mode so a dumped wp_users table can be cracked directly. Iterated MD5 finished with phpass's own base64; reuses the crypt `itoa64` alphabet. A hostile embedded cost (> 2^24 rounds) is rejected rather than allowed to hang. Verified byte-for-byte against the reference **passlib** library (an independent oracle) — `Compute` reproduces the passlib hash exactly; fuzz-clean. Native — `crypto/md5`, stdlib only (`internal/phpass`).
- `pbkdf2_password` (Django / Werkzeug PBKDF2 password hashes) + `hash_crack` django/werkzeug — ✅ shipped v0.457 — the user-credential format in a **Python web-app database dump** (Django `pbkdf2_sha256$…`, Werkzeug/Flask `pbkdf2:sha256:…`) — a top offline-crack target the toolkit couldn't previously handle. Compute or verify (framework auto-detected, constant-time, sha256/sha1/sha512), and `hash_crack_dictionary` gained `django`/`werkzeug` dictionary modes (high-iteration → workers capped) so a dumped user table can be cracked directly. Reuses the generic `internal/wpa.PBKDF2`. Verified against reference **Django and Werkzeug** library output (an independent oracle) + raw `hashlib.pbkdf2_hmac`; fuzz-clean. Offline, no hardware (`internal/webpass`).
- `flask_session` (Flask/itsdangerous session cookie decode / verify / forge) — ✅ shipped v0.456 — the **web-pentest analogue of the JWT trio** for Flask, the most common Python web framework. A Flask session is signed-not-encrypted, so **decode** reads the payload with no key; **verify** tests a candidate `SECRET_KEY` or a list (the **flask-unsign weak-key attack** — a top web finding, parallel to the JWT weak-secret test); **forge** mints a validly-signed cookie (e.g. `{"admin":true}`) for an authorized test. Handles itsdangerous's HMAC-SHA1 signing, the `HMAC-SHA1(SECRET_KEY, "cookie-session")` key derivation, the 2011 timestamp epoch, and transparent zlib payload inflation. **Verified byte-for-byte against the reference itsdangerous library** (an independent oracle via a venv) — decode/verify against itsdangerous-produced cookies (compressed + uncompressed) and `Sign` reproduces an itsdangerous cookie exactly; fuzz-clean. Native — HMAC-SHA1 + base64url + zlib, stdlib only (`internal/flasksession`).
- `aprs_packet_decode` Mic-E decode — ✅ v0.490.0 — decoded **Mic-E** (Microphone Encoder, APRS101 §10), the single most-used APRS position format (the great majority of trackers + mobile radios: Kenwood TM-D700/D710, Yaesu FTM, the original Mic-Encoder), previously listed deferred as "~150 LoC of bit-twiddling". Mic-E packs latitude + the A/B/C message bits + N/S + longitude-offset + W/E into the **6-char AX.25 destination address**, and longitude + speed + course + symbol into the **information field** (data-type IDs `` ` `` current / `'` old GPS + the 0x1c/0x1d beta IDs). Decodes `latitude_deg` / `longitude_deg`, `speed_knots`, `course_deg`, the 15 standard/custom/emergency `message_type`s, symbol, latitude `ambiguity`, and surfaces trailing status text raw. **Anchored byte-for-byte to the two APRS101 §10 worked examples** (destination `S32U6T` → 33°25.64'N / M3 Returning; info field `` `(_fn"Oj/ `` → 112°7.74'W / 20 kt / 251°), both re-extracted from the spec via the pypdf venv-oracle — and the canonical modular speed/course formula (`speed=sp*10+dc/10`, `course=(dc%10)*100+se`, with the −800/−400 wraps) reproduces the example exactly (820→20, 651→251). The destination-decode tables (`0-9`/`A-J`/`K`/`L`/`P-Y`/`Z` → digit/blank/std-vs-custom bit; bytes 4-6 high-group → North/+100/West) are transcribed verbatim from the §10 encoding table. A test-caught transcription slip (a stray space in the info-field vector) was fixed before shipping — verification working as intended. Mic-E telemetry channels are surfaced as part of the raw status text rather than parsed. Fuzz-clean (~12.1M execs, 0 crashes) incl. the Mic-E seeds. No new tool — extends `aprs_packet_decode` (`internal/aprs`); registry unchanged.
- `aprs_packet_decode` complete weather report — ✅ v0.489.0 — the noted v0.488 follow-up, completing APRS101 §12: a **position report (with or without timestamp) whose symbol code is `_`** carries weather data in place of a free-text comment. Per the spec, the 7-byte **`ddd/sss` Wind Direction/Speed Data Extension replaces the positionless `cccc/ssss` fields** (the gotcha — wind here uses the position course/speed encoding, not `c.../s...`), after which gust / temperature / optional fields follow identically — so the shared "gust-onward" tail was factored out of the v0.488 parser and reused. Re-extracted the authoritative APRS101 §12 complete-weather pages (pypdf venv-oracle) for the two canonical examples — `!4903.50N/07201.75W_220/004g005t077r000p000P000h50b09900wRSW` and the timestamped `@092345z…W_220/004g005t-07…` — and anchored the tests on both. **Gated on the `ddd/sss` pattern** so a plain `_`-symbol position carrying a free-text comment is NOT mis-parsed as weather (verified by a dedicated gate test; the weather data also never leaks into `comment`). Fuzz-clean (~12.3M execs, 0 crashes) incl. the new complete-weather seeds. No new tool — extends `aprs_packet_decode` (`internal/aprs`); registry unchanged. The compressed-lat/lon complete weather report remains deferred with the rest of the base-91 compressed-position format.
- `aprs_packet_decode` positionless weather report — ✅ v0.488.0 — re-surveyed off the credential vein into the project's **ham-RF domain**: decoded the APRS positionless weather report (`_` data-type identifier, APRS101 §12), previously dispatched-but-deferred ("deferred until real captures surface"). Pulled the authoritative APRS101.pdf §12 via the venv-oracle technique (pypdf) — including the canonical worked example `_10090556c220s004g005t077r000p000P000h50b09900wRSW` — so the decode is spec-anchored, not memory. Decodes the 8-char MDHM timestamp + the fully-specified fields: wind direction (c), sustained wind speed (s), gust (g), temperature (t, incl. the −01..−99 below-zero form), rainfall last hour / 24 h / since midnight (r/p/P, hundredths-inch → inches), humidity (h, 00 = 100%), barometric pressure (b, tenths-hPa → hPa) and luminosity (L ≤ 999 / l ≥ 1000 W/m²). The **`s` wind-speed-vs-snowfall ambiguity is resolved by the spec's fixed mandatory-head order** (wind dir → speed → gust → temp), so the leading `s` is unambiguously wind speed; an absent sensor (`...`/spaces) decodes to a **null field, not a zero**; and the under-specified tail (snowfall, `#` raw counter, software/WX-unit trailer) is surfaced raw rather than guessed (no confidently-wrong output). Added the package's first **fuzz harness — which immediately caught a liveness bug** I'd introduced (a recognised field code with a too-short value, e.g. `L3`, spun the optional-field loop forever); fixed with a no-progress→raw guard, the failing input locked in as a corpus regression seed, then fuzz-clean (~12.7M execs, 0 crashes). Verified against the spec's canonical example + non-zero/negative-temp/100%-humidity/unknown-sensor/luminosity cases. No new tool — extends `aprs_packet_decode` (`internal/aprs`); registry unchanged. The complete weather report (weather appended to a position report whose symbol code is `_`) is the noted follow-up.
- `snowflake_decode` (Snowflake-ID decoder) — ✅ v0.520.0 — the **integer / social-media-OSINT counterpart** to the string identifier decoders (`uuid_decode` / `objectid_decode` / `ulid_decode`). A Snowflake is the 64-bit ID used by Discord, Twitter/X, Instagram and others; it packs a 41-bit millisecond timestamp (from a platform-specific epoch) in its high bits, then machine/worker + sequence bits. Decoding one is a **standard OSINT technique**: a Discord user / message / channel / guild ID, or a tweet / X-post ID, reveals exactly **when the object was created** (account age, message timing, enumeration). Because a bare Snowflake does not identify its platform — and the same integer yields a different timestamp under each epoch — the decoder reports a **labelled candidate per platform** (Discord, epoch 2015-01-01; Twitter/X, epoch 2010-11-04) and **asserts none** (no confidently-wrong single answer); a `platform` filter narrows to one. Each candidate gives the UTC timestamp + unix-ms + the low-bit fields (Discord: worker/process/increment; Twitter/X: machine-id/sequence). Native — a shift + add + masks, stdlib only, no new go.mod dep. **Anchored to Discord's own documented example**: `175928847299117063` → 2016-04-30T11:18:25.796Z (worker 1, process 0, increment 7); Twitter/X arithmetic cross-checked. Instagram and other Snowflake variants use a different bit layout and are deliberately not decoded (a wrong field split would be confidently-wrong). Non-numeric / out-of-uint64 input rejected; fuzz-clean. **This completes the identifier info-leak family**: UUID (v1/v6/v7) + MongoDB ObjectId + ULID + Snowflake — all the major timestamp-bearing ID formats. `internal/snowflake`. Registry 538→539.
- `ulid_decode` (ULID decoder) — ✅ v0.519.0 — **completes the identifier-timestamp triad** with `uuid_decode` (v0.517) and `objectid_decode` (v0.518). A ULID (Universally Unique Lexicographically Sortable Identifier) is a 26-character Crockford-base32 string encoding 128 bits — a 48-bit millisecond Unix timestamp + 80 bits of randomness — widely used by modern backends as a sortable, UUID-sized identifier (the spec's answer to UUIDv4, a peer of UUIDv7). Like a UUIDv1/v7 or a MongoDB ObjectId it is **not opaque**: it leaks the creation time of whatever it identifies (tokens, URLs, API params, logs, DB keys), and its lexicographic sortability aids record enumeration. Returns the timestamp (UTC + unix ms) and the 80-bit randomness (hex). Input must be exactly 26 Crockford-base32 chars (case-insensitive; no I/L/O/U) with the first char 0-7 (a larger first char overflows 128 bits — the spec's max is `7ZZ…`); anything else is rejected. Native — Crockford base-32 decode (via `math/big` for the 128-bit value) + a uint48 read, stdlib only, no new go.mod dep. **Anchored to python-ulid AND a hand-verified Crockford decode**: `01ARZ3NDEKTSV4RRFFQ69G5FAV` → 1469922850259 ms / 2016-07-30T23:54:10.259Z / randomness `d6764c61efb99302bd5b` (I manually Crockford-decoded the first ten chars to confirm 1469922850259, settling a mis-recalled spec value; a test-vector boundary mis-split was caught by the test before shipping). All-zero ULID → epoch; fuzz-clean. `internal/ulid`. Registry 537→538.
- `objectid_decode` (MongoDB ObjectId decoder) — ✅ v0.518.0 — the **MongoDB analogue of `uuid_decode`** and the completion of the opaque-hex ObjectId rendering in `mongodb_decode` / `bson_decode`. A 12-byte ObjectId is the default `_id` of every MongoDB document and is not opaque — its first 4 bytes are the Unix-second creation time — and ObjectIds leak into URLs, REST API parameters, logs, and exported documents, so a captured ObjectId reveals **when its record was created** (and the trailing 3-byte counter supports record-enumeration / timing inference). Returns the timestamp (UTC + unix seconds), the 5-byte per-process random value (hex), and the 3-byte counter (int). The legacy pre-3.4 machine-id/process-id split of the middle bytes is deprecated and deliberately **not asserted** (it would be a confidently-wrong reading on a modern ObjectId — surfaced raw instead). Accepts a bare 24-hex string or an `ObjectId("…")` / quoted wrapper; a non-24-hex string is rejected. Native — BSON-spec field extraction (a 4-byte big-endian uint32 read), stdlib only, no new go.mod dep. **Anchored to pymongo `ObjectId.generation_time`**: `507f1f77bcf86cd799439011` → 2012-10-17T21:13:27Z, `65a1b2c3d4e5f60718293a4b` → 2024-01-12T21:44:35Z, the all-zero ObjectId → 1970-01-01T00:00:00Z (a test-vector mis-slice in the random/counter expectation was caught by the test before shipping — verification working as intended). Fuzz-clean. `internal/objectid`. Registry 536→537.
- `uuid_decode` (UUID/GUID structure + info-leak decoder) — ✅ v0.517.0 — a **recon primitive**, not just a parser: UUIDs appear in tokens, API responses, email Message-IDs, filenames, DB keys and session IDs, and some versions are not opaque. **v1/v6** (time-based) embed the **generating host's MAC address** + the **creation timestamp** — the classic `UUIDv1 leaks the MAC + time` info-disclosure finding — so a captured v1 UUID deanonymizes the minting host; **v7** embeds a millisecond creation timestamp (record time-ordering / enumeration); **v3/v5** (name-hash) and **v4** (random) carry no recoverable data and say so (never guessed). Reports version + variant (RFC 4122/9562 / NCS / Microsoft GUID / future), the v1/v6 timestamp (UTC) + node-as-MAC + a `node_is_mac` assessment **gated on the IEEE multicast bit** (a randomized node is never reported as a hardware MAC), the clock sequence, and recognises the nil/max UUIDs. Accepts dashed / bare-hex / `urn:uuid:` / `{braced}` forms. Native — RFC 9562 bit-layout extraction + a gregorian→unix epoch shift, stdlib only, no new go.mod dep (Go has no UUID type; distinct from `bluetooth_gatt_uuid_lookup`, which is BT GATT service UUIDs). **Anchored to Python's reference `uuid` module**: the v1 example → version 1, node 00:11:24:44:be:1e, 2006-06-10T10:48:31.013993Z, clock-seq 15642; the v7 example → 2022-02-22T19:22:22Z (unix-ms 1645557742000); v3/v4/v5 versions; nil UUID. v6 (which the reference module does not timestamp-decode) is cross-checked by field-reordering the v1 instant and asserting the same UTC. A non-UUID string is rejected; fuzz-clean. `internal/uuidinfo`. Registry 535→536.
- `geohash_decode` (geohash ↔ lat/lon) — ✅ v0.516.0 — added the **geohash converter as a decode companion to the project's data decoders**: the geohash (compact base-32 geocode, e.g. `ezs42`) is stored by **Redis GEO** (GEOADD/GEOPOS), Elasticsearch / MongoDB geo indexes, and countless location APIs — so a geohash surfaced by `redis_decode` / `mongodb_decode` / `bson_decode` in a captured cache value, document, or API response now decodes to a real latitude+longitude (a genuine forensic/triage tie, not generic utility; sibling of v0.515's `maidenhead_locator`). Bidirectional: **decode** a geohash (case-insensitive, 1–12 chars) → cell **center** + ± error (half-cell) + bounding box (precision explicit, never overstated); **encode** a fix → geohash at 1–12 chars (default 9 ≈ 4.8 m). Native — base-32 range-bisection over the lat (−90..90) / lon (−180..180) ranges, 5 bits per char (alphabet `0123456789bcdefghjkmnpqrstuvwxyz`, no a/i/l/o); stdlib only, no new go.mod dep. **Anchored byte-for-byte to the `pygeohash` reference library**: `ezs42` → (42.60498, −5.60303) ± 0.0220°, `u4pruydqqvj` → (57.649111, 10.407440), `9q8yy` → San Francisco, single-char `0` → (−67.5, −157.5) ± 22.5°; encode reproduces each, and decode→encode round-trips. Out-of-alphabet chars and out-of-range coords are rejected; fuzz-clean. `internal/geohash`. Registry 534→535.
- `maidenhead_locator` (Maidenhead grid-locator ↔ lat/lon) — ✅ v0.515.0 — added the **ham-radio geo converter** that complements the project's APRS / AIS / GPS-NMEA decoders: the Maidenhead grid locator (a.k.a. QTH / grid square, e.g. `FN31pr`) is the location format used throughout amateur radio — in APRS, ham logging, contesting, and on the displays of many handheld/mobile rigs. Bidirectional: **decode** a locator (case-insensitive, 1–4 pairs) to its grid-square **center** lat/lon + **SW corner** + cell size (so the precision is explicit, never overstated), or **encode** a fix to a locator at 1–4 pairs (default 3 → the common 6-character locator). Native — the Maidenhead system is public fixed-base arithmetic (alternating base-18 field A-R, base-10 square, base-24 subsquare a-x, base-10 extended over the 360°×180° world grid); stdlib only, no new go.mod dep. **Anchored byte-for-byte to the `maidenhead` reference library** (venv-oracle): e.g. (41.714, −72.728) → `FN31pr` (and `FN31`, `FN31pr21` at 2/4 pairs), (48.146, 11.608) → `JN58td`, (0,0) → `JJ00aa`; and `JN58td` → SW corner 48.125°N/11.5833°E, center 48.1458°N/11.625°E. Malformed locators (odd length, out-of-range field/square/subsquare char) and out-of-range coordinates are rejected; decode is case-insensitive and round-trips with encode. Fuzz-clean (~3.4M execs). `internal/maidenhead`. Registry 533→534.
- `subghz_jammer_detect` (offline sub-GHz jammer analyser) — ✅ v0.514.0 — shipped the §3 **rank-16** gap, and the last pure-offline, no-hardware item left on the Top-30 (the rest need a HackRF / Pineapple / Sniffle / PM3 / chip-dump backend). It is the host-side complement to the on-device Sub-GHz Jammer Detect FAP (`loader_subghz_jammer_detect`, which only flags on the Flipper itself): the operator feeds an already-captured RSSI sample sequence (dBm, capture order) and the analyser computes the objective statistics — min / max / mean / median / std-dev / **noise floor** (a low percentile, robust to bursts) / **occupancy fraction** / **longest continuous dwell** — then applies the same receive-only "RSSI floor + dwell" heuristic as the FAP. Three flags, each an **observation with its benign explanation** stated (never a jammer verdict — a confidently-wrong alert is worse than none, the same framing as the shipped `subghz_rollback_detect` and `tpms_anomaly_detect`): **elevated_noise_floor** (the channel idles at a raised level), **sustained_occupancy** (continuous rather than bursty energy), **long_dwell** (the longest unbroken above-busy run spans most of the capture). The decision thresholds (busy dBm, elevated-floor dBm, floor percentile, occupancy fraction, dwell fraction) all have documented defaults, are overridable, and are echoed back in the result so the reading is reproducible. Native — a pure statistical sequence transform, stdlib `math`/`sort` only, no hardware and no new go.mod dep. **No confidently-wrong risk**: the statistics are deterministic and hand-vector-verified (a known ramp's mean/median/std-dev/10th-percentile-floor/occupancy/dwell asserted exactly; a continuous −45 dBm carrier fires all three flags; a bursty low-duty-cycle capture fires none); the flags are thresholded observations, not a verdict. Fuzz-clean. `internal/subghz.AnalyzeJamming`. Registry 532→533.
- `gps_nmea_decode` GSV per-satellite + GST + ZDA — ✅ v0.513.0 — expanded the v0.512 NMEA decoder with the sentences most useful for GPS signal-integrity / spoofing-and-jamming analysis: **GSV per-satellite detail** (PRN / elevation / azimuth / SNR for each satellite in view — anomalous SNR or geometry is a spoofing/jamming signal; SNR is null when a satellite is tracked-but-unused), **GST** (pseudorange error statistics — RMS, the error-ellipse major/minor/orientation, and lat/lon/altitude standard deviations — fix integrity), and **ZDA** (UTC time + date). `gps_nmea_decode` now covers GGA/RMC/GLL/VTG/GSA/GSV/GST/ZDA. **Anchored to pynmea2** — the canonical example sentences reproduce its per-satellite PRN/elev/azim/SNR, GST error fields, and ZDA date exactly. Fuzz-clean (~6.9M execs). No new tool — extends `gps_nmea_decode` (`internal/nmea`); registry unchanged.
- `gps_nmea_decode` (GPS/GNSS NMEA 0183 sentence decode) — ✅ v0.512.0 — closed a real gap in the GPS workflow: the project had device-side GPS tools (`marauder_nmea`, `gps_tracker_*`, `gps_poi`) but **no offline NMEA decoder** — `marauder_nmea` only *streams* raw sentences the operator then has to read by hand. `gps_nmea_decode` is the offline complement (the GPS counterpart to `ais_nmea_decode`, which only handles AIS `!AIVDM`): paste a captured NMEA log — a wardriving track, a geotag stream, a drone-telemetry dump, or a single `$GPGGA…` line — and get the parsed fix. Decodes the position/velocity/fix sentences with the per-sentence XOR checksum validated: **GGA** (time, lat/lon, fix quality, satellites, HDOP, altitude), **RMC** (time, status, lat/lon, speed-knots, course, date, magnetic variation), **GLL** (lat/lon, time, status), **VTG** (true+magnetic course, speed knots+km/h), **GSA** (no-fix/2D/3D fix type, PDOP/HDOP/VDOP), **GSV** (satellites in view). Coordinates convert `ddmm.mmmm`/hemisphere → signed decimal degrees; the talker ID (GP/GN/GL/GA/GB/BD/GQ/GI) is identified; a multi-sentence stream decodes to an array. `checksum_ok` is reported (bad/absent checksum still parses but is flagged); an empty field (no fix yet) decodes to null, never a zero; an unrecognised sentence type is surfaced with its raw fields rather than guessed. Native — public comma-delimited ASCII + XOR checksum, stdlib only, no new go.mod dep (a Go NMEA library would be a runtime dep for a few hundred lines of field handling). **Anchored to the pynmea2 reference library** — the canonical example sentences reproduce its decoded latitude/longitude/time/speed/course/fix fields exactly (e.g. `$GPGGA,123519,4807.038,N,01131.000,E,…` → 48.1173°N, 11.5167°E). Fuzz-clean (~7.4M execs, 0 crashes). `internal/nmea`. Registry 531→532.
- `aprs_packet_decode` compressed complete weather report (APRS101 §12) — ✅ v0.511.0 — the noted v0.509 follow-up, completing APRS weather coverage: a **§9 compressed position whose symbol code is `_`** (weather station) carries a weather block after the 13-byte compressed position. Unlike the uncompressed §12 complete weather report, there is **no leading `ddd/sss` wind extension** — the wind is carried by the compressed cs course/speed (already decoded), so the block begins directly at the gust/temperature fields and is parsed by the same shared gust-onward tail parser used by the positionless and uncompressed-complete forms. Decodes gust / temperature / rain (1 h / 24 h / since-midnight) / humidity / pressure / luminosity. **Gated on at least one weather field actually parsing**, so a `_`-symbol compressed position carrying a plain free-text comment is reported as a comment, never as empty weather (no confidently-wrong output). **Anchored to the aprslib reference decoder** — three vectors (no-cs weather, cs-present weather, and the comment-gate case) reproduce aprslib's decoded gust/temp/humidity/pressure exactly (APRS-native units). Fuzz-clean (~6.3M execs) incl. the new weather seeds. This closes the last APRS gap noted in v0.509; no new tool — extends `aprs_packet_decode` (`internal/aprs`); registry unchanged. Shipped alongside a stability-hardening continuation of v0.510: FuzzDecode harnesses added to 33 more decoder packages (the remaining un-fuzzed binary + text parsers — arpdecode/bfd/imap/ntlm/redis/sip/syslog/… ), all swept clean over millions of execs and now permanent `go test` regression coverage.
- `aprs_packet_decode` compressed position (APRS101 §9) — ✅ v0.509.0 — re-surveyed off the (now-exhausted) credential / key-file veins back into the project's **ham-RF domain** and closed the last open APRS position-format gap: the **base-91 compressed position** (APRS101 §9), previously dispatched-but-deferred ("a separate format with base-91 lat/lon encoding"). A compressed position packs symbol-table + 4-byte latitude + 4-byte longitude + symbol-code + a 2-byte cs + a 1-byte type into 13 bytes; modern trackers and APRS-IS feeds use it heavily. Decodes `latitude_deg` (90 − base91/380926) + `longitude_deg` (−180 + base91/190463) + symbol, plus the cs+type extension to exactly one of **course_deg + speed_knots**, **altitude_ft**, or **radio_range_mi** (kept in APRS-native units; a space cs byte = no extension), dispatched by the type byte (`type&0x18==0x10` → altitude; `c1` 0..89 → course/speed; `c1==90` → range) exactly as the reference decoders do. Compressed vs §8 uncompressed is detected unambiguously (compressed leads with a `/`/`\`/A-Z/a-j symbol-table char; uncompressed with a digit). This completes APRS position coverage: **uncompressed (§8) + Mic-E (§10) + compressed (§9)**. **Anchored to the `aprslib` Python reference library** (venv-oracle): five vectors — course/speed, no-cs, altitude, radio-range, and a `=`-with-comment case — reproduce aprslib's decoded lat/lon/course/speed/altitude/range exactly (converted to APRS-native units: knots = aprslib km/h ÷ 1.852, feet = m ÷ 0.3048, miles = km ÷ 1.609344). Fuzz-clean (~9M execs, 0 crashes) incl. the new compressed seeds; an uncompressed-still-works regression guards the dispatch. No new tool — extends `aprs_packet_decode` (`internal/aprs`); registry unchanged. The compressed-position **complete weather report** (a §9 position whose symbol code is `_`, trailing a weather block) remains the noted follow-up.
- `pem_privkey_decode` (PEM private-key triage) — ✅ v0.508.0 — completed the **key-file loot-triage trio** (after `ssh_privkey_decode` v0.506 and `putty_privkey_decode` v0.507) with the **openssl-key counterpart**: a stolen `.pem` / `.key` (TLS server key, client-cert key, API key) is top loot, and this answers the same triage questions across the openssl PEM family — PKCS#1 (`BEGIN RSA PRIVATE KEY`), SEC1 (`BEGIN EC PRIVATE KEY`), PKCS#8 (`BEGIN PRIVATE KEY`), and both encrypted forms (traditional `Proc-Type`/`DEK-Info`, and PKCS#8 `BEGIN ENCRYPTED PRIVATE KEY`). **encrypted?** → if yes, crack with openssl / pem2john + John before use; **algorithm + size** — RSA (+ modulus bits, with an **RSA-1024 weak-key warning**), ECDSA (+ curve), Ed25519, DSA (the type/size of an *encrypted* key live in the ciphertext and are reported unavailable, never guessed); **public_sha256** (unencrypted) — the SHA-256 of the SubjectPublicKeyInfo DER, to correlate the key with a known cert / TLS endpoint; **cipher + KDF** (encrypted) — traditional cipher + IV len (legacy MD5 KDF), or PKCS#8 PBES2 cipher + IV + the KDF: **PBKDF2** (salt len + iterations + PRF) or **scrypt** (N / r / p) — the crack cost. Native — own code + Go stdlib: `crypto/x509` parses the standard unencrypted key DER (hand-rolling RSA/EC DER would reinvent stdlib poorly and risk a confidently-wrong decode), and the part stdlib won't do passphrase-free — reading the cipher/KDF params out of an encrypted key — is a hand-rolled `encoding/asn1` walk of the `EncryptedPrivateKeyInfo` (PBES2 → PBKDF2/scrypt + encryption scheme). No PEM/PKCS library added to go.mod. **Anchored to openssl**: a generated EC P-256 (`535d736b…`), Ed25519 (`ae34dd3b…`) and RSA-1024 (`55116c99…`) key reproduce `openssl pkey -pubout -outform DER | sha256sum` exactly; the encrypted vectors' cipher / KDF / salt-len / iterations (PBKDF2 20000, hmacWithSHA256) / N,r,p (scrypt 16384/8/1) / IV-len reproduce `openssl asn1parse` exactly. Every recognised OID is vector-checked; an unrecognised algorithm/cipher/KDF/PRF OID is surfaced as its dotted string with `(unrecognized)`, never guessed. A non-PEM blob / un-parseable DER is rejected; an OpenSSH-format key is redirected to `ssh_privkey_decode`. Fuzz-clean (~7.3M execs, 0 crashes/hangs). `internal/pemkey`. Registry 530→531.
- `putty_privkey_decode` (PuTTY .ppk private-key triage) — ✅ v0.507.0 — continued the **key-file loot triage** vein opened by `ssh_privkey_decode`, adding its **Windows counterpart**: a saved PuTTY / WinSCP / FileZilla / pscp `.ppk` key is the same top-tier loot as a stolen `id_ed25519`, and it answers the same triage questions. **encrypted?** (`encryption` none / aes256-cbc + flag → if yes, putty2john + John the Ripper; if no, directly usable); for a v3 encrypted key the **Argon2 KDF** flavour (Argon2id/i/d) + memory / passes / parallelism / salt length (crack cost); the **key type + SHA256 fingerprint** (a .ppk's `Public-Lines` block is byte-for-byte the **same SSH-wire public blob** as an OpenSSH `.pub`, so the fingerprint correlates the key with an `authorized_keys` entry); and the **comment** — which, unlike an OpenSSH key, is a **cleartext header readable even when the key is encrypted**. The format is the documented line-based `PuTTY-User-Key-File-N` text container (PuTTY AppendixC). Native — line parsing + base64 + `crypto/sha256`, stdlib only, no new go.mod dep (a PPK library would be a runtime dep for a few dozen lines of text parsing). **Verification: the key-type + fingerprint are cross-validated against `ssh-keygen -l`** — a .ppk built from a generated ed25519 (alice@victim → `SHA256:RYwu7r7v81keCa5t4NM5dGBCBmoH1M80rW8RJt3HlKY`) and rsa-2048 (svc@host → `SHA256:AWeSx77RBL5vG83pduVmy6GNYeQr2qZTvlOGE+S6UfU`) key reproduces ssh-keygen's exact fingerprint, type and comment; the header fields (version 2/3, Encryption, Comment, Key-Derivation, Argon2-*, Private-MAC) follow the AppendixC spec verbatim (plain-text reads — no transform that could be confidently wrong). The Private-Lines section (PuTTY's own private layout) and the Private-MAC are surfaced/raw, not decoded or verified — MAC verification needs the passphrase-derived key, and a wrong valid/invalid verdict would be worse than none. Block-count consumption and line counts are bounds-checked; CRLF tolerated; fuzz-clean (~9.6M execs, 0 crashes/hangs). No `puttygen` was available to produce an end-to-end oracle, so the security-critical decode (fingerprint/type, the only transform) is cross-validated against `ssh-keygen` instead — a stronger anchor than a single tool's self-output. `internal/puttykey`. Registry 529→530.
- `ssh_privkey_decode` (OpenSSH private-key triage) — ✅ v0.506.0 — pivoted off the (now-exhausted) recognized-credential-format vein into **key-file loot triage**, a fresh area. A stolen private key (`id_ed25519` / `id_rsa`) is top pentest loot, and the openssh-key-v1 format exposes — from the **public** portion, readable even when the key is encrypted — everything that drives the next step: **encrypted?** (cipher + flag → if yes, ssh2john + hashcat `-m 22921`; if no, directly usable), the **bcrypt KDF** rounds + salt length (crack cost), the **key type** + **SHA256 fingerprint** (exactly as `ssh-keygen -l` prints, to correlate the key with an `authorized_keys` entry / known target), and — for an unencrypted key — the **comment** (often `user@host`; for an encrypted key it lives in the encrypted section and is reported unavailable, never guessed). Native — base64 + an SSH-wire length-prefixed walk + SHA-256 (x/crypto/ssh refuses to even parse an encrypted key without the passphrase, defeating triage). **Anchored to `ssh-keygen -l`**: generated ed25519 + rsa keys (encrypted and not) reproduce its exact SHA256 fingerprint / type / encrypted state / comment byte-for-byte. Length fields bounds-checked; fuzz-clean. No new go.mod dep (`internal/sshkey`). Registry 528→529.
- `dcc2` (Domain Cached Credentials v2 / mscash2 compute + verify) — ✅ v0.505.0 — the remaining recognized-but-uncomputable AD credential format, completing the credential-compute set: DCC2 is how Windows (Vista / Server 2008+) caches domain logons locally, dumped from a compromised workstation's SECURITY hive (impacket secretsdump), hashcat mode 2100. Unlike an NT hash it **cannot be passed-the-hash** — it's offline-crack-only — so the compute/verify side is the gap (`hash_identify` flags `$DCC2$`, `hash_crack` attacks it, but neither produced/checked one). Algorithm: DCC1 = MD4(MD4(password)‖lower(username)) (UTF-16LE); DCC2 = PBKDF2-HMAC-SHA1(DCC1, lower(username), iterations, 16); stored `$DCC2$<iters>#<username>#<hex>` (default 10240). Compute (username = the salt, optional iterations) or verify a candidate (username+iterations taken from the hash, constant-time; malformed rejected). **Strongest verification class — gated byte-for-byte against the canonical hashcat mode-2100 example** `$DCC2$10240#tom#e4e938d12fe5974dc42a90120bd9c90f` (password `hashcat`, the independent anchor) + pycryptodome-confirmed vectors; round-trip + case-insensitive-salt + malformed tests; fuzz-clean. Native — composes the in-tree **MD4** (`internal/nthash`) + **PBKDF2** (`internal/wpa`), no new go.mod dep (`internal/dcc2`). Registry 527→528.
- `krb_roast_hashcat` AES support — ✅ v0.504.0 — the noted v0.503 follow-up: extended the roast-line builder to **AES (etype 17/18)** tickets, now the modern AD default. The AES cipher splits **edata ‖ checksum(12)** — the checksum is the LAST 12 bytes, opposite to RC4's leading 16 — so getting this wrong would be confidently-wrong; the format is anchored to **impacket's GetUserSPNs / GetNPUsers AES output verbatim**: AES Kerberoast → `$krb5tgs$<17|18>$user$realm$*spn*$checksum$edata` (hashcat **-m 19600** AES128 / **-m 19700** AES256, the SPN `*`-wrapped alone with `:`→`~`); AES AS-REP roast → `$krb5asrep$<17|18>$user$realm$checksum$edata` (no standard hashcat mode — flagged for John). The RC4 path also gained the `:`→`~` SPN convention. Verified against hand-built AES TGS-REP (etype 18, SPN `MSSQLSvc/db.corp.local:1433`) + AES AS-REP (etype 17) DERs reproducing impacket's exact lines byte-for-byte; fuzz-clean. No new tool — completes `krb_roast_hashcat` (`internal/krbroast`); registry unchanged.
- `krb_roast_hashcat` (Kerberoast / AS-REP-roast crack-line builder) — ✅ v0.503.0 — the Kerberos sibling of v0.502's `netntlm_hashcat`, closing the capture→crackable-hash step for the **#1/#2 AD credential attacks**. From a captured KDC response: an **AS-REP** (user with pre-auth disabled — its [6] enc-part is encrypted with the user's password key) → `$krb5asrep$23$user@REALM:checksum$edata` (hashcat **-m 18200**); a **TGS-REP** (its service ticket's enc-part is encrypted with the service account's key) → `$krb5tgs$23$*user$REALM$spn*$checksum$edata` (hashcat **-m 13100**, SPN taken from the service ticket's sname). Required extending `kerberos_decode` to surface the AS-REP/TGS-REP [6] enc-part etype+cipher and the service ticket's enc-part cipher (reusing the v0.501 EncryptedData walker, now capturing the cipher bytes). The RC4 (etype 23) cipher is split checksum(16)‖edata as hashcat expects; an **AES (etype 17/18)** enc-part places its checksum differently (modes 19600/19700) and is reported as not-yet-supported rather than mis-split (no confidently-wrong output). **Anchored to hand-built, spec-conformant AS-REP + TGS-REP DERs** (raw-TLV-assembled to avoid pyasn1 friction, with known enc-parts; the EncryptedData parse is the same one verified against the impacket Ticket) producing the canonical hashcat 18200 / 13100 lines byte-for-byte. Fuzz-clean. Native — reuses the in-tree `internal/kerberos` decoder, no new go.mod dep (`internal/krbroast`). Registry 526→527.
- `netntlm_hashcat` (NetNTLM crack-line builder) — ✅ v0.502.0 — closes the **capture→crackable-hash** step of the SMB-relay / Responder / NTLM-over-HTTP loot workflow, complementing `ntlm_decode` (which surfaces the server challenge + NtChallengeResponse but left the operator to hand-assemble). From a captured NTLMSSP **AUTHENTICATE** message + the 8-byte **server challenge** (supplied directly, or extracted from the prior **CHALLENGE** message), it emits the ready-to-crack hashcat line: **NetNTLMv2 (`-m 5600`)** `user::domain:serverChallenge:NTProofStr:blob` (NtChallengeResponse = NTProofStr(16)‖blob), or **NetNTLMv1 (`-m 5500`)** `user::domain:LMresp:NTresp:serverChallenge` when the NtChallengeResponse is exactly 24 bytes (NTLM-downgrade capture). The version is chosen by the response length; a non-AUTHENTICATE input or a non-8-byte challenge errors rather than emitting a malformed line. **Anchored byte-for-byte to the canonical hashcat mode-5600 example** (`admin::N46iSNekpT:08ca45b7d7ea58ee:88dcbe…:5c7830…`): an AUTHENTICATE message built via impacket to embed that example's user/domain/NtChallengeResponse, plus its server challenge, reproduces the example line exactly. Reuses the in-tree `internal/ntlm` parser (no new dep, one-way import); fuzz-clean. Native (`internal/netntlm`). Registry 525→526.
- `kerberos_decode` bare-Ticket decode + `ccache_decode` ticket chain — ✅ v0.501.0 — completed the AD-loot chain from v0.499/v0.500: `kerberos_decode` previously only **measured** the embedded ticket's byte length; it now decodes a **bare [APPLICATION 1] Ticket** (RFC 4120 §5.3) — the cleartext outer fields **tkt-vno / realm / service principal (sname) / enc-part etype + kvno** — and decodes the same for the **embedded ticket of an AS-REP / TGS-REP** (not just its length). An **etype 23 (RC4)** ticket is flagged **Kerberoastable / hashcat-13100-crackable** — the key triage signal. `ccache_decode` now **chains each credential's ticket blob into this Ticket decoder** (`inner_ticket`), so a pasted ccache directly surfaces every ticket's target service + encryption (reusing the existing `decodePrincipalName` / `decodeGeneralString` ASN.1 helpers; no import cycle, no new tool). **Anchored to an authoritative impacket-built Ticket DER** (sname HTTP/web.corp.local@CORP.LOCAL, etype 23) — and the rebuilt ccache vector embedding it is double-confirmed by `klist -cf` which lists the same `Ticket server: HTTP/web.corp.local@CORP.LOCAL`. Fuzz-clean on both the new Ticket path and the chain. Native — reuses the in-tree Kerberos ASN.1 walker, no new go.mod dep (`internal/kerberos` + `internal/ccache`); registry unchanged.
- `ccache_decode` (MIT Kerberos credential cache parser) — ✅ v0.500.0 — the natural keytab sibling and the **pass-the-ticket** loot complement to v0.499's keytab (pass-the-key): an on-disk `/tmp/krb5cc_*` / KRB5CCNAME file or a Rubeus / Mimikatz dump holds **live Kerberos tickets**, and a TGT (server `krbtgt/…`) is the golden-ticket / delegation pivot (flagged). Parses the v0x0504 FILE format: the default principal, then per credential the **client** + **server** principals, session **key_type** + key, the validity **times** (auth/start/end/renew-till → RFC 3339), the **ticket_flags** decoded to names (forwardable / renewable / initial / pre-authent / ok-as-delegate / …), and the embedded **ticket** (hex — the pass-the-ticket blob). Length fields are bounds-checked, hostile address/authdata counts capped, truncated credentials rejected; legacy 0x0501–0x0503 reported rather than mis-decoded. **Anchored end-to-end to the authoritative MIT `klist -cf`**: a hand-built ccache is confirmed to list `alice@EXAMPLE.COM`, service `krbtgt/EXAMPLE.COM@EXAMPLE.COM`, the three times, and `Flags: FRIA` — the oracle also settled the v4 keyblock's version-specific `etype` field and the exact bit→name flag mapping — and the same bytes parse identically here. The sibling-bug sweep from v0.499 (the keytab MinInt32 panic) confirmed no analogous signed-length path exists; fuzz-clean (~11.5M execs, all-unsigned-length design). Native — `encoding/binary` length-prefixed walk, stdlib only, no new go.mod dep (`internal/ccache`). Registry 524→525.
- `keytab_decode` (MIT Kerberos keytab parser) — ✅ v0.499.0 — fresh **AD-pentest loot** target and the file-format complement to `kerberos_decode` (which dissects the Kerberos *wire protocol*). A `.keytab` recovered from a compromised host holds the long-term Kerberos keys of the principals it serves — used for offline ticket forging (**silver tickets**), **pass-the-key / overpass-the-hash**, and for the **RC4 (etype 23)** entries the account's **NT hash** directly (flagged). Parses the v0x0502 format: per entry the **principal** (components joined `/` + `@realm`), realm + components, **name_type** (KRB5_NT_PRINCIPAL / SRV_HST / …), **kvno** (8-bit or the 32-bit extended form), **enctype** id + name (aes256 / aes128 / arcfour-RC4 / des3 / …), timestamp, and the raw **key** (hex); deleted/hole entries (negative size) are counted and skipped; the legacy 0x0501 host-byte-order variant is reported rather than mis-decoded. **Anchored to the authoritative MIT `ktutil`**: a hand-built keytab (HTTP/web.example.com@EXAMPLE.COM, kvno 5, etype 18, 32-byte key) is confirmed by `ktutil rkt … / list -e -t` to list exactly those values, and the same bytes parse identically here. **The fuzz harness immediately caught a real panic** — a MinInt32 (0x80000000) entry size on the deleted-entry path negated back to negative and drove the offset out of range; fixed with int64 promotion + a bounds guard, locked in as a corpus regression + explicit test, then 30s/15.3M-exec fuzz-clean. Native — `encoding/binary` length-prefixed walk, stdlib only, no new go.mod dep (`internal/keytab`). Registry 523→524.
- `saml_decode` (SAML 2.0 message decode) — ✅ v0.498.0 — continued the **web-auth decode stack** (jwt_decode + paseto_decode + now SAML), the SSO triage trio. A SAMLRequest / SAMLResponse captured from an SSO flow is standard pentest loot, and **golden-SAML** (forged/unsigned assertions) is a top AD-federation attack. `saml_decode` auto-detects the binding — **HTTP-Redirect** (base64(raw-DEFLATE(xml)), the GET-redirect form) vs **HTTP-POST** (base64(xml)) — tolerating percent-encoding (URL-pasted) and base64url, and surfaces the decoded **xml** (always, the source of truth) plus the extracted message_type / issuer / destination / id / version / issue_instant / in_response_to / **name_id** (the asserted identity) / status_code / Conditions (NotBefore/NotOnOrAfter) / audiences, and — crucially for golden-SAML / unsigned-assertion triage — a **signature_count + signature_present** flag (whether an XML-DSig Signature element is present). Fields are extracted by a namespace-agnostic local-name token scan with the raw XML as the source of truth (an absent field is empty, never guessed); a value that is neither DEFLATE-compressed XML nor raw XML is rejected; the inflate is size-bounded (LimitReader) against a hostile blob. XML-DSig signature **verification** (canonicalization + cert trust) and EncryptedAssertion decryption are deferred — it reports whether a signature is present (the attack-surface signal), not whether it validates. **Verification: the base64+DEFLATE path is anchored to an independent redirect-binding vector produced by Python zlib** (the same standard DEFLATE real IdPs/SPs emit) that inflates to its exact source XML, plus a POST-binding signed Response whose every field is asserted; percent-encoded + malformed cases covered; fuzz-clean (~3.6M execs, 0 crashes/hangs). Native — `encoding/base64` + `compress/flate` + `encoding/xml`, stdlib only, no new go.mod dep (`internal/saml`). Registry 522→523.
- `paseto_decode` (PASETO token decode + Ed25519 verify) — ✅ v0.497.0 — decisively diversified off the binary-serialization vein into the **web-token decode stack**: the **PASETO counterpart of `jwt_decode`**. PASETO ("Platform-Agnostic Security Tokens") is the modern signed/encrypted token format positioned as the safer JWT alternative (no algorithm confusion, versioned crypto), increasingly seen in modern APIs. Paste a token from an Authorization header / cookie / API body and get its structure — and, for the **public** (signed, not encrypted) variants, the **cleartext claims without any key** (exactly like jwt_decode reads a JWT body). A token is `version.purpose.payload[.footer]` (base64url); for public tokens the payload is `message ‖ signature` (v2/v4 Ed25519 = 64 B, v3 ECDSA P-384 = 96, v1 RSA-PSS = 256), so claims + signature are surfaced; for **local** (encrypted) tokens only the structure is visible (payload as length + hex + note — decryption needs the symmetric key); the footer (often a `kid`) is decoded. Optionally **verifies** v2/v4 public signatures with a provided Ed25519 public key — the authenticity check, computed natively over the PASETO **Pre-Authentication Encoding (PAE)** via `crypto/ed25519`. **Anchored to the official PASETO v4 test vectors** (`paseto-standard/test-vectors`): 4-S-1 decodes to its exact cleartext claims and verifies true with its published public key; a tampered token and a wrong implicit assertion verify false; 4-S-2's footer decodes; the v4.local 4-E-1 payload is surfaced encrypted, never guessed. Malformed tokens (wrong part count / bad base64url / payload shorter than the signature) are rejected. v1 (RSA) / v3 (ECDSA) verify and local decryption are deferred. Two fuzz harnesses (decode + verify), both clean. Native — base64url + `crypto/ed25519`, stdlib only, no new go.mod dep (`internal/paseto`). Registry 521→522.
- `bson_decode` Decimal128 decode — ✅ v0.496.0 — the noted v0.495 follow-up: decoded the BSON **Decimal128** type (deferred at v0.495 as raw hex) from its **IEEE 754-2008 Binary-Integer-Decimal** form to sign + 113-bit coefficient + biased exponent + an **exact plain (non-scientific) decimal value** (preserving Decimal128's unnormalized scale/trailing zeros), with **NaN / ±Infinity** surfaced as such; the raw 16 bytes are kept for traceability. Decimal128 carries the financial/scientific values in MongoDB documents, so this turns the last raw-hex BSON type into a usable number. The fiddly 128-bit BID layout (sign / 14-bit exponent at bits 62–49 / 113-bit coefficient via `math/big`, plus the Inf/NaN/large-coefficient combination forms) was **anchored byte-for-byte against PyMongo `Decimal128` vectors** — coefficient + exponent taken straight from the library's `Decimal.as_tuple()` (1.50→150×10⁻², -7.50E+3→750×10¹, a 30-digit coefficient, 1.234E-10, ±0, NaN, ±Infinity). The structural components (not a re-derived canonical string) are the decode, so there is no confidently-wrong E-notation-formatting risk; the plain value is the exact coefficient×10^exp. Fuzz-clean through the new `math/big` path. No new tool — completes `bson_decode` (`internal/bson`); registry unchanged.
- `bson_decode` (BSON document dissector) — ✅ v0.495.0 — continued the binary-serialization decode vein: BSON is the format MongoDB stores and that `mongodump` writes to `.bson` files (standard MongoDB loot). It is the **document-format complement to `mongodb_decode`** — which dissects the MongoDB *wire protocol* and only shallowly extracts a command name + a few argument fields (its `bsonValueLength` walker just skips values and doesn't even handle several element types). `bson_decode` **fully, recursively decodes a standalone BSON document** into a structured tree, like `cbor_decode` / `msgpack_decode` do for theirs. Decodes every element type: double, int32, int64, bool, null, UTC datetime (→ RFC 3339 + unix ms), timestamp (increment + seconds), string (UTF-8, length-validated), ObjectId (→ hex), binary (length + subtype → subtype name + data hex), embedded document + array (recursive, order-preserving), regex (pattern + options), JavaScript code + code-with-scope, the deprecated symbol / DBPointer / undefined, min/max key, and Decimal128 (surfaced as raw 16 bytes — the IEEE 754-2008 decimal-string decode deferred to avoid a confidently-wrong value). A truncated/malformed document is rejected with an error (never a partial decode), length fields are bounds-checked, nesting is depth-capped, and trailing bytes are surfaced. **Strongest verification class — every element type gated byte-for-byte against the reference PyMongo `bson` library** (venv-oracle): string, int32/64, double, bool/null, array, nested doc+array, ObjectId, datetime, binary (generic + UUID subtype), regex, timestamp, decimal128, min/max key. Fuzz-clean (~9.3M execs, 0 crashes/hangs). Native — recursive-descent over a byte cursor, no new go.mod dep (`internal/bson`). Registry 520→521.
- `msgpack_decode` Timestamp ext decode — ✅ v0.494.0 — the noted v0.493 follow-up: decoded the **MessagePack Timestamp extension (ext type -1)** to RFC 3339 across all three wire layouts — **32-bit** (uint32 seconds), **64-bit** (30-bit nanoseconds + 34-bit seconds), and **96-bit** (uint32 nanoseconds + *signed* int64 seconds, so pre-epoch times decode correctly). Timestamps are pervasive in msgpack payloads (Redis stream entry IDs, API record fields), so this turns the most common ext type from raw hex into a usable time. A non-standard Timestamp length, or a nanoseconds field ≥ 1e9, is left raw with a note rather than decoded into a confidently-wrong time; other ext types stay raw. Anchored byte-for-byte to reference `msgpack` library vectors for all three layouts (2018-01-01Z 32-bit, +1µs 64-bit, 1900-01-01Z pre-epoch 96-bit) + malformed-length/out-of-range-nanos cases; fuzz-clean. No new tool — extends `msgpack_decode` (`internal/msgpack`); registry unchanged.
- `msgpack_decode` (MessagePack dissector) — ✅ v0.493.0 — deliberately diversified off the credential vein (5 consecutive credential ships) into the **binary-serialization decode space**: the msgpack sibling of `cbor_decode` / `protobuf_decode`. MessagePack (https://msgpack.org) is the compact binary serialization used by Redis internals, msgpack-RPC, many web/API backends, mobile-sync protocols and game-server traffic — an operator pastes the hex of a captured blob (packet payload, cache value, stored token) and gets the decoded tree without a throwaway script. Decodes every core type: nil/bool, positive/negative fixint, uint8/16/32/64, int8/16/32/64 (sign-extended), float32/float64, str (fixstr/str8/16/32, UTF-8 with a non-UTF-8→hex+note fallback), bin (bin8/16/32→hex), array + map (fix/16/32, recursive, order- and duplicate-preserving), and ext (fixext1..16/ext8/16/32 → type + data hex; the Timestamp ext type -1 is flagged but surfaced raw, time decode deferred to avoid a confidently-wrong value). Each node reports its wire `format` so the exact encoding is visible. A truncated/malformed blob is rejected with an error (never a partial/guessed decode), the reserved 0xc1 tag is rejected, hostile huge length fields are bounded (no giant up-front alloc), nesting is depth-capped, and trailing bytes after the top-level value are surfaced as `trailing_bytes_hex`. **Strongest verification class — every format family gated byte-for-byte against the reference `msgpack` Python library** (venv-oracle): nil/bool, fixint/neg-fixint, the uint/int widths, float32/64, fixstr/str8, bin8, fixarray, fixmap, fixext4, and a nested `{"x":[1,true,nil],"y":"z"}` vector. Fuzz-clean (~5.6M execs, 0 crashes/hangs — the length-bounding holds). Native — recursive-descent over a byte cursor, no new go.mod dep (`internal/msgpack`). Registry 519→520.
- `cisco_type8` (Cisco IOS type-8 secret compute + verify) — ✅ v0.492.0 — the compute/verify side of the **Cisco modern `enable secret`** algorithm (introduced 2013, hashcat mode 9200), the network-device-config analogue of the DB-credential computes: `hash_identify` already flags `$8$…` as Cisco type 8 and `hash_crack` attacks it, but neither could produce or check one (the reversible older type 7 is handled by `cisco_type7_decode`). Type 8 is **PBKDF2-HMAC-SHA256** with an 80-bit salt and 20,000 iterations encoded in the Cisco base64 alphabet: `$8$<14-char salt>$<43-char digest>`, where the salt fed to PBKDF2 is the **ASCII of the salt string itself** (a Cisco quirk) and the 32-byte output is encoded big-endian over the `./0-9A-Za-z` alphabet. The exact construction (salt interpretation + big-endian base64 ordering) was **pinned empirically against the canonical hashcat-9200 vector** `$8$TnGX/fE4KGHOVU$pEhnEvxr…` (password `hashcat`) **plus a second vector cracked to `cisco`** — both reproduced byte-for-byte in the tests (no confidently-wrong risk; I refused to ship until the published vector matched). Compute (optional 14-char salt, random if omitted) or verify a candidate (constant-time; a malformed hash — wrong prefix / salt or digest length / alphabet — is rejected, never silently "verified"). Type 9 (`$9$`, scrypt) is deliberately deferred (scrypt is not in the standard library; a wrong parameterisation would be confidently-wrong — hash_identify flags it for cracking). PBKDF2-HMAC-SHA256 reuses the in-tree `internal/wpa.PBKDF2`; fuzz-clean. Native — `internal/wpa.PBKDF2` + `crypto/sha256`, stdlib only (`internal/ciscopw` type8.go). Registry 518→519.
- `postgres_scram` (PostgreSQL SCRAM-SHA-256 verifier compute + verify) — ✅ v0.491.0 — the **modern PostgreSQL credential primitive**: scram-sha-256 is the **default since PostgreSQL 10** (2017) and the successor to the md5 verifier shipped in v0.487, completing the PG credential pair. The value stored in `pg_authid.rolpassword` is `SCRAM-SHA-256$<iter>:<b64 salt>$<b64 StoredKey>:<b64 ServerKey>` (hashcat mode 28600), derived per RFC 5802: SaltedPassword = PBKDF2-HMAC-SHA256(password, salt, iter); ClientKey = HMAC(SP, "Client Key"); StoredKey = SHA256(ClientKey); ServerKey = HMAC(SP, "Server Key"). Compute the verifier (optional base64 salt + iterations, default 4096), or verify a candidate by recomputing StoredKey from the verifier's own salt/iterations and constant-time comparing; a malformed verifier (wrong prefix/field-count/base64/iteration-count) is rejected, never silently "verified" (with an iteration-count cap so a hostile verifier can't wedge PBKDF2). **Strongest verification class — anchored byte-for-byte to the RFC 7677 §3 worked example** (password `pencil`, salt `W22ZaJ0SNY7soEsUEjb6gQ==`, i=4096): the derivation reproduces the RFC's ClientProof (`p=`) and ServerSignature (`v=`) exactly (confirmed via an independent Python computation), and the resulting verifier string is pinned in the test. PBKDF2-HMAC-SHA256 reuses the in-tree `internal/wpa.PBKDF2` (the same primitive `pbkdf2_password` / `wpa_pmk_derive` use) — no new dep. Full SASLprep of non-ASCII passwords is deferred (no-op for ASCII, the common case; raw UTF-8 otherwise, flagged). Round-trip + malformed-input tests; fuzz-clean (~5M execs, 0 crashes). Native — `internal/wpa.PBKDF2` + `crypto/hmac` + `crypto/sha256` + base64, stdlib only (`internal/pgscram`). Registry 517→518.
- `postgres_password` (PostgreSQL md5 password compute + verify) — ✅ v0.487.0 — the **DB-credential sibling of `mysql_password`**, covering the PostgreSQL md5-authentication value stored in `pg_authid.rolpassword` / `pg_shadow.passwd` (hashcat mode 12) — a standard `pg_dumpall --globals` loot format. PostgreSQL's `pg_md5_encrypt` salts the password with the **role name** before a single MD5: the stored value is `md5` + hex(MD5(password ‖ username)) — the literal `md5` + 32 lowercase hex. Because the salt is the username, the same password under two roles yields different values, so verification requires the username as well as the password. Compute the stored value, or verify a candidate against a captured value (`md5`-prefixed or bare 32-hex, either case, constant-time; a malformed value is rejected, never silently "verified"). `hash_identify` deliberately does **not** flag this format — `md5`+32hex is ambiguous with a raw MD5, so adding an identify branch would be confidently-wrong; the tool stands alone. SCRAM-SHA-256 (the PG 10+ default, hashcat 28600 — salted + iterated PBKDF2/HMAC) is out of scope. Strongest verification class — the construction is unambiguous (single salted MD5, no rounds) and is exactly the documented `pg_md5_encrypt`, gated against the stdlib-hashlib oracle; round-trip (incl. empty/unicode passwords + empty role) + bare/uppercase + username-salting + malformed-input tests; fuzz-clean (~5.3M execs, 0 crashes). Native — `crypto/md5` + `encoding/hex`, stdlib only (`internal/pgpassword`). Registry 516→517.
- `mysql_password` (MySQL / MariaDB mysql_native_password compute + verify) — ✅ v0.486.0 — the **MySQL credential primitive** and the next compute/verify gap-fill in the credential cluster: `hash_identify` already flagged this as **MySQL4.1+** (hashcat mode 300) and `hash_crack_dictionary` attacks it (its `mysql` branch), but neither could produce or check one — the same gap `nt_hash` / `ldap_password` filled for their formats. The hash is the value stored in `mysql.user` — an unsalted double SHA-1, `*` + `UPPER(hex(SHA1(SHA1(password))))`, a 41-character string (literal `*` + 40 uppercase hex; no per-row salt, so equal passwords share a hash). Compute a candidate's hash, or verify a candidate against a captured hash (`*`-prefixed or bare 40-hex, either case, constant-time compared; a malformed hash is rejected, never silently "verified"). The pre-4.1 OLD_PASSWORD (16-hex, hashcat 200 — obsolete since 2003) and the `caching_sha2_password` / `sha256_password` plugins (salted, iterated) are out of scope. Strongest verification class — the construction is unambiguous (double SHA-1, no rounds/salt), gated against the universally-published `PASSWORD('password') = *2470C0C06DEE42FD1618BB99005ADCA2EC9D1E19` vector + the hashcat-300 example; round-trip + bare/lowercase + malformed-input tests; fuzz-clean (~5.4M execs, 0 crashes). The same algorithm already backed the `mysql` branch of `hash_crack_dictionary`; this factors the compute/verify side into `internal/mysqlpw` so it is callable directly. Native — `crypto/sha1` + `encoding/hex`, stdlib only. Registry 515→516.
- `ldap_password` (LDAP RFC 2307 userPassword compute + verify) — ✅ v0.485.0 — the **directory-server credential primitive** and a new compute/verify gap-fill in the md5crypt / sha_crypt / bcrypt cluster: `hash_identify` already recognised `{SSHA}`/`{SHA}`/`{MD5}` (hashcat 111/101/1001) and `hash_crack` attacks them, but neither could produce or check one. Covers the OpenLDAP slapd / 389-DS / Dovecot / Atlassian Crowd `userPassword` schemes — `{SHA}`/`{SSHA}` + `{MD5}`/`{SMD5}` (RFC 2307) and the OpenLDAP pw-sha2 / Dovecot SHA-2 extensions `{SHA256}`/`{SSHA256}`/`{SHA384}`/`{SSHA384}`/`{SHA512}`/`{SSHA512}`. Every scheme is `{SCHEME}base64( H(password ‖ salt) ‖ salt )` with no key-stretching, so on verify the **salt length is recovered from the blob** (slapd's 4-byte default and Dovecot's longer salts both verify) and the digest is compared constant-time. Compute (scheme default `{SSHA}`; verbatim or random 4-byte salt; salt rejected for unsalted schemes) or verify a candidate against a captured value (e.g. a slapcat dump). Strongest verification class — the construction is unambiguous (single digest + base64, no rounds), gated byte-for-byte against the **OpenLDAP `slappasswd` oracle** (`{SHA}`/`{SSHA}`/`{MD5}`) and the definitional pw-sha2 / Dovecot vectors (SHA-2 variants); fuzz-clean (~5.9M execs, 0 crashes). `{CRYPT}` (RFC 2307 delegates to crypt(3) — covered by md5crypt / sha_crypt / bcrypt) and `{CLEARTEXT}` are out of scope. Native — `crypto/{md5,sha1,sha256,sha512}` + `encoding/base64`, stdlib only (`internal/ldappw`). Registry 514→515.
- `dcerpc_decode` sec_trailer NTLMSSP decode — ✅ v0.484.0 — sibling-sweep of the v0.483 SMB2 NTLMSSP work, applying the same signature-scan chain to **DCE/RPC** (the MS-RPC framing layer behind nearly every Windows AD attack chain). When `auth_length > 0` the fragment's last `auth_length + 8` bytes are the sec_trailer header (auth_type / auth_level / pad_length / reserved / auth_context_id) + the auth_value token; the decoder previously surfaced `auth_length` only. Now it parses the sec_trailer — naming `auth_type` (NTLMSSP / SPNEGO / Kerberos / NETLOGON / Schannel) + `auth_level` — and when the auth_value carries an **NTLMSSP** message (located by the unambiguous 8-byte `NTLMSSP\0` signature, so both raw and **SPNEGO-wrapped** auth_values are handled without parsing the SPNEGO ASN.1) decodes it in place via `internal/ntlm` (the NTLM-over-RPC relay / Pass-the-Hash capture: server challenge, target-info AV pairs, domain / user / workstation — the drsuapi/netlogon bind-time NTLM exchange). A Kerberos/Schannel auth_value (no NTLMSSP signature) surfaces auth_type/auth_level but its token body is left for `kerberos_decode` — the no-confidently-wrong path. Verified with NTLMSSP CHALLENGE in a BIND_ACK sec_trailer + SPNEGO-wrapped NTLMSSP in an AUTH3 + Kerberos-not-decoded + oversized-auth_length cases; extended the dcerpc fuzz harness with the sec_trailer seeds, fuzz-clean (~6.3M execs, 0 crashes). No new tool — extends `dcerpc_decode` (`internal/dcerpc` → `internal/ntlm`). (Confirmed no import cycle: `internal/ntlm` does not import `internal/dcerpc`.)
- `smb2_decode` SESSION_SETUP NTLMSSP decode — ✅ v0.483.0 — high-value AD-pentest chain: SMB2 SESSION_SETUP carries the auth token in its SecurityBuffer (NTLM / Kerberos), but the decoder previously didn't even parse SESSION_SETUP. Now it extracts the SESSION_SETUP request/response SecurityBuffer and, when it contains an **NTLMSSP** message — located by the unambiguous 8-byte `NTLMSSP\0` signature, so both raw and **SPNEGO-wrapped** blobs are handled without parsing the SPNEGO ASN.1 — decodes it in place via `internal/ntlm` (the NTLM-over-SMB relay / Responder / pass-the-hash capture: server challenge, target-info AV pairs, domain / user / workstation). Verified `kerberos.Decode`/`ntlm.Decode` both require the GSS-API wrapper pre-stripped, so the signature-scan approach is the clean no-confidently-wrong path; a Kerberos GSS-API token (no NTLMSSP signature) is left for `kerberos_decode`. Verified with NTLM CHALLENGE (response) + NEGOTIATE (request) + SPNEGO-wrapped + Kerberos-not-decoded vectors; added an smb2 fuzz harness, fuzz-clean through the SecurityBuffer→ntlm path. No new tool — extends `smb2_decode` (`internal/smb2` → `internal/ntlm`).
- `pppoe_decode` inner-IP decode — ✅ v0.482.0 — extended the chain-to-existing-decoder seam to **PPPoE** (the broadband-edge encapsulation — DSL/fibre CPE, ISP access). A PPPoE Session frame carries a PPP frame; when the PPP Protocol ID is 0x0021 (IPv4) / 0x0057 (IPv6) the subscriber's inner IP packet is now **decoded in place** via `internal/ipdecode`, surfacing the session's inner flow's addresses / protocol / ports. PPP control protocols (LCP / IPCP / PAP / CHAP / EAP) are left as hex; an IP-typed payload that fails to parse → `inner_decode_error` + raw hex. Verified with inner IPv4/UDP + IPv6/UDP + LCP-not-decoded + garbage cases; added a pppoe fuzz harness, fuzz-clean. No new tool — extends `pppoe_decode` (`internal/pppoe` → `internal/ipdecode`). (MSDP SA-encapsulated multicast is the last niche `ip_packet_decode` sibling.)
- `sflow_decode` sampled-header decode — ✅ v0.481.0 — extended the chain-to-existing-decoder seam to **sFlow** packet-sampling telemetry (the dominant datacenter monitoring export, UDP 6343). A Raw Packet Header flow record carries the first bytes of the sampled frame; when its header-protocol is Ethernet (1, IP after the 14-byte L2 header), IPv4 (11) or IPv6 (12), the sampled L3 packet is now **decoded in place** via `internal/ipdecode`, surfacing the sampled flow's addresses / protocol / ports. The capture is truncated to the sampled-header length so the decode may be partial (ipdecode degrades gracefully); a non-IP EtherType / non-IP header protocol is left as hex; IP-typed garbage → `inner_decode_error` + raw hex. **Also fixed a pre-existing doc bug**: the package's header-protocol name table in the doc comment listed "11 802.11 / 12 IPv4 / 13 IPv6" but the implementation (and the sFlow v5 spec) is "11 IPv4 / 12 IPv6 / 13 MPLS" — corrected. Verified with Ethernet-inner-IPv4 + direct-IPv4/IPv6 + ARP-not-decoded + garbage cases; added an sflow fuzz harness, fuzz-clean. No new tool — extends `sflow_decode` (`internal/sflow` → `internal/ipdecode`).
- `mpls_decode` inner-IP decode — ✅ v0.480.0 — extended the tunnel-decap pattern to **MPLS** (provider/enterprise label-switched networks, MPLS-over-GRE/UDP): after the bottom-of-stack label, when the payload is IP — an IPv4/IPv6 Explicit NULL bottom label, or a first nibble of 4/6 (the heuristic the package already computed) — the inner packet is now **decoded in place** via `internal/ipdecode`, surfacing the label-switched flow's addresses / protocol / ports. EoMPLS / pseudowire payloads (0-nibble control word, Ethernet) are left as hex; an IP-typed payload that fails to parse → `inner_decode_error` + raw hex. Verified with inner IPv4/UDP + IPv6/UDP + IPv4-Explicit-NULL + EoMPLS-not-decoded + garbage cases; added an mpls fuzz harness, fuzz-clean through the inner→ipdecode path. No new tool — extends `mpls_decode` (`internal/mpls` → `internal/ipdecode`).
- `gxrp_decode` GARP / GVRP / GMRP (IEEE 802.1D) — dynamic VLAN + multicast registration (VLAN-hopping recon) — ✅ v0.566.0 — **new tool** completing the project's Cisco/L2 **VLAN-attack** decoder family alongside `dtp` (trunk-negotiation VLAN-hopping), `vtp` (VLAN-database tampering) and `vqp` (VMPS VLAN assignment). **GARP** (Generic Attribute Registration Protocol) and its applications **GVRP** (VLAN) and **GMRP** (multicast) are an L2 attack surface: a host that emits GVRP **JoinIn** attributes can **register arbitrary VLANs onto the trunk** it is attached to (extending the trunk's allowed-VLAN set), a VLAN-hopping primitive; GMRP does the equivalent for multicast group membership. A captured PDU reveals which VLANs / multicast groups are being registered or withdrawn and by which **event** (Join / Leave / LeaveAll). Decodes the GARP framing — protocol id and the nested list of **messages** (each a type + a list of **attributes**: length, event + name, value), interpreting the attribute value by length: a 2-byte value is a **VLAN id** (GVRP), a 6-byte value a **group MAC** (GMRP), a 1-byte value a **GMRP service**. **No confidently-wrong output**: the GVRP-vs-GMRP application is keyed in the standard by the L2 destination MAC (01:80:c2:00:00:21 = GVRP, ...:20 = GMRP), which is **not carried in the PDU**, so the attribute kind is inferred from the value length (the lengths are non-overlapping and deterministic) and the **raw value is always surfaced** alongside, with a note naming the definitive signal; unknown value lengths are surfaced as raw hex only. Native — a byte-field read + two nested walks, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's GARP layer** (`scapy.contrib.gxrp`) — 200 random PDUs (1-3 messages × 1-4 attributes mixing GVRP VLAN / GMRP group-MAC / GMRP service values across all 6 events incl. LeaveAll) decode identically, zero mismatches; the multi-attribute GVRP PDU, the GMRP group, the GMRP service and the bad-length edge case unit-pinned. `internal/gxrp`. Registered as `gxrp_decode` (Low risk, pure offline parser); registry 566→567.
- `ripng_decode` RIPng (RFC 2080) — IPv6 distance-vector routing (routing recon / route-injection) — ✅ v0.565.0 — **new tool**, the IPv6 sibling of the project's `rip` (RIPv1/v2, IPv4). **RIPng** (UDP 521) is a routing-reconnaissance and route-injection surface: it carries no authentication, so a captured **Response** reveals every route a router advertises (each IPv6 prefix, route tag and hop-count metric), and because the protocol trusts any Response on the segment, an on-link attacker can **inject or blackhole routes**. Decodes the 4-byte header (command — request / response, version) and the array of 20-byte **route table entries** (128-bit prefix, route tag, prefix length, metric), correctly handling the two RFC 2080 special entries — the **next-hop RTE** (metric 0xFF — the address field is a gateway, not a route) and the **infinity metric** (16 — an unreachable / withdrawn route); the RFC 2080 whole-table request (a single `::/0`, metric-16 entry) is flagged. **No confidently-wrong output**: every byte maps to a defined field, and trailing bytes that do not form a whole entry are noted rather than guessed. Native — a byte-field read + an array walk, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's RIPng layer** (`scapy.contrib.ripng`) — 200 random messages (request / response, 1-5 entries mixing next-hop RTEs and routes with random prefix / tag / prefix-length / metric incl. infinity) decode identically, zero mismatches; the multi-route response (next-hop + route + infinity), the whole-table request and the bad-command / trailing-byte edge cases unit-pinned. `internal/ripng`. Registered as `ripng_decode` (Low risk, pure offline parser); registry 565→566.
- `tzsp_decode` TZSP (TaZmen Sniffer Protocol) — remote wireless-capture encapsulation (MikroTik sniffer recon) — ✅ v0.564.0 — **new tool** joining the project's wireless tooling (`ieee80211`, `marauder`) and tunnel-decap decoders (`gre`, `geneve`, `vxlan`, `mpls`). **TZSP** is the UDP encapsulation (default port 37008 / 0x9090) that **MikroTik RouterOS**, Aruba and other gear use to **stream sniffed wireless frames to a remote analyser** — a captured TZSP stream is a remote packet-capture feed: each datagram wraps one sniffed frame (802.11 / Ethernet / Prism / AVS) together with the radio metadata the sensor observed (RX **channel**, **RSSI**, SNR, link rate, FCS-error flag), so decoding it surfaces both the wireless-recon metadata and the encapsulated frame. Decodes the 4-byte header (version, **packet type** — RX / TX / CONFIG / KEEPALIVE / PORT, **encapsulated protocol**) and the tag list — RAW_RSSI / SNR (both signed), DATA_RATE (named Mb/s), TIMESTAMP, CONTENTION_FREE, DECRYPTED, **FCS_ERROR**, **RX_CHANNEL**, PACKET_COUNT, RX_FRAME_LENGTH, WLAN sensor id — lifting RSSI / SNR / rate / channel / FCS to top-level fields. **No confidently-wrong output**: standard tags are decoded by type; the complete encapsulated frame is surfaced as raw hex with its protocol named (for `ieee80211_decode` etc.) rather than partially parsed here, and unknown tags are surfaced as raw hex. Native — a byte-field read + a tag walk, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's TZSP layer** (`scapy.contrib.tzsp`) — 200 random datagrams (all packet types × all encapsulations, with byte/short RSSI, SNR, all 19 data-rate codes, channel, FCS and a random encapsulated frame) decode identically, zero mismatches; the RX-packet metadata set, the keepalive, the short-form signed RSSI and the data-rate table unit-pinned. `internal/tzsp`. Registered as `tzsp_decode` (Low risk, pure offline parser); registry 564→565.
- `igmpv3_decode` IGMPv3 (RFC 3376) — IPv4 multicast group membership v3 (multicast recon) — ✅ v0.563.0 — **new tool**, the v3 companion to the project's `igmp` (v1/v2), whose group-record + source-list structure differs enough to warrant its own decoder. **IGMPv3** is a network-reconnaissance source: a **Membership Report** reveals exactly which multicast groups a host has joined and — via the source-filter group records (**INCLUDE / EXCLUDE** mode) — which senders it accepts or blocks, exposing the host's multicast-based services (mDNS 224.0.0.251, SSDP 239.255.255.250, PTP, IPTV / streaming groups); a **Membership Query** reveals the active querier and its robustness / query-interval parameters. Decodes the header (type, **verified** checksum) and, for a **Query**, the Max-Resp-Time, group address, S flag, **QRV**, **QQIC** (both Max-Resp and QQIC via the RFC 3376 §4.1 floating-point code), the source list and the **query type** (general / group-specific / group-and-source-specific); for a **Report**, each **group record** — record type (MODE_IS_INCLUDE / EXCLUDE / CHANGE_TO_* / ALLOW_NEW_SOURCES / BLOCK_OLD_SOURCES), the multicast address and its source list. **No confidently-wrong output**: the 16-bit one's-complement checksum is verified and surfaced as `checksum_valid` (a failure is flagged, not hidden), and rare auxiliary record data is surfaced as raw hex. Native — byte-field reads + two array walks, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's IGMPv3 layer** (`scapy.contrib.igmpv3`) — 200 random messages (120 Reports with 1-4 group records of all 6 record types × varied source lists + 80 Queries incl. general / group / group-and-source with random QRV / QQIC / Max-Resp / S) decode identically with valid checksums, zero mismatches; the report, the group-and-source query, the general query and the §4.1 float encoding unit-pinned. `internal/igmpv3`. Registered as `igmpv3_decode` (Low risk, pure offline parser); registry 563→564.
- `vqp_decode` Cisco VQP / VMPS — dynamic VLAN assignment by MAC (VLAN-assignment recon) — ✅ v0.562.0 — **new tool** completing the project's **Cisco VLAN-attack** decoder family alongside `dtp` (DTP trunk-negotiation VLAN-hopping) and `vtp` (VTP VLAN-database tampering). **VQP** is the wire protocol of **VMPS** (VLAN Membership Policy Server, UDP 1589): a switch asks a server "what VLAN should this source MAC be on?" and the server answers with a VLAN name. Dynamic VLAN-by-MAC is an attack surface — a host that **spoofs a MAC the server maps to a privileged VLAN** (e.g. a voice / management VLAN — the **voiphopper / yersinia** technique) lands its access port in that VLAN, and a server that replies with **shutdownPort / accessDenied** is the lockout response. A captured exchange reveals the queried **MAC**, the switch **port** name, the VTP **domain** and the assigned **VLAN name**. Decodes the 8-byte header (version, **opcode** — requestPort / responseVLAN / requestReconfirm / responseReconfirm, **response code** — none / accessDenied / shutdownPort / wrongDomain, flag, sequence) and the list of **data entries** (each `datatype(4)+len(2)+value`): clientIPAddress → IPv4, Req/ResMACAddress → MAC, portName / VLANName / Domain → text; a denied response is flagged. **No confidently-wrong output**: typed entries are decoded by datatype, and the ethernetPacket / unknown entry bodies (plus any non-printable name) are surfaced as raw hex. Native — a byte-field read + a TLV walk, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's VQP layer** (`scapy.contrib.vqp`) — 200 random messages (all 4 opcodes × all response codes, with Domain / portName / Req|ResMAC / VLANName / IP entries) decode identically, zero mismatches; the VMPS request, the responseVLAN, and the accessDenied lockout response unit-pinned. `internal/vqp`. Registered as `vqp_decode` (Low risk, pure offline parser); registry 562→563.
- `rpl_decode` RPL (RFC 6550) — IPv6 routing for 6LoWPAN / 802.15.4 IoT mesh (routing-attack recon) — ✅ v0.561.0 — **new tool** joining the IoT decoder set (`ieee802154`, `zigbee`, `ndp`). **RPL** is the IPv6 routing protocol that builds the mesh (the "DODAG") of a low-power IoT network — 6LoWPAN over IEEE 802.15.4 — and is carried in **ICMPv6 type 155**. It is a recognised **IoT-routing attack** surface: a malicious node can advertise a forged low **rank** in a DIO to draw the mesh's traffic through itself (a **sinkhole / on-path** attack), or bump the DODAG **version** number to force a costly network-wide rebuild (a **DoS**) — both attacks are directly visible in a captured DIO. Decodes the ICMPv6 RPL header (type + the **message name** — DIS / DIO / DAO / DAO-ACK / DCO) and, for a **DIO**, the RPLInstanceID, **version**, **rank**, Grounded flag, **Mode of Operation** (+ name: storing / non-storing / …), DODAG preference, DTSN and **DODAGID**; for **DAO / DAO-ACK** the InstanceID, sequence, status and (when the D-flag is set) the DODAGID — with the headline rank + version surfaced under an attack note. **No confidently-wrong output**: only the fixed message header is decoded — the trailing RPL options (DODAG Config, Prefix / Transit / Target Info) are a varied TLV list and are surfaced as raw hex, and the ICMPv6 checksum (which covers an IPv6 pseudo-header not present in the message alone) is not verified. Native — bitfield/byte extraction, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's RPL layer** (`scapy.contrib.rpl` + `ICMPv6RPL`) — 250 random messages (200 DIO with random instance / version / rank / MOP / prf / DTSN / DODAGID + 50 DAO) decode identically, zero mismatches; DIS, the DIO field set, DAO, separator-tolerance and non-RPL/truncation rejection unit-pinned. `internal/rpl`. Registered as `rpl_decode` (Low risk, pure offline parser); registry 561→562.
- `skinny_decode` Skinny / SCCP — Cisco IP-phone signalling (VoIP recon) — ✅ v0.560.0 — **new tool** joining the VoIP / signalling decoders (`sip`, `rtp`, `rtcp`). Skinny (SCCP) is the Cisco-proprietary protocol between a Cisco IP phone and a CallManager / CUCM (TCP 2000); it is unencrypted in many deployments, so a captured exchange is a **VoIP-reconnaissance** goldmine — the message stream reveals the **call flow** (Register, OffHook, KeypadButton, CallInfo, Ring, Connect, OnHook) and, via the **KeypadButton** messages, the **actual digits the user dials** (the numbers being called). Decodes the self-delimiting little-endian Skinny stream: per message the **length**, the **message ID** and its **name** (from the full 132-entry SCCP message table), and — for KeypadButton — the **dialed digit** (0-9, `*`, `#`); multiple concatenated messages decode to a list. **No confidently-wrong output**: only the KeypadButton body is decoded — Skinny has ~130 message types with varied, version-specific bodies, so every other body is surfaced as raw hex with the message named. Native — a framed little-endian walk + an ID lookup, stdlib only, no new go.mod dep; the **132-entry message-name table is code-generated from scapy.contrib.skinny** (not hand-transcribed). **Verified field-for-field against scapy's Skinny layer** — 150 random messages (KeypadButton with all key codes + random message IDs / body lengths) decode identically, zero mismatches; the KeypadButton (digit + `#`), OffHook, and the OffHook+Keypad two-message stream unit-pinned. Fuzz-clean (~9s). `internal/skinny`. Registered as `skinny_decode` (Low risk, pure offline parser); registry 560→561.
- `pfcp_decode` PFCP — 5G N4 / 4G CUPS control protocol (session-manipulation recon) — ✅ v0.559.0 — **new tool** for the **5G-core-security** space, joining the cellular decoder set (`gtp_decode`, `gtpv2_decode`, `gsmtap_decode`, `diameter_decode`). PFCP (3GPP TS 29.244) is the control protocol of the **5G N4 interface (SMF↔UPF)** and the 4G CUPS Sxa/Sxb/Sxc interfaces, by which the control plane programs the user-plane function's packet-forwarding rules (PDRs/FARs/QERs/URRs) over UDP 8805. It is a recognised attack surface: a host that can reach N4 can forge PFCP **Session Modification / Deletion** messages to tear down or redirect a subscriber's bearer (a 5G-core DoS / on-path attack), so decoding a captured PFCP exchange surfaces the message type, the session endpoint identifier (**SEID**) and the rule IEs being programmed. Decodes the header (version, message-priority / SEID flags, the **message type** named from the TS 29.244 table — Heartbeat, Association Setup, Session Establishment / Modification / Deletion / Report, …, the length, the optional 64-bit **SEID**, the sequence number) and the body as a list of **Information Elements** (each TLV's 2-octet type — named from the full TS 29.244 IE table — length, value); the **Cause** IE is decoded. **No confidently-wrong output**: grouped IEs (those that nest further IEs) and non-Cause IE values are surfaced as raw hex — the IE value formats are numerous and release-specific, so only the structure + Cause are decoded. Native — byte-field extraction + a TLV walk, stdlib only, no new go.mod dep; the **message-type + IE name tables (161 IE types) are code-generated from scapy.contrib.pfcp** rather than hand-transcribed (avoiding the IE-numbering errors that hand-transcription invites). **Verified field-for-field against scapy's PFCP layer** — 109 random messages (12 message types, with/without SEID, varied seq, IE list) decode identically, zero mismatches; the Heartbeat (Recovery-Time-Stamp IE), the Session-Establishment header, and the Cause IE unit-pinned. Fuzz-clean (~8s). `internal/pfcp`. Registered as `pfcp_decode` (Low risk, pure offline parser); registry 559→560.
- `gtpv2_decode` GTPv2-C cellular control plane (IMSI-harvesting recon) — ✅ v0.558.0 — **new tool** closing the **GTP-C gap explicitly deferred by `gtp_decode`** (which decodes the GTP-U user plane, TS 29.281, and defers the control plane). GTPv2-C (TS 29.274) signals EPS bearer/session management across the LTE & 5G-NSA core (S11 MME↔SGW, S5/S8 SGW↔PGW, S10/S16; UDP 2123) and is a recognised telecom-security target — the roaming/core GTP plane has been abused for **IMSI harvesting**, subscriber tracking and the GTPdoor backdoor, and a captured Create-Session / Modify-Bearer exchange carries the subscriber's **IMSI, MSISDN and MEI in the clear** (chains into `imsi_decode`). Decodes the header (version, the piggyback / TEID / message-priority flags, the **message type** with a TS 29.274 name table — Create/Modify/Delete Session, Create/Update/Delete Bearer, Echo, Release Access Bearers, …, the length, the optional **TEID**, the sequence number) and the body as a list of **Information Elements** (each TLV's type — named from the TS 29.274 table — length, instance, value). The **TBCD-encoded subscriber identifiers (IMSI / MSISDN / MEI) are decoded to their digit strings**; every other IE value is surfaced as raw hex. **No confidently-wrong output**: only the TBCD identifiers + the Cause code are decoded — the IE value formats are many and version-specific, so the rest are left raw rather than guessed. Native — byte-field extraction + a TLV walk, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's GTPv2 layer** — 150 random messages (12 message types, with/without TEID, varied seq, IMSI) decode identically, zero mismatches; the canonical Create-Session-Request (IMSI 262011234567890), the no-TEID Echo Request, and the TBCD decode unit-pinned. Fuzz-clean (~9s). `internal/gtpv2`. Registered as `gtpv2_decode` (Low risk, pure offline parser); registry 558→559.
- `erspan_decode` ERSPAN (Encapsulated Remote SPAN) header — ✅ v0.557.0 — **new tool** for port-mirror / traffic-interception recon, pairing with `gre_decode` (the tunnel that carries it). ERSPAN is Cisco's protocol for shipping a port-mirror (SPAN) session across a routed network inside a **GRE tunnel**; seeing it in a capture is itself a finding — traffic on some switch is being **mirrored and exported** (lawful intercept, an IDS feed, or an attacker exfiltrating mirrored traffic) — and the encapsulated payload is the **original mirrored Ethernet frame in the clear**, so the capture both exposes the monitoring topology (session id, source VLAN) and lets the mirrored frame be **peeled out** for further analysis. Decodes the **Type II** (8-octet) and **Type III** (12-octet) headers: version, source VLAN, class of service, the truncated flag, the session id, and (II) the port index / (III) the 32-bit timestamp; accepts the ERSPAN header itself or a **GRE packet** (proto 0x88BE / 0x22EB) whose payload is ERSPAN (the GRE header is stripped); the mirrored frame is surfaced as hex. **No confidently-wrong output**: the Type III platform-specific sub-header flags beyond the timestamp are left in the raw remainder rather than decoded into possibly-wrong fields. Native — fixed bitfield bit-masking, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's ERSPAN_II / ERSPAN_III layers** — 150 random headers (varied vlan / cos / encap / session-id / index / timestamp) decode identically, zero mismatches; canonical Type II (incl. max fields), Type III, and the GRE-wrapped form unit-pinned. Fuzz-clean (~9s). `internal/erspan`. Registered as `erspan_decode` (Low risk, pure offline parser); registry 557→558.
- `carp_decode` Common Address Redundancy Protocol (FHRP-hijacking recon) — ✅ v0.556.0 — **new tool completing the FHRP-decoder triad** with `hsrp_decode` (Cisco HSRP) and `vrrp_decode` (IETF VRRP). CARP is the **open** FHRP used by OpenBSD / FreeBSD / **pfSense / OPNsense** for gateway/firewall HA, and is decoded for the same reason the others are: **FHRP hijacking is a classic on-path (MITM) attack** — a host that advertises for the virtual router with a better election metric becomes master and draws the LAN's default-gateway traffic through itself. Decodes the advertisement: version/type, the **VHID** (redundancy group), **advskew** + **advbase** (and the derived advertisement interval = advbase + advskew/256 s), auth length, demotion, checksum, the 64-bit counter, and the 20-octet SHA-1 HMAC; accepts the CARP PDU or an IPv4 packet (protocol 112, shared with VRRP) whose payload is CARP. **The advskew is the attack signal**: lower skew advertises more often and wins the master election, so a low/zero advskew is the CARP hijack/preemption (MITM) tell — the same FHRP attack class as HSRP/VRRP priority 255. **No confidently-wrong output**: the 20-octet **HMAC is surfaced as hex and not verified** — it is an SHA-1 HMAC keyed by the CARP passphrase, which is not on the wire. Native — fixed 36-octet byte-field extraction, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's CARP layer** — 200 random advertisements (varied vhid / advskew / advbase / authlen / demotion) decode identically, zero mismatches; canonical advertisement, the zero-advskew hijack signal, and the IPv4-wrapped form unit-pinned. Fuzz-clean (~9s). `internal/carp`. Registered as `carp_decode` (Low risk, pure offline parser); registry 556→557.
- `vtp_decode` Cisco VLAN Trunking Protocol (VLAN-database-attack recon) — ✅ v0.555.0 — **new tool** continuing the switch-L2 security set (`cdp`/`lldp`/`stp`/`lacp`/`vlan_decode`/`macsec_decode`/`dtp_decode`). VTP synchronises the VLAN database across a domain's switches, and is a notorious hazard: a VTP server (or a rogue host reaching a trunk) advertising a **higher configuration-revision number** overwrites every switch's VLAN database in the domain — a Subset advertisement with a high revision + a forged/empty VLAN list deletes or rewrites all VLANs domain-wide (a classic L2 DoS). Decoding a captured VTP frame surfaces the **domain name**, the message type, and the all-important **configuration revision** — the recon a switch-security audit needs. Decodes the header (version, **code** — Summary / Subset / Advertisement-Request / Join — and domain) and the bodies: **Summary** (followers, the config revision, updater identity IPv4, update timestamp, MD5) and **Subset** (sequence number, config revision, and the full **VLAN list** — each VLAN's id / name / status / type / MTU, switch-recon gold). Accepts the PDU or any frame carrying the LLC/SNAP VTP signature (OUI 0x00000C, PID 0x2003). **No confidently-wrong output**: the **MD5 digest is surfaced as hex and not verified** — it is computed over the VLAN database + the VTP password, which is not on the wire; Advertisement-Request / Join bodies are surfaced raw. Native — byte-field extraction + a record walk, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's VTP layer** — 120 random Summary/Subset PDUs (varied domain / revision / updater / VLAN-lists) decode identically, zero mismatches; canonical Summary/Subset/AdvRequest + the SNAP-wrapped form unit-pinned. Fuzz-clean (~9s). `internal/vtp`. Registered as `vtp_decode` (Low risk, pure offline parser); registry 555→556.
- `dtp_decode` Cisco Dynamic Trunking Protocol (VLAN-hopping recon) — ✅ v0.554.0 — **new tool** for switch-security pentesting, joining the L2 decoder set (`cdp`, `lldp`, `stp`, `lacp`, `vlan_decode`, `macsec_decode`). DTP is the L2 protocol a Cisco switch port uses to negotiate whether a link becomes an 802.1Q/ISL **trunk**, and is the basis of the classic **VLAN-hopping** attack: a port left in a negotiating mode (dynamic desirable / dynamic auto — the default on many switches) can be talked into forming a trunk by a rogue host, giving it reach into every VLAN. Decoding a captured DTP frame surfaces the **VTP domain name** (an info leak), the **neighbour MAC** and the raw trunk-negotiation status — the recon a switch-security audit needs. Decodes the **version** + the Domain (0x0001) / Status (0x0002) / DTP-Type (0x0003) / Neighbour (0x0004 → MAC) TLVs; accepts the PDU itself or any frame carrying the LLC/SNAP DTP signature (OUI 0x00000C, PID 0x2004). **No confidently-wrong output**: DTP is Cisco-proprietary and reverse-engineered, and its status-bit semantics (the exact dynamic-desirable / dynamic-auto / trunk encoding) are **not authoritatively standardised**, so the status and trunk-type octets are surfaced **raw** (hex + decimal) rather than decoded into a named mode that could be wrong — while the certain, high-value signal *is* stated: the mere presence of DTP means the port is participating in trunk negotiation (not 'nonegotiate'), the VLAN-hopping prerequisite. Native — a short TLV walk, stdlib only, no new go.mod dep. **Verified field-for-field against scapy's DTP layer** — 200 random PDUs (varied domain / status / type / neighbour, versions 1-2) decode identically, zero mismatches; canonical vectors + the SNAP-wrapped form unit-pinned. Fuzz-clean (~9s). `internal/dtp`. Registered as `dtp_decode` (Low risk, pure offline parser); registry 554→555.
- `macsec_decode` IEEE 802.1AE (MACsec) SecTAG — ✅ v0.553.0 — **new tool** bodying out a wired-L2 EtherType the existing decoders only *named*: the `vlan_decode` / VXLAN / Geneve EtherType tables already label `0x88E5` as "MACsec (802.1AE)", but nothing decoded its Security TAG. MACsec gives hop-by-hop L2 confidentiality + integrity on a wired LAN (keyed by 802.1X / MKA); the SecTAG is sent **in the clear**, so decoding it from a capture exposes the secure-channel identity, association number, replay-protection packet number and whether the user data is encrypted — the visibility a **network-access-control / MACsec-deployment audit** needs (fits the project's wired-L2 decoder set alongside LLDP/CDP/STP/LACP). Decodes the **TCI flags** (Version, End-Station, SCI-present, Single-Copy-Broadcast, **Encryption** E, Changed-text C) + the 2-bit **Association Number**, the **Short Length**, the 32-bit **Packet Number**, and (when SC is set) the 64-bit **Secure Channel Identifier** split into its 48-bit system MAC + 16-bit port id; accepts either the SecTAG onward or a **full Ethernet frame** (0x88E5 → outer dst/src MACs surfaced too), and splits the trailing data into Secure Data + the 16-octet ICV. **No confidently-wrong output**: the encrypted Secure Data is surfaced opaque and the ICV is *not* verified — both need the Secure Association Key (SAK), which MKA derives and never puts on the wire (deferred, like the lorawan / wmbus encrypted payloads); the 16-octet ICV split assumes the mandatory GCM-AES cipher and says so. Native — fixed public bit/byte layout (802.1AE-2006 §9.3), stdlib only, no new go.mod dep. **Verified field-for-field against scapy's MACsec layer** — 200 random SecTAGs (all TCI-flag combinations, SCI present/absent, SL, PN) decode identically, zero mismatches; canonical hand-built vectors unit-pinned. Fuzz-clean (~9s). `internal/macsec`. Registered as `macsec_decode` (Low risk, pure offline parser); registry 553→554.
- `bcbp_decode` IATA Bar Coded Boarding Pass (Resolution 792) — ✅ v0.552.0 — **new tool** for the travel-OSINT space (companion to `mrz_decode`): decodes the text encoded in the PDF417 / Aztec / QR barcode on a boarding pass. Boarding passes are routinely photographed, posted to social media and discarded intact, and the barcode leaks the **passenger name**, the **booking reference (PNR)** — which with the surname is often enough to open the full reservation on the airline's site — the **itinerary**, **seat**, **check-in sequence** and frequent-flyer data; decoding it is a recognised privacy-exposure / recon check. Decodes the mandatory fields (format code, leg count, passenger name, e-ticket indicator, and per leg the PNR, from/to airports, operating carrier, flight number, flight day-of-year, compartment class, seat, check-in sequence, passenger status); multi-leg passes supported. **No confidently-wrong output**: the mandatory fields are fixed-width (verified against the canonical IATA Resolution 792 example), and because every leg's conditional section is **length-prefixed** the leg boundaries are found from the declared hex sizes — a multi-leg pass cannot be mis-sliced. The variable **conditional (airline-use) section is surfaced raw** rather than decoded field-by-field (its layout is version-dependent and airline-extensible, so guessing it would risk a wrong decode); the flight date is a day-of-year (1-366) with **no year encoded**, so it is surfaced as the raw day count and not resolved to a calendar date. Native — fixed-width slicing driven by the self-describing length markers, stdlib only, no new go.mod dep. **Verified against the canonical IATA 792 example** (`M1DESMARAIS/LUC…EABC123 YULFRAAC 0834 226F001A0025 100` → DESMARAIS/LUC, PNR ABC123, YUL→FRA on AC 0834, day 226, seat 001A) plus multi-leg and conditional-section round-trips. Fuzz-clean (~9s). `internal/bcbp`. Registered as `bcbp_decode` (Low risk, pure offline parser); registry 552→553.
- `mrz_decode` ICAO 9303 Machine Readable Zone (passport / ID / visa) — ✅ v0.551.0 — **new tool** decoding the `<`-padded OCR-B lines at the bottom of a travel document. **Directly relevant to the NFC tooling**: the MRZ is the input to the **Basic Access Control (BAC)** key derivation that unlocks an e-passport / e-ID **NFC chip** — the document number, date of birth and date of expiry (each with its check digit) are hashed into the BAC seed — so an MRZ read off the printed page is what lets a reader talk to the chip (a natural companion to the `iso14443*` / `nfc_*` tooling). Also a core OSINT / border-forensics artefact. Auto-detects the format (**TD3** passport 2×44, **TD1** ID card 3×30, **TD2** 2×36) and breaks out document type, issuing country, surname + given names, document number, nationality, date of birth, sex, date of expiry and the optional/personal-number field. **No confidently-wrong output**: every check digit (document number, DOB, expiry, optional field, composite) is independently recomputed with the ICAO **7-3-1 weighted-modulo-10** algorithm and compared — per-field and overall `valid` are surfaced with a note naming any field that fails, and a valid check digit is *not* asserted to prove the document genuine (an MRZ can be mistyped or altered); the check digits are the verification anchor, as for the IMEI/ICCID Luhn and the VIN check digit. Dates are surfaced as raw YYMMDD plus an ISO rendering with the **century left unresolved** (the MRZ does not encode it, so 19xx/20xx is not guessed). Native — fixed-width slicing + the public check-digit algorithm, stdlib only, no new go.mod dep. **Verified field-for-field and check-digit-for-check-digit against the `mrz` reference library** for all three formats (e.g. the canonical ICAO TD3 `P<UTOERIKSSON<<ANNA<MARIA…` → ERIKSSON / ANNA MARIA, doc L898902C3, DOB 740812, all check digits valid; the ICAO worked example `D23145890`→7, `740812`→2 is unit-pinned). Fuzz-clean (~9s). `internal/mrz`. Registered as `mrz_decode` (Low risk, pure offline parser); registry 551→552.
- `iccid_decode` ICCID SIM-card-serial decode — ✅ v0.550.0 — **new tool completing the cellular identifier triad**: with `imei_decode` (the device) and `imsi_decode` (the subscriber, shipped v0.549), `iccid_decode` adds the **SIM card itself** — the Integrated Circuit Card Identifier (ITU-T E.118 / ISO-IEC 7812) printed on and stored in every SIM/USIM. The three together are the identities a SIM-swap / forensic seizure / IMSI-catcher engagement enumerates. Breaks the ICCID into the **MII** (the leading `89` = telecom; a non-89 MII is flagged), the **E.164 country code** (1-3 digits after the MII → issuing country / ISO region; E.164 codes are a prefix-free set so the code is parsed unambiguously by longest valid prefix, and a code shared by several territories — NANP `+1`, the `+44` British-Isles group — is flagged with its primary country), the **issuer + account** digits, and the trailing **Luhn check digit**. **The Luhn check digit is the verification anchor** (recomputed and compared — a mismatch is reported as `luhn_valid=false` with a note, never asserted as definitively fake), exactly like the IMEI Luhn and VIN check-digit anchors. No confidently-wrong output: the issuer identifier and individual account number are **not split** from each other (the issuer-identifier length varies by operator with no public fixed rule — surfaced combined, like `imei_decode` defers TAC→device and `imsi_decode` defers the operator name). Native — Luhn arithmetic + a prefix-table lookup, stdlib only, no new go.mod dep; the **E.164 calling-code → country table is code-generated** from Google libphonenumber + pycountry (215 calling codes, machine-readable authoritative source) rather than hand-transcribed (`internal/iccid/callingcode_table.go`). Verified against an independent Luhn reference (the canonical `79927398713` vector) and machine-minted valid ICCIDs whose country resolves correctly (e.g. `89440000000000000002` → +44 United Kingdom, Luhn-valid). Fuzz-clean (~9s). `internal/iccid`. Registered as `iccid_decode` (Low risk, pure offline parser); registry 550→551.
- `imsi_decode` IMSI cellular subscriber-identity decode — ✅ v0.549.0 — **new tool** closing the **IMSI gap explicitly deferred by `imei_decode`**: the International Mobile Subscriber Identity is the up-to-15-digit number on a SIM/USIM that uniquely names a cellular subscriber, disclosed in plaintext in a GSM/LTE Identity Response — the message an **IMSI-catcher** / cell-site simulator forces a handset to send (visible in a `gsmtap` capture). It is the subscriber-identity companion to `imei_decode` (the device identity); the two together are exactly what an IMSI-catcher harvests, and both are now decodable offline. Splits the IMSI into **MCC** (first 3 digits → country, via the authoritative ITU-T E.212 assignment table), **MNC** (the 2- or 3-digit operator code, length country-determined) and **MSIN** (subscriber digits). **No confidently-wrong output**: the MCC→country map is authoritative; the MNC/MSIN split was verified against the python-stdnum reference library across **all 2543 assigned MNCs of the 215 single-MNC-length countries — zero mismatches**; the 22 countries that assign *both* 2- and 3-digit MNCs (e.g. USA 310, India 405) use the predominant length and are **flagged** (`mnc_length_assumed`) rather than asserted; and the operator (MNC→carrier name) lookup is **deferred** (proprietary, churns as carriers rename/merge — like `imei_decode` defers the TAC→device GSMA lookup), surfacing the numeric MNC instead. IMSI carries no check digit, so validity is structural (length + a known MCC). Native — table lookup + a digit-field split, stdlib only, no new go.mod dep; the **MCC→country + per-MCC MNC-length table is code-generated** from python-stdnum's `imsi.dat` (a machine-readable authoritative source) rather than hand-transcribed (`internal/imsi/mcc_table.go`, 237 MCCs). Fuzz-clean (~9s). `internal/imsi`. Registered as `imsi_decode` (Low risk, pure offline parser); registry 549→550.
- `gps_nmea_decode` marine-instrument NMEA sentences — ✅ v0.548.0 — extended the NMEA decoder beyond the GPS/GNSS position sentences (GGA/RMC/GLL/VTG/GSA/GSV/GST/ZDA) with the **marine-instrument sentences** carried on a vessel's NMEA 0183 bus: **HDT** (true heading), **HDG** (magnetic heading + deviation + variation), **VHW** (water speed + true/magnetic heading), **DBT** (depth below transducer, feet/metres/fathoms), **DPT** (depth + transducer offset), **MTW** (water temperature), **MWV** (wind speed + angle, relative or true), **MWD** (wind direction + speed, true/magnetic, knots + m/s) and **ROT** (rate of turn). Maritime-cyber angle: NMEA 0183 is an **unauthenticated** bus, so a spoofed depth / heading / wind value injected onto it can mislead an autopilot or crew — the maritime counterpart to GPS spoofing — which makes decoding a captured marine NMEA stream a genuine integrity-analysis surface (companion to `ais_nmea_decode`, and the same "NMEA has no auth" problem that motivates the AIS work). Native — string-split + the existing XOR-checksum / float-pointer helpers, stdlib only, no new dep; absent fields decode to null (never a zero), and E/W deviation/variation are sign-resolved. **Anchored field-for-field to pynmea2 1.19.0** — the canonical example sentence for each type reproduces its decoded heading / depth / wind / temperature / rate-of-turn fields exactly (e.g. `$SDDBT,17.5,f,5.3,M,2.9,F` → 17.5 ft / 5.3 m / 2.9 fathoms; `$HCHDG,98.3,0.0,E,12.6,W` → heading 98.3°, variation −12.6° W). Fuzz-clean (~3.3M execs). `internal/nmea`. No new tool (extends `gps_nmea_decode`) — registry unchanged at 549.
- `rtcm_decode` RTCM 3.x differential-GNSS messages — ✅ v0.547.0 — **new tool** completing the **GNSS protocol triad**: after `gps_nmea_decode` (text output) and `ubx_decode` (u-blox binary input), `rtcm_decode` handles the *corrections* leg — the messages a GNSS **base station** broadcasts (over radio / NTRIP / serial) to feed real-time RTK corrections to rovers. **GNSS-integrity angle**: injecting forged RTCM corrections (a false 1005 reference-station position, or fabricated observables) is a known way to pull an RTK rover off its true fix — decoding a stream surfaces the reference-station id, the broadcast base ECEF position, and which message types / constellations a stream carries, flagging an anomalous or spoofed correction source. Decodes + validates: the **transport frame** (`0xD3` preamble + 10-bit length + the **CRC-24Q** poly 0x1864CFB over the whole frame; a stream of back-to-back frames decodes to a list, leading non-preamble bytes are skipped, and a frame whose CRC fails is not emitted — a bad CRC reads as a false sync); the **message type** (DF002) with names for the common types (1001-1004/1009-1012 legacy RTK observables, 1005/1006 station ARP, 1007/1008/1033 descriptors, 1019/1020/1042-1046 ephemerides, the 107x-112x MSM families per constellation, 1230 GLONASS biases) plus the **reference-station id** (DF003) for the types that carry it; and the **1005/1006 Stationary RTK Reference Station ARP** body — the base antenna reference point as ECEF X/Y/Z (metres, from the 38-bit 0.0001 m fields), the GPS/GLONASS/Galileo service indicators, and (1006) the antenna height. The CRC-24Q gate means no confidently-wrong output; observable/ephemeris/MSM bodies are CRC-validated + type/station-named with the per-satellite payload surfaced as raw hex rather than guessed (1005/1006 are bodied out because they carry the base position an integrity check cares about). Native — fixed public wire format + a bit reader + a CRC loop, stdlib only, no new go.mod dep. **Anchored to the pyrtcm reference library**: a real 1005 frame reproduces its decoded station-id (2003) + ECEF (1114104.60 / −4850729.71 / 3975521.46 m) exactly, and a 1006 frame (1005 + 1.5 m antenna height) round-trips through pyrtcm; the CRC-24Q check value (0xCDE703, distinct from the OpenPGP CRC-24's 0x21CF02) is unit-pinned. Fuzz-clean (~9s). `internal/rtcm`. Registered as `rtcm_decode` (Low risk, pure offline parser); registry 548→549.
- `ubx_decode` UBX NAV-SAT + NAV-STATUS body decode — ✅ v0.546.0 — extended the UBX decoder beyond NAV-PVT with the two messages that complete the GNSS picture: **NAV-SAT** (per-satellite signal info — for every tracked SV the constellation (GPS / SBAS / Galileo / BeiDou / QZSS / GLONASS / NavIC), satellite id, C/N0, elevation / azimuth, pseudorange residual, the signal-quality indicator, whether it is used in the solution and its 2-bit health) and **NAV-STATUS** (fix type + the gnssFixOK / differential-solution / week-number / time-of-week status flags + time-to-first-fix + receiver uptime). NAV-SAT is the highest-value addition for this project: anomalous per-satellite C/N0 or geometry is the primary tell of **GPS spoofing / jamming**, so it is the UBX counterpart to the NMEA GSV decode and directly serves the GPS-spoof-detection use case for a tracking / wardriving rig. Native — variable-length per-SV bit-field extraction (8 + 12·numSvs) + a 4-byte flags dword (qualityInd bits 0-2, svUsed bit 3, health bits 4-5, ephAvail bit 11), stdlib only, no new dep. **Anchored field-for-field to pyubx2 1.3.1** — a 2-satellite NAV-SAT frame (a used/healthy GPS SV with qualityInd 7 + a unused/unhealthy GLONASS SV) and a NAV-STATUS frame (3D fix, fixOK, week+TOW set, TTFF 3500 ms) minted by pyubx2 reproduce its decoded fields exactly. The previously-undecoded-message test now pins NAV-POSLLH (still frame-only). Fuzz-clean (~8s). `internal/ubx`. No new tool (extends `ubx_decode`) — registry unchanged at 548.
- `ubx_decode` u-blox UBX binary GNSS protocol — ✅ v0.545.0 — **new tool** closing the binary half of the GPS/GNSS decode space: u-blox receivers (the NEO-6/7/8/9 family — the dominant GNSS chips, in countless wardriving / drone / GPS-tracker rigs, including modules paired with the Flipper / Marauder) speak the compact **UBX binary protocol** as the alternative to NMEA text, and a UBX capture is undecodable by `gps_nmea_decode`. `ubx_decode` is its **binary counterpart**: decodes the UBX frame envelope (`0xB5 0x62` sync, class+id named for the common NAV/RXM/CFG/MON classes, little-endian length) and validates the **8-bit Fletcher checksum** (CK_A/CK_B over class+id+length+payload); a stream of back-to-back frames decodes to a list and leading non-sync bytes are skipped so a mid-stream capture still parses. **NAV-PVT** (the flagship position/velocity/time message) is fully bodied out: iTOW, UTC date/time + validity flags + time accuracy, fix type (no-fix / DR / 2D / 3D / GNSS+DR / time-only) + gnssFixOK, satellites used, lon/lat, height (ellipsoid + MSL), horizontal/vertical accuracy, the NED velocity vector, ground speed, heading of motion, and position DOP — with the raw integer units (mm, mm/s, deg×1e-7, deg×1e-5, 0.01 DOP) converted to metres / m·s⁻¹ / degrees. Other UBX messages are frame-decoded + class/id-named with the body surfaced as raw hex rather than guessed (NAV-PVT is the one message that carries a whole fix; others can land later against a vector). Native — fixed public wire format + a two-byte checksum loop, stdlib only, no new go.mod dep. **Anchored field-for-field to pyubx2 1.3.1**: a NAV-PVT frame minted by pyubx2 from known values (Munich, 3D fix, 9 SV) reproduces its decoded time/position/velocity/accuracy fields exactly, and the Fletcher checksum + bad-checksum-flagging + leading-garbage-skip + back-to-back-stream paths are unit-tested. Fuzz-clean (~9s). `internal/ubx`. Registered as `ubx_decode` (Low risk, pure offline parser); registry 547→548.
- `ais_nmea_decode` AIS Type 9 + Type 27 body decode — ✅ v0.544.0 — rounded out the AIS decoder's **dynamic position coverage** with the last two position-bearing message types that were name-labelled only: **Type 9 SAR Aircraft Position Report** (the broadcast from a search-and-rescue helicopter / fixed-wing — altitude in place of nav status, whole-knot speed, position, course, timestamp, DTE/assigned/RAIM) and **Type 27 Long-Range Position Report** (the compressed broadcast meant for satellite reception — nav status, coarse 1/10-minute position, whole-knot speed, whole-degree course, RAIM, and the GNSS position-latency flag). With these, every AIS *position* report an offline receiver sees (Types 1/2/3/9/18/19/27) now bodies out fully. Native — bit-field extraction reusing the package's signed-coordinate / nav-status helpers, stdlib only, no new dep. Note the resolution/units differences these two types carry vs the Class-A reports: Type 9 speed and Type 27 speed are *whole knots* (not deciknots), Type 27 course is *whole degrees*, and Type 27 position is the coarse 1/10-minute scale (`/600`, not `/600000`) with 18/17-bit signed lon/lat. **Anchored field-for-field to pyais 3.0.1** — Type 27 against a real satellite-AIS sentence (137.02°E/4.84°N, 57 kn, Not-under-command), Type 9 against a sentence minted by pyais's *encoder* from known values (350 m / 95 kn SAR aircraft) then cross-decoded, so the bit layout is checked against independent code. Fuzz-clean (~8s). `internal/ais`. No new tool (extends `ais_nmea_decode`) — registry unchanged at 547.
- `ais_nmea_decode` AIS Type 19 + Type 21 body decode — ✅ v0.543.0 — extended the existing AIS decoder (which bodied out Types 1/2/3/4/5/18/24) with two more high-traffic message types that were previously name-labelled only: **Type 19 Extended Class B Position Report** (Type-18's position fields *plus* vessel name, ship type and dimensions — a single 312-bit message that doubles as static data for small craft) and **Type 21 Aid-to-Navigation Report** (how buoys, lighthouses, RACONs, beacons and *virtual* AtoNs broadcast — full 32-entry aid-type table per ITU-R M.1371-5 Table 76, name with the 0-88-bit extension field, position, dimensions, EPFD, timestamp, and the off-position / virtual-aid / assigned flags). Maritime recon: Type 21 enumerates the navigation marks (and exposes spoofed/virtual AtoNs — a known AIS attack surface), Type 19 ties small-craft identity to position. Native — bit-field extraction reusing the package's existing 6-bit-ASCII / signed-coordinate / ship-type / EPFD helpers, stdlib only, no new dep. **Anchored field-for-field to pyais 3.0.1** decode() of real sentences (Type 19 `CAPT.J.RIMES` cargo vessel at 29.54°N/−88.81°E; Type 21 multi-fragment AtoN incl. the `EXTENDED NAME` overflow field) — and the decoder correctly rejects the bad NMEA checksums that the widely-circulated AtoN test vector ships with (pyais skips checksum validation; we don't, so the corrected `*79`/`*62` suffixes are used). Fuzz-clean (~3.3M execs). `internal/ais`. No new tool (extends `ais_nmea_decode`) — registry unchanged at 547.
- `meshtastic_decode` Meshtastic LoRa-mesh packet header — ✅ v0.542.0 — **new tool** for the LoRa-mesh decode space (and the fire that broke a two-hold lull by finding an *accessible-reference* target): **Meshtastic**, the dominant off-grid LoRa mesh-comms project. The 16-byte packet header is sent in the clear before the AES-encrypted payload, so decoding it from a Flipper Sub-GHz / SDR LoRa capture **enumerates the nodes in mesh range** (source + destination node IDs in the `!xxxxxxxx` form), reveals the **channel hash** (fingerprints which channel a packet belongs to — same name+PSK → same hash), and exposes the hop / want-ack / via-MQTT routing flags (hops-taken = hop_start − hop_limit) — passive mesh reconnaissance without touching the air. Also surfaces packet ID, next-hop and relay-node bytes, and the encrypted payload (length + hex). **Verified against the Meshtastic firmware wire layout** (RadioInterface.h PacketHeader — little-endian uint32 to/from/id, then flags/channel/next_hop/relay_node — and the PACKET_FLAGS_* masks, both fetched from the firmware source): broadcast (0xFFFFFFFF → `^all`) and directed (`!a1b2c3d4`) headers, the flag decode (hop_limit/want_ack/via_mqtt/hop_start) and hops-taken, channel hash and routing bytes all anchored. The AES-256-CTR (or PKI, for DMs) encrypted MeshPacket payload needs the channel PSK / node key (not on the wire) and is deferred — surfaced as ciphertext. Native — byte-field extraction + bit-masking, stdlib only, no new dep. Fuzz-clean. Registered as `meshtastic_decode` (Low risk, pure offline parser); registry 546→547.
- `lin_frame_decode` LIN automotive body-bus frame — ✅ v0.541.0 — **new tool** for the automotive decode space: **LIN** (Local Interconnect Network, ISO 17987 / LIN 2.x), the low-cost single-wire sub-bus that hangs off CAN for body electronics — door / mirror / seat modules, climate flaps, wiper and rain/light sensors, switch panels. LIN is a real automotive reverse-engineering / pentest surface (cheap to tap, usually unauthenticated) and is **distinct from the CAN family** the existing automotive decoders cover (canbus_fd_decode / isotp_decode / uds_decode / obd2_*). Decodes the **Protected Identifier** (6-bit frame ID + the two parity bits, with the parity recomputed — P0 = ID0^ID1^ID2^ID4, P1 = !(ID1^ID3^ID4^ID5) — and reported valid/invalid) + the frame class (signal / master-request or slave-response diagnostic [0x3C/0x3D] / user-defined / reserved); the data bytes; and the **checksum**, computing both the classic (data-only, LIN 1.x + diagnostics) and enhanced (PID + data, LIN 2.x) inverted carry-folded sums and reporting which the frame uses (or invalid). An optional leading 0x55 sync byte is tolerated. **Verified against the standard LIN PID constants** (frame ID 0x3C→PID 0x3C, 0x3D→0x7D, 0x00→0x80, 0x01→0xC1, 0x02→0x42 — anchoring the error-prone parity-bit positions to real LIN values) and the carry-folded checksum (the carry-fold edge case 0x80+0x80→0x01→0xFE distinguishing it from a plain sum); enhanced / classic-diagnostic / sync-byte / bad-parity / bad-checksum paths all covered. Signal (LDF/DBC) interpretation of the data bytes (needs the vehicle's LIN Description File) is deferred — raw data surfaced. Native — two parity XOR equations + one carry-folded checksum, stdlib only, no new dep. Fuzz-clean. Registered as `lin_frame_decode` (Low risk, pure offline parser); registry 545→546.
- `wmbus_decode` transport header + encryption detection — ✅ v0.540.0 — extended the v0.539 wM-Bus radio decoder with the **transport-protocol-layer (TPL) header** (EN 13757-3) that follows the CI field — and, the headline, **encryption detection**: short (CI 0x7A) / long (0x72) / none (0x78) header dispatch, the access number + status byte, and the **encryption mode** decoded from the configuration word (`mode = (cfg>>8)&0x1F`): plaintext (0) vs **AES-128-CBC** (mode 5 dynamic-IV / mode 7 no-IV), with the encrypted-block count and the bidirectional / accessibility / synchronous flags. The long-header (0x72) embedded device address (which can differ from the link-layer M/A) is surfaced too. Whether a meter's payload is readable or AES-encrypted (and which mode) is the first thing a smart-meter RF assessment needs — it determines whether the consumption is directly readable or requires key recovery. **Verified** against CRC-valid Format-A frames carrying complete short TPL headers: an unencrypted one (config 0x0000 → mode 0, plaintext) and an AES-CBC one (config 0x0520 → mode 5, 2 encrypted blocks); the config-word bit layout (`cfg2<<8|cfg1`, mode/flags/block-count) ported from the wmbusmeters reference. AES decryption itself needs the meter key (not in the frame) and is deferred — the mode is reported. Native — pure byte-field extraction, stdlib only, no new dep. Fuzz-clean. No new tool — extends `wmbus_decode` (`internal/wmbus`); registry unchanged.
- `wmbus_decode` Wireless M-Bus (868 MHz) radio frame — ✅ v0.539.0 — **new tool** filling the wM-Bus radio-framing gap that `internal/mbus` **explicitly deferred** ("wM-Bus radio framing (mode S/T/C, 3-of-6 / Manchester) is out of scope"): **Wireless M-Bus** (EN 13757-4), the 868 MHz over-the-air framing smart water / heat / gas / electricity meters broadcast and a Flipper Sub-GHz capture lifts off the air. The wired decoder handles the shared application layer; this adds the radio-specific frame — and reading these telegrams **enumerates the meters in radio range** (manufacturer, ID, type) and validates integrity, the passive-recon start of a smart-meter RF assessment. Decodes (Format A): the L (length) field + the block layout with **every block's CRC-16** (EN 13757 poly 0x3D65, init 0, final XOR 0xFFFF) recomputed and validated; **block 1** — the C (control) field (+ name SND-NR / SND-IR / …), the **manufacturer** (3-letter FLAG code from the M field), the **BCD meter ID**, version and **device/medium type** (+ name water / gas / heat / electricity / …); and the **de-chunked application payload** (CRCs stripped, concatenated) + its CI field, ready to feed into `mbus_decode`. **Verified** against a CRC-valid Format-A frame (Elster gas, ID 12345678, payload 7A2A0020) and, crucially, the **CRC-16 anchored to a real meter header block** `2e449315785634123303` → 0x3363 (the header decoding cleanly to a genuine Elster gas meter confirms it is real-world data, not a self-constructed vector); manufacturer FLAG (0x1593→ELS, 0x2C2D→KAM) and corrupt-CRC detection also covered. Format B (single trailing CRC) is detected by CRC mismatch and noted; the line coding (upstream) and DIF/VIF app decode (`mbus_decode`'s job) are deferred. Native — byte-field extraction + one standard CRC-16, stdlib only, no new dep; reimplemented from EN 13757-4, not wrapped. Fuzz-clean. Registered as `wmbus_decode` (Low risk, pure offline parser); registry 544→545.
- `dsmr_p1_decode` DSMR / P1 smart-meter telegram — ✅ v0.538.0 — **new tool** for the smart-metering / energy-IoT decode space: **DSMR / P1**, the ASCII data stream a Dutch / Belgian (and increasingly EU) smart electricity / gas meter pushes out of its P1 customer port every second. Reading the P1 stream is the energy-side data-exfiltration / monitoring surface — per-tariff energy import & export, instantaneous and per-phase power, per-phase voltage / current, gas volume, power-outage counters and the meter equipment IDs. Per DSMR 5.0 / IEC 62056-21. Decodes the meter identifier line, the **CRC-16/ARC** (over `/`…`!` inclusive — recomputed and validated, so a tampered/truncated telegram is caught; CRLF endings normalised first so an LF-only paste still validates), and every **OBIS object line** (`C-D:E.F.G(value*unit)`) with its documented meaning + parsed value/unit (energy per tariff, +P/-P power, per-phase V/A/W, sag/swell + power-failure counters, the gas M-Bus channel, IDs, timestamp, active tariff); unknown OBIS codes surfaced raw. **Verified byte-for-byte against the dsmr_parser reference telegram** (KFM5KAIFA-METER): the CRC-16/ARC reproduces the telegram's published `!6796` exactly, energy import tariff-1 = 001581.123 kWh, instantaneous power 02.027 kW, gas reading 00981.443 m3, L2 current 006 A — plus the LF-normalisation and tamper-detection paths. Native — string parse + one standard CRC-16 + a documented OBIS table, stdlib only, no new dep; the field interpretation reference parsers leave implicit is implemented directly. Fuzz-clean. Registered as `dsmr_p1_decode` (Low risk, pure offline parser); registry 543→544.
- `enocean_decode` EnOcean ESP3 / ERP1 building-automation radio — ✅ v0.537.0 — **new tool** for the sub-GHz building-automation decode space: **EnOcean** (the self-powered 868 / 902 / 315 MHz protocol behind batteryless light switches, occupancy / window-contact / temperature sensors and actuators — a real RF pentest surface, squarely in the Flipper sub-GHz wheelhouse). A USB gateway (USB300/USB400J) or an SDR emits ESP3 frames; decoding them gives the device identity (32-bit sender ID), telegram type, signal strength and an integrity check — the recon a building-automation RF pentest starts from. Decodes the **ESP3 framing** (0x55 sync + 16-bit data length + optional length + packet type [+ name] + a **header CRC-8 and a data CRC-8**, both recomputed and validated) and the **RADIO_ERP1 telegram**: the **RORG** type with its name (RPS rocker-switch 0xF6, 1BS contact 0xD5, 4BS sensor 0xA5, VLD 0xD2, MSC, UTE teach-in, secure 0x30/0x31/0x35), the payload, the 32-bit sender ID, the status byte + repeater count, and the optional data (sub-telegram count, destination ID, **RSSI in -dBm**, security level). **Verified against a CRC-8-valid RADIO_ERP1 reference frame** `55 00 07 07 01 7A F6 30 00 29 28 9C 30 03 FF FF FF FF 3D 00 DD` → RPS from sender 0029289C, header CRC-8 0x7A + data CRC-8 0xDD both valid, RSSI -61 dBm; the CRC-8 (poly 0x07) reproduced byte-for-byte. EEP (device-profile) payload decode is deferred — the meaning of the data bytes depends on the FUNC/TYPE profile the telegram does not carry, so decoding it would be a confidently-wrong guess; raw payload + RORG are surfaced. Native — byte-field extraction + a table-driven CRC-8, stdlib only, no new dep; reimplemented from the ESP3 spec / kipe enocean reference, not wrapped. Fuzz-clean. Registered as `enocean_decode` (Low risk, pure offline parser); registry 542→543.
- `eas_same_decode` EAS / SAME emergency-alert header — ✅ v0.536.0 — **new tool** for the RF-broadcast / SDR decode space: **EAS / SAME** (Specific Area Message Encoding), the AFSK digital header that prefixes every Emergency Alert System and NOAA Weather Radio alert (the `ZCZC…` burst at 520.83 baud on 162 MHz weather radio / broadcast EAS). A demodulator (multimon-ng, rtl_fm) recovers the raw header string; this **interprets its fields** — the part multimon-ng leaves to the operator. Per NWS NWSI 10-1712 / FCC 47 CFR 11.31, format `ZCZC-ORG-EEE-PSSCCC…+TTTT-JJJHHMM-LLLLLLLL-`: originator (PEP/CIV/WXR/EAS/EAN), the FCC/NWS event-code table (RWT/RMT tests, TOR/SVR/FFW/HUW/TSW weather warnings, watches, statements, and civil events like CAE child-abduction, CEM, EVI, NUW — with a third-letter fallback for unrecognised codes: …W warning / …A watch / …E emergency / …S statement), the FIPS location codes (part-of-county digit + state FIPS → state name + county FIPS), the valid/purge duration (TTTT hhmm) and issue time (JJJHHMM ordinal-day + UTC), and the 8-char callsign. **Verified against the documented NWS worked example** `ZCZC-EAS-RWT-012057-012081-012101-012103-012115+0030-2780415-WTSP/TV-` → EAS Participant / Required Weekly Test / 5 Florida counties (FIPS state 12) / valid 30 min / day 278 04:15 UTC / WTSP/TV — plus a WXR/TOR Texas tornado example and the unknown-suffix fallback. County-FIPS→name (~3200 entries) and ordinal-day→calendar-date (no year in the header) are deferred — state name + raw FIPS surfaced. Native — deterministic string parse + three documented lookup tables (originator / event / state-FIPS), stdlib only, no new dep. Fuzz-clean. Registered as `eas_same_decode` (Low risk, pure offline parser); registry 541→542.
- `osdp_packet_decode` reply payload decode — ✅ v0.535.0 — extended the v0.534 OSDP dissector with **typed reply-payload field decode** for the codes that carry a well-defined plaintext layout: **osdp_RAW** (card-read — reader number, format [raw/wiegand/ascii], bit count, and the **card data** itself, the actual badge bits to feed into `wiegand_decode` / `rfid_pacs_decode`), **osdp_PDID** (device fingerprint — vendor OUI, model, version, serial number, firmware version), **osdp_COM** (address + baud rate), **osdp_KEYPAD** (PIN key presses, rendered ASCII) and **osdp_LSTATR** (tamper + power). The wire layouts are ported verbatim from the libosdp reference reply builder (`src/osdp_pd.c`), which states each field's width and endianness explicitly (bwrite_u16_le / u24_le / u32_le / u24_be — e.g. PDID's firmware is u24 **big**-endian while its serial is u32 little-endian). Verified by decoding packets constructed field-by-field to that exact layout with a valid CRC (the framing CRC itself was independently anchored to libosdp's own phy vectors in v0.534): card-read bit-count 26 + 4 card bytes, PDID vendor 0x00A1B2 / serial 0x12345678 / firmware 1.2.3, COM 9600 baud, keypad "1234", LSTATR tamper/power. Command (CP→PD) parameter decode (LED/BUZ/OUT/TEXT) and secure-channel-encrypted payloads remain deferred (surfaced as hex). Native — pure byte-field extraction, stdlib only, no new dep. Fuzz-clean. No new tool — extends `osdp_packet_decode` (`internal/osdp`); registry unchanged.
- `osdp_packet_decode` OSDP reader-bus dissector — ✅ v0.534.0 — **new tool** for the physical access-control decode space: **OSDP** (Open Supervised Device Protocol, SIA / IEC 60839-11-5), the RS-485 serial protocol modern access-control readers speak to their controllers — the secure successor to Wiegand. An OSDP bus tap is a stream of these packets; dissecting them shows the poll/reply traffic, the card-read replies, the secure-channel handshake and any integrity failures — a staple of an access-control pentest. Decodes the full frame: optional 0xFF driver mark + 0x53 SOM + address byte (bit 7 = reply direction, low 7 = PD address, 0x7F broadcast) + 16-bit length + control byte (2-bit sequence number, CRC-vs-checksum mode, secure-channel-block flag); the security control block (length + type + meaning, SCS_11..SCS_18) when present; the command (CP→PD) / reply (PD→CP) code with its name (osdp_POLL / osdp_ID / osdp_RAW card-read / osdp_ACK / osdp_NAK / …); the NAK error code with its meaning; and **trailer integrity** — the CRC-16/AUG-CCITT (poly 0x1021, init 0x1D0F) or the 1-byte two's-complement checksum is recomputed and reported valid/invalid. **Verified byte-for-byte against the libosdp phy-layer reference test vectors** (`tests/unit-tests/test-cp-phy.c`): the POLL command `53 65 08 00 04 60 60 90` (CRC 0x9060), the ID command with data, the ACK reply in checksum mode (`53 e5 07 00 01 40 80`, checksum 0x80), and the NAK reply with error 0x01 → "message check error" (`53 e5 09 00 05 41 01 0e 8f`, CRC 0x8f0e) — plus the mark-byte and corrupt-trailer paths. Native — pure byte-field extraction + the standard CRC, stdlib only, no new dep; the libosdp reference is reimplemented, not wrapped. Fuzz-clean (2.6M execs). Registered as `osdp_packet_decode` (Low risk, pure offline parser); registry 540→541. Per-payload field decode (osdp_RAW card-data layout etc.) and secure-channel-encrypted payloads are deferred (data surfaced as hex, code name identifies it).
- `rds_decode` group 0A alternative frequencies — ✅ v0.533.0 — decoded the **alternative-frequency (AF) list** in group 0A — the VHF frequencies a station simulcasts on (EN 50067 Annex D), the other half of the most-common RDS group (PS being the first). The AF codes in block C are decoded to a flat, de-duplicated kHz list (code 1-204 → 87.5 MHz + code×0.1 MHz), the "N AFs follow" count filler (225-249) is surfaced as `af_count`, and the LF/MF marker (250) is noted without a confidently-wrong VHF mis-decode. **Verified byte-for-byte against the redsea Method A test vector**: YLE Yksi → {87900, 90900, 89800, 89200, 93200, 88500, 89500} kHz with count 7 (all seven distinct codes check against the linear formula). **Clock-time (group 4A) was investigated and deliberately re-deferred**: the redsea reference produces its clock_time via `mktime`/`localtime` with the UTC offset stuffed into `tm_sec`, making its output timezone/implementation dependent (the negative-offset vector keeps the UTC calendar date rather than rolling back) — there is no trustworthy deterministic oracle to verify a port against, so a wrong datetime would be worse than none. The AF Method B same-programme/regional-variant structuring is also deferred (a Method B station's flat list still contains its tuned frequency; clearly noted). Native — pure bit-field extraction + the standard AF formula, stdlib only, no new dep. Fuzz-clean. No new tool — extends `rds_decode` (`internal/rds`); registry unchanged.
- `rds_decode` group 1A + 10A — ✅ v0.532.0 — rounded out the v0.531 RDS decoder with two more of the common groups: **group 1A** (Programme Item Number — the day + time the current programme started — plus the linkage flag and the slow-labelling variants: extended country code, **language** name, TMC identification, SLC broadcaster bits, emergency-warning field) and **group 10A Programme Type Name** (the 8-character long-form programme-type label). **Verified byte-for-byte against the redsea reference test vectors** (`test/components-hex.cc`): programme item 31828 → day 15 / 17:20 with ECC 0xE1 + PI country nibble 6 (YLE Yksi); language "Finnish" (code 0x27); SLC broadcaster bits 0x5AB (RTL 102.5); and Programme Type Name "CRI.CN " — the last exercising the exact RDS terminator-rendering semantics (expected length runs to the first 0x0D, which itself renders as a blank, and unlike RadioText the PTYN is **not** right-trimmed, so the trailing space is preserved). The 128-entry RDS language table is ported verbatim. The ECC→country-name table (a 300-cell 2D lookup with only one reachable test cell) is deliberately deferred — the raw extended country code + PI country nibble are surfaced instead, rather than ship 299 unverified cells. Native — pure bit-field extraction + the language table, stdlib only, no new dep. Fuzz-clean (2.8M execs). No new tool — extends `rds_decode` (`internal/rds`); registry unchanged. Clock-time (4A), AF lists, ODA/TMC, EON and the country-name table remain deferred (group type still reported).
- `rds_decode` RDS / RBDS FM Radio Data System — ✅ v0.531.0 — **new tool** for the FM-broadcast / SDR decode space: **RDS / RBDS** (Radio Data System), the 57 kHz digital sub-carrier riding on every FM station — the data an rtl_fm → redsea pipeline (or any RDS tuner) pulls off the air, and a staple of broadcast-RF spectrum survey. Per IEC 62106 (RDS) / NRSC-4 (RBDS). Decodes from the post-demod block hex (four 16-bit blocks A/B/C/D per group): **PI** code + the **RBDS four-letter call sign** derived from it (K/W stations, e.g. PI 0x4569 → KUFX); group type (0A..15B) + **TP** flag + **PTY** programme type with both the European RDS and North American RBDS name tables; **group 0A/0B Programme Service name** (8 chars, assembled across segments) + TA / MS / DI flags; **group 2A/2B RadioText** (up to 64 chars, assembled + truncated at the 0x0D terminator); and the full **RDS G0 character set** (Annex E) so non-ASCII station text renders correctly. **Verified byte-for-byte against the redsea reference test vectors** (`test/components-hex.cc`): Programme Service "YLE X3M" (PI 0x6204, PTY "Varied", TA on) and "  KRKA"; RadioText "JACK 96.9", "Robbie Williams - Feel", and "Vega Kväll" (the last exercising the G0 charset's 0x91→ä); RBDS call sign KUFX. Native — pure bit-field extraction + the G0 charset + PTY tables + the RBDS PI→call-sign arithmetic, stdlib only, no DSP, no crypto, no new dep; the redsea decoder is reimplemented, not wrapped. Fuzz-clean (2.8M execs). Registered as `rds_decode` (Low risk, pure offline parser); registry 539→540. Deferred (group type still reported): clock-time (4A), alternative-frequency lists, ODA/TMC (3A/8A), EON (14), PIN (1A), PTYN (10A), legacy three-letter/linked call signs.
- `adsb_mode_s_decode` surveillance altitude + squawk — ✅ v0.530.0 — decoded the **AC13 pressure altitude** (DF0/4/16/20) and **ID13 Mode-A squawk** (DF5/21) from the 13-bit field at message bits 20-32 — the surveillance header fields the v0.529 Comm-B note had left deferred, and which apply across all six surveillance downlink formats (not just Comm-B). Altitude decodes all three ICAO encodings: the 25-ft Q-bit form, the **Gillham (Mode C) gray-coded 100-ft** form (gray2alt + gray2int ported from pyModeS), and the metric M-bit form. Squawk decodes the 4-digit octal Mode-A code and **flags the three reserved emergency codes — 7500 (unlawful interference / hijack), 7600 (radio failure), 7700 (general emergency)** — a genuinely useful passive security signal. **Verified byte-for-byte against the pyModeS reference** (py_common.altcode / idcode): squawks 1200/4721 + all three emergency codes; altitudes 5000ft & -1000ft (25-ft), 38000ft & 25000ft & -1200ft (Gillham Q=0); plus the real DF20 Comm-B frames cross-checked at 3300 / 30275 / 5450 ft. Vectors generated by brute-forcing the 13-bit field against the pyModeS oracle for each target value, then truncated to valid short/long frames and re-confirmed. Native — extends the existing Mode S decoder, stdlib only, no new dep. Fuzz-clean (3.7M execs, DF4/DF5 seeds added). No new tool — extends `adsb_mode_s_decode` (`internal/adsb`); registry unchanged. The Mode S decode is now comprehensive across the civil DF set: ADS-B ES (DF17/18) + Comm-B BDS (DF20/21) + surveillance altitude/identity (DF0/4/5/16/20/21).
- `adsb_mode_s_decode` Comm-B BDS register decode — ✅ v0.529.0 — decoded the **Mode S Comm-B (DF20/21) BDS registers** (ICAO Doc 9871), the data-link replies that carry aircraft *intent* and state ADS-B doesn't — and the one DF20/21 capability the package explicitly flagged as deferred ("a separate ~600 LoC effort"). A Comm-B reply's 56-bit MB field is **not self-describing**: the same bits could be any BDS register and the frame says nothing about which. So the register is **inferred via validity gates** (reproducing pyModeS `is10/is17/is20/is40/is50/is60`): a register is only reported when its status bits are internally consistent and its decoded values fall in the physically valid range, and when more than one gate passes the result is flagged ambiguous rather than guessed — the no-confidently-wrong ethos applied structurally. Decodes **BDS 2,0** (aircraft identification / callsign), **BDS 4,0** (selected vertical intention — MCP/FCU + FMS selected altitude + barometric pressure setting), **BDS 5,0** (track + turn report — roll, true track, ground speed, track-angle rate, true airspeed), **BDS 6,0** (heading + speed report — magnetic heading, IAS, Mach, barometric + inertial vertical rate), and **BDS 1,7** (GICB capability list); BDS 1,0 participates in inference. **Verified byte-for-byte against the official pyModeS reference test vectors** (`tests/test_commb.py`): KLM1017_ / IBK2873_ callsigns, 3008ft MCP+FMS / 1020mb (BDS40), roll 2.109375° / track 114.258° / gs 438 / tas 424 (BDS50), heading 42.715° / IAS 252 / Mach 0.42 / vertical rate -1920fpm (BDS60) — each frame validating under exactly its own gate. Caught a real cross-version subtlety: pyModeS v2.18 added an all-ones "no-data" sentinel guard to the track-angle-rate field that older releases lack — the newer/correct behaviour is implemented. Native — extends the existing Mode S decoder, stdlib only, no new dep. Fuzz-clean (3.2M execs). No new tool — extends `adsb_mode_s_decode` (`internal/adsb`); registry unchanged. The Gillham-coded DF20/21 altitude/squawk header fields and meteorological BDS 4,4 / 4,5 remain deferred.
- `quic_long_header_decode` Retry integrity verification — ✅ v0.528.0 — added **QUIC Retry Integrity Tag verification** (RFC 9001 §5.8), the last unverified element of QUIC long-header forensics (after v0.526 Initial decryption + v0.527 JA4-q). A server's Retry packet ends with a 16-byte AES-128-GCM tag computed, with empty plaintext, over the Retry Pseudo-Packet (`ODCID-length ‖ original-DCID ‖ Retry-without-tag`) under a fixed per-version key/nonce — no secret involved, so anyone with the packet bytes and the client's original DCID can recompute it. The decoder previously surfaced the tag as opaque hex; now, when the operator supplies `original_dcid` (the connection ID the client chose in its first Initial — echoed by the integrity check but absent from the Retry packet itself), the tag is recomputed and reported as `integrity_verified` true/false, flagging forged / corrupt / off-path Retries (Retry-injection detection). **Verified byte-for-byte against the RFC 9001 Appendix A.4 worked example** — the A.4 Retry (`ff000000010008f067a5502a4262b574…`) with original DCID `8394c8f03e515708` reproduces the published tag `04a265ba2eff4d829058fb3f0f2496ba`; tampering any byte of the tag or token, or supplying the wrong DCID, fails verification (constant-time compare). Only QUIC v1 is verified (v2/drafts use different Retry keys with no anchoring vector → reported, not guessed); fuzz-clean (3.5M execs). Native — Go-stdlib AES-128-GCM, no new dep. No new tool — extends `quic_long_header_decode` (`internal/quic`) with an optional `original_dcid` param; registry unchanged. **QUIC long-header forensics is now complete: decode (all 5 packet types) + Initial decryption + JA4/JA4S-q fingerprinting + Retry authentication.**
- `quic_long_header_decode` JA4 QUIC fingerprint — ✅ v0.527.0 — added the **JA4 QUIC fingerprint** (FoxIO, protocol prefix `q`) on top of last fire's Initial decryption, **completing the JA4+ family across the QUIC/HTTP3 transport**. Now that v0.526 decrypts the QUIC Initial → ClientHello/ServerHello, the JA4 is computed over the recovered handshake and surfaced as `decrypted.ja4` — JA4 for a client Initial, JA4S for a server Initial. Per the FoxIO spec the QUIC JA4 is **identical to the TLS-over-TCP JA4 bar the leading protocol character** (`q` not `t`), so `internal/tlsdecode`'s `computeJA4` / `computeJA4S` were parameterised on the protocol prefix (the existing TLS path still passes `t`, anchors unchanged) and a new exported `tlsdecode.QUICHandshakeJA4` parses the bare CRYPTO-stream handshake message (no TLS record envelope, RFC 9001 §4.1) and computes the `q` fingerprint, which `internal/quic` calls only for a complete contiguous handshake (a partial ClientHello would fingerprint wrong → surface nothing, no confidently-wrong output). **Verified end-to-end against FoxIO's own QUIC pcap**: extracted the QUIC Initial from `pcap/quic-tls-handshake.pcapng`, decrypted it with our v0.526 code, reassembled the Chrome ClientHello, and reproduced FoxIO's published snapshot JA4 `q13d0310h3_55b375c5d22e_cd85d2d88918` byte-for-byte (the full pcap→decrypt→ClientHello→JA4 chain, the real-world dog-food of the decryptor); the JA4S `q` variant is anchored to the same snapshot's `q130200_1301_234ea6891581` (the `q`-prefix of the already-verified TCP `t130200_1301_234ea6891581`). Native — extends the existing JA4 machinery + the v0.526 decryptor, stdlib + already-present `golang.org/x/crypto/hkdf`, no new dep. No new tool — extends `quic_long_header_decode` (`internal/quic` → `internal/tlsdecode`); registry unchanged. The JA4+ family now spans every layer **and** both transports: TCP (JA4T) / TLS (JA4/JA4S) / HTTP (JA4H) / cert (JA4X) over TCP, plus JA4/JA4S over QUIC.
- `quic_long_header_decode` Initial decryption — ✅ v0.526.0 — **decrypts the QUIC v1 Initial packet** — the one long-header packet whose protection keys are *public* (HKDF-derived from the clear-text Destination Connection ID + a fixed per-version salt, RFC 9001 §5.2, with no TLS-handshake secret), so it is fully decryptable offline from the packet bytes alone. The package previously surfaced the Initial payload as protected hex and explicitly deferred decryption; now `internal/quic` derives the client/server Initial keys (HKDF-Extract + HKDF-Expand-Label), removes header protection (AES-128-ECB over the 16-byte sample → mask → unprotect the first byte's low nibble + the 1-4 packet-number bytes), runs AES-128-GCM (nonce = iv XOR padded packet number, AAD = the unprotected header), then dissects the cleartext frames (PADDING run-collapsed / PING / ACK+ACK-ECN parsed-to-skip / CRYPTO / CONNECTION_CLOSE) and **reassembles the CRYPTO stream into the TLS ClientHello / ServerHello** — the bytes QUIC otherwise hides, ready to paste into `tls_handshake_decode` for the full JA4 / ALPN / SNI view. `Decode` tries the client role then the server role automatically. **Verified byte-for-byte against the RFC 9001 Appendix A.2 worked example**: the full 1200-byte protected client Initial (DCID `8394c8f03e515708`) reproduces every documented intermediate — key `1f369613…`, iv `fa044b2f…`, hp `9f50449e…`, sample `d1b1c98d…`, mask `437b9aec36`, unprotected first byte `c3`, AAD `c300…0002`, nonce `fa04…255e` — and decrypts to the documented CRYPTO frame (offset 0, length 241) carrying the ClientHello, followed by 917 padding-zero bytes. Wrong-role decryption fails GCM authentication (integrity holds). Only QUIC v1 is decrypted — v2 / drafts use a different salt + `quicv2 ` key labels with no published vector to anchor, so they're held rather than risk a wrong decode. Native — stdlib AES/GCM + `golang.org/x/crypto/hkdf` (already a dependency); no new runtime dep, no shell-out. Fuzz-clean (4.4M execs, no panic). No new tool — extends `quic_long_header_decode` (`internal/quic`); registry unchanged.
- `geneve_decode` inner-IP decode — ✅ v0.479.0 — **completes the tunnel-decap sweep** (GRE → GTP-U → VXLAN → Geneve). Geneve (RFC 8926; VMware NSX-T / OVN / Antrea overlays, UDP 6081) is the richest case: its protocol-type field gives both forms — 0x6558 (Transparent Ethernet Bridging) wraps an inner Ethernet frame (decoded like VXLAN: when its EtherType is IP, the L3 packet is decoded via `internal/ipdecode`), while 0x0800 / 0x86DD wrap an IP packet directly (decoded like GRE). Either way the encapsulated flow's addresses / protocol / ports surface in place. Non-IP forms (MPLS / NSH, or non-IP inner EtherTypes like 802.1Q) are left as hex; an IP-typed payload that fails to parse → `inner_decode_error` + raw hex. Verified with direct-IPv4/IPv6 + TEB-inner-IPv4 + non-IP + garbage cases; added a geneve fuzz harness, fuzz-clean through both inner→ipdecode paths. No new tool — extends `geneve_decode` (`internal/geneve` → `internal/ipdecode`). **Tunnel-decap family now complete.**
- `vxlan_decode` inner-IP decode — ✅ v0.478.0 — third of the tunnel-decap sweep, and the first **inner-Ethernet** case: a VXLAN packet (overlay networks / cloud / Kubernetes CNI / NSX, UDP 4789) encapsulates an Ethernet frame, and when its EtherType is IPv4 (0x0800) / IPv6 (0x86DD) the inner L3 packet is now **decoded in place** via `internal/ipdecode` (the decoder already parsed the inner Ethernet header — dst/src MAC + EtherType — so the L3 bytes start at the known payload offset), surfacing the overlaid flow's addresses / protocol / ports. Non-IP inner frames (ARP, 802.1Q, MPLS, …) are left for their own decoders; an IP-typed inner frame that fails to parse is reported with `inner_decode_error` + raw hex preserved. Verified with inner IPv4/UDP + IPv6/UDP + ARP-not-decoded + garbage cases; added a vxlan fuzz harness, fuzz-clean through the inner→ipdecode path. No new tool — extends `vxlan_decode` (`internal/vxlan` → `internal/ipdecode`). (Geneve is the last sibling; its inner is also Ethernet.)
- `gtp_decode` inner-IP decode — ✅ v0.477.0 — second of the tunnel-decap sweep: a GTP-U **G-PDU** (message type 0xFF — the 4G/5G mobile-core user-plane tunnel) carries the subscriber's IP packet directly, now **decoded in place** via `internal/ipdecode` (the version nibble is self-describing — GTP-U has no protocol-type field), surfacing the tunnelled flow's addresses / protocol / ports. Gated on message type 0xFF (Echo / Error-Indication / End-Marker etc. carry no IP payload and are left alone); a G-PDU payload that fails to parse as IP is reported with `inner_decode_error` + raw hex preserved. Verified with inner IPv4/UDP + IPv6/UDP vectors + non-G-PDU + garbage cases; fuzz-clean through the inner→ipdecode path (existing gtp harness). No new tool — extends `gtp_decode` (`internal/gtp` → `internal/ipdecode`). (Geneve / VXLAN remain — their inner is Ethernet, needing an L2 strip first.)
- `gre_decode` inner-IP decode — ✅ v0.476.0 — extended the chain-to-existing-decoder seam to **tunnel decapsulation**: a GRE packet (PPTP / VPN / MPLS-over-GRE — IP protocol 47) encapsulates an inner packet, and when the GRE protocol-type field marks it IPv4 (0x0800) or IPv6 (0x86DD) the inner packet is now **decoded in place** via `internal/ipdecode`, surfacing the tunnelled flow's source/dest addresses, protocol and ports directly in the GRE decode. Non-IP payload kinds (Transparent Ethernet 0x6558, ARP, MPLS, …) are left as hex (no confidently-wrong output), and an IP-typed payload that fails to parse is reported with `inner_decode_error` + raw hex preserved. Verified with inner IPv4/UDP + IPv6/UDP vectors (inner addresses + ports asserted) + non-IP-not-decoded + IP-typed-garbage cases; added a gre fuzz harness, fuzz-clean through the inner→ipdecode path. The first of the tunnel family (GTP-U / Geneve / VXLAN are the natural follow-on sweep). No new tool — extends `gre_decode` (`internal/gre` → `internal/ipdecode`).
- `icmp_packet_decode` embedded-packet decode — ✅ v0.475.0 — continued the chain-to-existing-decoder seam: an ICMP **error** message (Destination Unreachable / Time Exceeded / Parameter Problem / Redirect / Packet-Too-Big) quotes the original IP packet that triggered it (the IP header + ≥8 bytes of payload), and that quote was previously surfaced as raw hex "to feed into `ip_packet_decode`". It is now **decoded in place** via `internal/ipdecode`, so the offending flow's source/dest addresses, protocol and (for UDP, or a long-enough TCP quote) ports appear directly in the ICMP decode. Also added v6 Destination Unreachable (type 1) / Time Exceeded (type 3) — previously raw-hex — routing their embedded IPv6 packet through the same path. No confidently-wrong output: a quote that doesn't parse as IP is reported with `embedded_decode_error` and the raw hex preserved (truncated TCP quotes degrade gracefully — IP layer + ports as available). Verified with embedded IPv4/UDP + IPv6/UDP vectors (inner addresses + ports asserted) + a garbage-quote fallback; added an icmp fuzz harness, fuzz-clean through the embedded→ipdecode path. No new tool — extends `icmp_packet_decode` (`internal/icmp` → `internal/ipdecode`).
- `dtls_decode` Certificate body — ✅ v0.474.0 — sibling-swept the v0.473 TLS Certificate decode into **DTLS**: the DTLS Certificate handshake message (msg_type 11) body uses the same TLS 1.2 layout, so each DER cert is chained through `internal/x509decode` (subject / issuer / validity / SAN / fingerprints / CA). Gated on the DTLS handshake message being **unfragmented** (the existing dispatch already returns early on a fragment, so a partial cert is never mis-decoded); mismatched list-length → note; unparseable DER → raw + `decode_error`. Self-verified with `crypto/x509`-generated certs (single + chain) wrapped in a DTLS 1.2 Certificate record; added a DTLS fuzz harness (cert→x509 seed), fuzz-clean. No new tool — extends `dtls_decode` (`internal/dtls` → `internal/x509decode`).
- `ip_packet_decode` JA4T fingerprint — ✅ v0.525.0 — added the **JA4T TCP-SYN fingerprint** (FoxIO) to `ip_packet_decode` — a **passive OS / TCP-stack fingerprint**, the modern p0f analogue and a genuinely distinct capability class from the TLS/HTTP/cert JA4 members. From a SYN it emits `window_size_option-kinds_MSS_window-scale` (e.g. `65535_2-1-3-1-1-8-4-0-0_1460_6`): the TCP window size, the exact wire sequence of TCP option *kinds* (every NOP and EOL/padding byte included, in order), the MSS, and the window scale. **No hashes — pure structural extraction**, so there's no confidently-wrong hash risk; the only subtlety was that the display option-list stops at the first EOL while JA4T counts every trailing padding byte, so JA4T gets its own option walk. Computed only for SYN packets (the options are negotiated at setup). **Verified byte-for-byte against the FoxIO `macos_tcp_flags` snapshot** by extracting the actual SYN's IP packet from FoxIO's pcap and confirming the parse reproduces `65535_2-1-3-1-1-8-4-0-0_1460_6` exactly (both trailing zeros included). Native — extends the existing TCP-header+options decode, stdlib only, no new dep. No new tool — extends `ip_packet_decode` (`internal/ipdecode`); registry unchanged. The full JA4+ family is now JA4 / JA4S / JA4X / JA4H / JA4T — five members, all byte-for-byte verified, fingerprinting the TCP, TLS, HTTP, and certificate layers.
- `http_message_decode` JA4H fingerprint — ✅ v0.524.0 — added the **JA4H HTTP-client fingerprint** (FoxIO), **completing the entire JA4+ family**: JA4 (client TLS, v0.521) + JA4S (server TLS, v0.522) + JA4X (cert, v0.523) + JA4H (HTTP, this) now fingerprint every layer a connection touches. JA4H identifies the HTTP client stack (browser / library / bot / malware) and is computed for requests in `internal/httpmsg`: `method + version + cookie/referer flags + header-count + Accept-Language` `_` `hash12(header names)` `_` `hash12(cookie names)` `_` `hash12(cookie name=value)` — header names hashed in **wire case + order** (a client signal, not lower-cased), Cookie/Referer/pseudo-headers excluded from the count and the header hash, cookies sorted by name. JA4H had been **HELD three fires running** as "needs tshark" — this fire I cracked it by reading the full `ja4h.py` reference algorithm, then **extracting the actual HTTP requests as plaintext directly from FoxIO's own test pcaps** (`http-empty-useragent.pcap`, `http1-with-cookies.pcapng`) and anchoring byte-for-byte against the snapshot outputs: a 1-header GET → `ge10nn010000_b8bcd45ac095_…` (which revealed the rule I'd been getting wrong — header names are hashed in **original case**: `sha256("User-Agent")` = `b8bcd45ac095`, not `sha256("user-agent")`), and a curl GET with Referer + 2 cookies + `Accept-Language: da` → `ge11cr04da00_8ddaef5d77af_280f366eaa04_c2fb0fe53442` (all three sub-hashes reproduced exactly, anchoring the cookie-name-sort + name=value + language transform). Native — `crypto/sha256` + field assembly over the already-parsed request, stdlib only, no new dep. No new tool — extends `http_message_decode` (`internal/httpmsg`); registry unchanged. **The JA4+ family is now complete and every member is byte-for-byte verified.**
- `x509_certificate_decode` JA4X fingerprint — ✅ v0.523.0 — added the **JA4X X.509-certificate fingerprint** (FoxIO), completing the **verifiable JA4+ family** alongside JA4 (client, v0.521) and JA4S (server, v0.522): JA4 → JA4S → JA4X fingerprint the client stack, the server stack, and the **certificate-generation stack** respectively — and malware C2 / phishing infrastructure reuses cert-generation patterns across deployments, making JA4X a real threat-intel pivot. JA4X = `hash12(issuer RDN OIDs)_hash12(subject RDN OIDs)_hash12(extension OIDs)`, each the comma-joined hex of the OID DER content-octets **in certificate order** (not sorted). Computed in `internal/x509decode` from the already-parsed cert, so it surfaces in both `x509_certificate_decode` and every cert in a `tls_handshake_decode` chain. **Verified by the same reverse-engineering approach that cracked JA4S**: three recurring RDN hashes from the FoxIO browsers-x509 snapshot were reproduced byte-for-byte — `hash12("550406,55040a,550403")` = `a373a9f83c6b` (C,O,CN), C,O,OU,CN = `7d5dbb3783b4`, C,ST,L,O,CN = `2bab15409345` — plus an end-to-end anchor (a generated C,O,CN self-signed cert with SKI/AKI/BasicConstraints decodes to `a373a9f83c6b_a373a9f83c6b_795797892f9c`). Cross-confirmed against the FoxIO rust reference (`ja4x/src/lib.rs`: OIDs as `hex(attr_type().as_bytes())`, joined, not sorted). Native — `crypto/x509` ordered RDN/extension OIDs + `encoding/asn1` OID marshaling + SHA-256, stdlib only, no new dep. No new tool — extends `x509_certificate_decode` (`internal/x509decode`); registry unchanged. Only JA4H (HTTP) now remains deferred (no inline vectors; needs tshark).
- `tls_handshake_decode` JA4S fingerprint — ✅ v0.522.0 — added the **JA4S TLS *server* fingerprint** (FoxIO), the server-side counterpart to v0.521's JA4 client fingerprint — together they fingerprint **both ends** of a TLS session (client-stack + server-stack / C2 identification). Computed from the ServerHello (which `tls_handshake_decode` already decodes): JA4S = `protocol(t) + TLS-version + 2-digit server-extension-count + chosen-ALPN` `_` `negotiated-cipher(4-hex)` `_` `SHA256(server extensions in WIRE ORDER)[:12]`. **The crucial difference from JA4: the server extensions are NOT sorted** — the server's extension order is itself the fingerprint signal. I'd HELD on JA4S two fires earlier (FoxIO publishes no inline worked example), then **cracked the verification by reverse-engineering it against two real FoxIO snapshot outputs**: the same two TLS-1.3 ServerHello extensions (key_share 0x0033 + supported_versions 0x002b) appear in **opposite order** for two server stacks — `SHA256("0033,002b")[:12]` = `234ea6891581` (Google → `t130200_1301_234ea6891581`) and `SHA256("002b,0033")[:12]` = `a56c5b993250` (LastPass → `t130200_1302_a56c5b993250`) — which both reproduce exactly *and* prove the wire-order rule. Cross-confirmed against the FoxIO rust reference source (`tls.rs`: "preserving the original order of server's TLS extensions"). An end-to-end ServerHello decode caught a real bug — the server's `supported_versions` extension is a bare 2-byte version (not the client's length-prefixed list), so the JA4S version is now driven from the parsed negotiated version. Native — SHA-256 + field assembly over the already-parsed ServerHello, stdlib only, no new dep. No new tool — extends `tls_handshake_decode` (`internal/tlsdecode`); registry unchanged. JA4H / JA4X remain deferred (no inline vectors; need tshark).
- `tls_handshake_decode` JA4 fingerprint — ✅ v0.521.0 — added the **JA4 TLS client fingerprint** (FoxIO LLC), the modern successor to the JA3 the decoder already computed and increasingly the preferred client / C2 / malware fingerprint for threat-intel (JA3 is being retired in its favour). It was explicitly listed as deferred in the package doc; now implemented from the already-parsed ClientHello. JA4 = `protocol + TLS-version + SNI-flag + non-GREASE-cipher-count + non-GREASE-extension-count + first-ALPN` `_` `SHA256(sorted ciphers)[:12]` `_` `SHA256(sorted extensions, SNI+ALPN removed, + signature_algorithms in order)[:12]`, with GREASE ignored throughout. **Anchored byte-for-byte to the FoxIO worked example** `t13d1516h2_8daaf6152771_e5627efa2ab1` — both truncated SHA-256 hashes reproduce the spec's exact pre-hash strings, the GREASE-exclusion is unit-tested, and an end-to-end real-ClientHello decode yields a well-formed `t13d…h2_…_…`. Raw `signature_algorithms` and `supported_versions` code points are now captured (alongside their existing name forms) to feed JA4_c and the JA4 version. Only the TLS-over-TCP client JA4 is computed; JA4S/JA4H/JA4X and the QUIC/DTLS variants remain deferred. No new tool — extends `tls_handshake_decode` (`internal/tlsdecode`); registry unchanged.
- `tls_handshake_decode` Certificate body — ✅ v0.473.0 decoded the TLS **Certificate handshake message** (previously labelled-but-deferred): the TLS 1.2 (and earlier) certificate_list is walked and each DER certificate is chained through `internal/x509decode`, so a captured handshake now yields the server's full cert chain — subject / issuer / validity / SAN / fingerprints / CA flag — in one decode (the network-pentest analogue of the NDEF→WSC/BT-OOB carrier wirings). In TLS 1.3 the Certificate message is encrypted (post-key-exchange), so the plaintext form in a passive capture is the pre-1.3 layout decoded here; a body that doesn't match it is reported with a note, never mis-parsed; a cert whose DER fails to parse is surfaced raw with a `decode_error` rather than a confident decode. Self-verified by wrapping `crypto/x509`-generated certs (single + chain) in a Certificate record and asserting the chained x509 fields, plus malformed-list-length / garbage-DER cases; fuzz-clean (5M execs through the cert→x509 path, 0 panics). No new tool — extends `tls_handshake_decode` (`internal/tlsdecode` → `internal/x509decode`).
- `epc_decode` SGTIN-198 — ✅ v0.472.0 extended `epc_decode` with **SGTIN-198** (header 0x36), the 198-bit SGTIN whose serial is a variable-length **alphanumeric** 7-bit-ASCII string (up to 20 chars) rather than a numeric value — used where the GS1 serial isn't purely numeric (e.g. pharmaceutical serialisation), and previously reported "unsupported". `Decode` now dispatches by length: 12 bytes → the 96-bit schemes, 25/26 bytes → SGTIN-198 (the word-aligned form a RAIN reader emits; the 140-bit serial is twenty null-padded 7-bit chars). Company prefix / item reference / GTIN-14 reconstruction are shared with SGTIN-96; only the serial decode differs. Anchored byte-for-byte against the epc-encoding-utils oracle — `3614257BF7194E60C286C5933…` → `sgtin-198:0.0614141.812345.ABC123` and `…6C59B4B5C8…` → `…812345.XYZ-9`. The other 198-bit variants (GRAI-170 / GIAI-202 / SGLN-195) remain reported-unsupported. No new tool. Fuzz-clean. (`internal/epc`)
- `epc_decode` SGLN-96 / GRAI-96 / GIAI-96 — ✅ v0.471.0 **completed the 96-bit EPC family** in `epc_decode` by adding the three GS1-keyed schemes deferred at v0.470 (the prior fire HELD on these for lack of an authoritative source): **SGLN-96** (header 0x32, location — company prefix + location reference + extension), **GRAI-96** (0x33, returnable asset — company prefix + asset type + serial), and **GIAI-96** (0x34, individual asset — company prefix + asset reference). The remaining-source problem was solved with the **venv-oracle technique**: the `epc-encoding-utils` Python library was installed and used as an oracle to extract the authoritative partition tables and generate decoded vectors, anchoring each scheme byte-for-byte — `3214257BF460720000000190` → `sgln-96:0.0614141.12345.400`, `3314257BF40C0E400000162E` → `grai-96:0.0614141.12345.5678`, `3414257BF400000000003039` → `giai-96:0.0614141.12345` (+ a 12-digit-prefix GIAI partition-0 vector). Each decodes filter / partition / company prefix / reference fields + the EPC tag and pure-identity URIs, matching the oracle's formatting exactly (per-scheme zero-padding rules). `epc_decode` now covers all six 96-bit schemes (SGTIN/SSCC/SGLN/GRAI/GIAI/GID); 198-bit variants remain reported-unsupported. No new tool. Fuzz-clean. (`internal/epc`)
- `epc_decode` GID-96 — ✅ v0.470.0 extended `epc_decode` with **GID-96** (EPC header 0x35), the General Identifier — the EPC scheme used when no GS1 key (GTIN/SSCC/GLN/…) applies. Three fixed-width fields, no partition table / company prefix / check digit: general manager number (28 bits) + object class (24) + serial number (36), plus the EPC tag / pure-identity URIs. **Verified byte-for-byte against three worked vectors** — `3500079FF00000B00000000C` → `gid-96:31231.11.12`, `3500E82D900000F000000001` → `gid-96:951001.15.1`, `350000A2600019003ADE56FA` → `gid-96:2598.400.987649786`. Remaining GS1-keyed schemes (SGLN/GRAI/GIAI, which still need their own partition tables) stay identified-not-decoded. No new tool. Fuzz-clean. (`internal/epc`)
- `epc_decode` SSCC-96 — ✅ v0.469.0 extended `epc_decode` with **SSCC-96** (EPC header 0x31), the GS1 Serial Shipping Container Code — the logistics-unit (case/pallet/container) scheme, the second-most-common EPC after SGTIN. Decodes filter / partition / company prefix / serial reference and reconstructs the **SSCC-18** (extension digit + company prefix + serial-reference remainder + recomputed GS1 mod-10 check digit) plus the EPC tag / pure-identity URIs. The SSCC partition table (distinct from SGTIN's — no item reference, wider serial reference) and the SSCC-18 reconstruction are **verified byte-for-byte against the worked vector** `3134257BF4499602D2000000` → `urn:epc:tag:sscc-96:1.0614141.1234567890` (company prefix 0614141, serial reference 1234567890, SSCC-18 106141412345678908). The 24 trailing reserved bits are checked (non-zero noted). Remaining 96-bit schemes (SGLN/GRAI/GIAI/GID) still identified-not-decoded. No new tool. Fuzz-clean. (`internal/epc`)
- `epc_decode` (GS1 EPC UHF RAIN RFID — SGTIN-96) — ✅ shipped v0.468.0 — decodes the GS1 **Electronic Product Code** binary on UHF RAIN RFID (EPC Gen2 / ISO 18000-63) tags — retail item-level tagging and supply-chain logistics — a **whole RFID band the toolkit did not decode** (it covered HF ISO 14443/15693/NDEF and LF EM4100/HID/FDX-B/T5577, but not UHF EPC). Reading a UHF tag needs a RAIN reader (hardware, out of scope), but the captured 96-bit EPC decodes entirely offline. **SGTIN-96** (header 0x30, the dominant retail scheme) is fully decoded: filter, partition, company prefix, item reference, serial number, the canonical EPC tag URI (`urn:epc:tag:sgtin-96:…`) and pure-identity URI (`urn:epc:id:sgtin:…`), and the reconstructed **GTIN-14** (with a recomputed GS1 mod-10 check digit). The SGTIN-96 layout, the partition table, and the SGTIN→GTIN reconstruction are taken from the **GS1 EPC Tag Data Standard** and **verified byte-for-byte against its canonical worked example** — `3074257BF7194E4000001A85` → `urn:epc:tag:sgtin-96:3.0614141.812345.6789` (company prefix 0614141, item ref 812345, serial 6789, GTIN-14 80614141123458) — not recalled. No confidently-wrong output: the other 96-bit schemes (SSCC/SGLN/GRAI/GIAI/GID, 0x31-0x35) are identified by name but not field-decoded (raw + note) rather than guessed; unknown headers and 198-bit variants are reported unsupported. Verified against the canonical vector + a second GS1 check-digit cross-check + other-scheme/unknown/error cases; fuzz-clean (3.4M execs, 0 panics). Native — fixed bit-field extraction + GS1 mod-10, stdlib only (`internal/epc`).
- `nfc_t2t_tlv_decode` (NFC Type 2 Tag data-area TLV walker) — ✅ shipped v0.467.0 — walks the TLV blocks in a Type 2 Tag's **data area** (user memory from page 4 onward) and **decodes the NDEF message in place**, bridging a raw tag-memory dump to `ndef_decode`: a Type 2 Tag (NTAG / MIFARE Ultralight) stores its NDEF inside a TLV structure (NULL 0x00 / Lock Control 0x01 / Memory Control 0x02 / NDEF 0x03 / Proprietary 0xFD / Terminator 0xFE), and this locates and decodes it. The data-area complement to `nfc_t2t` (which decodes the page 0-3 header: UID/BCC/lock/CC) — previously an operator had to manually extract the NDEF bytes from a dump to feed `ndef_decode`. Each block is reported with type/offset/length/raw value; the NDEF TLV value is handed to the shared NDEF walker (URI/Text/Smart Poster/handover/MIME). The 1-or-3-byte length form (0xFF big-endian escape) is handled and the Terminator ends the walk. The TLV type values + length encoding are from the **NFC Forum Type 2 Tag Operation Specification** (cross-checked vs the Nordic nrfxlib T2T docs). No confidently-wrong output: a TLV whose declared length overruns the buffer is reported as truncated, and the Lock/Memory Control reserved-area descriptors are surfaced raw rather than guessed. Verified against a full Lock+NDEF+Terminator data area (NDEF Text record decoded in place) + NULL-skip / 3-byte-length / unknown-type / truncated / no-terminator / error cases; fuzz-clean (3.8M execs, 0 panics). Native — TLV framing over the shared NDEF walker (`internal/t2t`).
- `ntag_config_decode` (NTAG213/215/216 configuration-page decode) — ✅ shipped v0.466.0 — decodes the NTAG21x **configuration pages**, the registers controlling an NFC Type-2 tag's password protection, lock state, NFC counter, and UID/counter ASCII-mirror — answering an operator's first questions about a dumped tag: is it password-protected, **read+write or write-only (PROT)**, is the config **permanently locked (CFGLCK)**, how many failed password attempts are allowed (**AUTHLIM**), is the NFC read counter enabled. The protection-state complement to `ndef_decode` / `nfc_t2t` (which decode the tag's *data*, not its access registers). Decodes **AUTH0** (first protected page), the **ACCESS** byte (PROT/CFGLCK/NFC_CNT_EN/NFC_CNT_PWD_PROT/AUTHLIM), and the **MIRROR** byte (MIRROR_CONF UID/counter mirror, MIRROR_BYTE/PAGE, strong-modulation) from CFG0+CFG1 (8 bytes; 16 bytes adds PWD+PACK). The config pages live at different addresses per variant (29h/83h/E3h) but the layout is identical, so it decodes the page bytes regardless of origin. The page layout (Table 8), MIRROR byte (Table 9), ACCESS byte (Table 10), and every field value meaning (Table 11) were **extracted and verified byte-for-byte from the NXP NTAG213/215/216 data sheet rev 3.2 §8.5.7** (read directly from the PDF — not recalled). No confidently-wrong output: an undocumented MIRROR_CONF value (0b11) is surfaced as reserved; AUTH0=0xFF (protection disabled) is noted rather than asserted against an unknown memory size. Verified against factory-default / read+write-protected / CFGLCK+mirror / NFC-counter / PWD+PACK / error cases; fuzz-clean (4.8M execs, 0 panics). Native — fixed bit-field extraction, stdlib only (`internal/ntag`).
- `t5577_config_decode` (T5577 / ATA5577 configuration-register decode) — ✅ shipped v0.465.0 — decodes the **T5577 block-0 configuration register**, the 32-bit word that controls how an LF 125 kHz tag modulates/clocks its data. The T5577 is the ubiquitous rewritable LF blank used to clone EM4100 / HID Prox / Indala / AWID; decoding its config block surfaces the **modulation** (Direct / FSK1/2/1a/2a / ASK-Manchester / PSK1/2/3 / Biphase / Biphase-a), **data bit rate** (RF/8…RF/128), PSK clock, AOR, **max block**, password-enabled and sequence-terminator flags — the diagnostic complement to the project's T5577 clone/write tooling (`rfid_write` / `loader_t5577_multiwriter`, which set these configs but couldn't read them back). The bit layout, bit-rate table, and the modulation value→name table are taken from the **Proxmark3 reference** (`doc/T5577_Guide.md` + `client cmdlft55xx.c GetModulationStr`) and verified byte-for-byte against **two real config words**: `0x00148040` (EM4100 → RF/64, Manchester, 2 blocks) and `0x00107060` (HID Prox → RF/50, FSK2a, 3 blocks). (Verification caught a recalled error — PSK1 is modulation value 1, not 0x0A.) No confidently-wrong output: a modulation value outside the documented set is surfaced as a numeric code, and extended mode (master key 0x6/0x9, which reinterprets the reserved bits) is flagged rather than mis-decoded. Verified against both vectors + constructed PSK/flags / extended-mode / error cases; fuzz-clean (4.2M execs, 0 panics). Native — fixed bit-field extraction, stdlib only (`internal/t55xx`).
- `fdxb_decode` (ISO 11784/11785 FDX-B animal/pet-microchip LF decode) — ✅ shipped v0.464.0 — decodes the **FDX-B data block**, the 134.2 kHz LF transponder format in pet/animal microchips (and many asset/biothermo tags) — the LF complement to the project's EM4100 / HID-Prox / PACS decoders (it read/emulated FDX-B on hardware but had no offline data-block decode). Recovers the **country code** (10-bit LSB-first), the **38-bit national identification number** (LSB-first), the data-block-status and animal-application flags, and validates the **CRC-16** (CCITT poly 0x1021, init 0x0000, refin=false/refout=true, over the 8-byte ID block — the integrity gate). On-air framing (11-bit preamble + control-bit stuffing) is the demodulator's job and out of scope, so the decode is deterministic. The field layout, LSB-first bit order, and CRC-16 parameters are taken from the **Proxmark3 reference** (`common/crc16.c crc16_fdxb` + `client cmdlffdxb.c`) and **verified byte-for-byte against two real decoded tags** — country 528 (Netherlands) / national 140000795552 (raw ID `05 D9 4D 19 04 21 00 01`) and country 999 / national 1500030037 (CRC `0x8A1C`) — not recalled. No confidently-wrong output: a CRC-failed frame is reported as such (never asserted as a real tag), a 900-999 manufacturer/test country code is noted rather than mapped, and the reserved/extended bits are surfaced raw. Verified against both real vectors + CRC-mismatch / extended-block / error cases; fuzz-clean (5.6M execs, 0 panics). Native — fixed bit/byte extraction + CRC-16, stdlib only (`internal/fdxb`).
- NDEF **Connection Handover** records (`Hs` / `Hr` / `ac` / `cr` / `err`) — ✅ shipped v0.463.0 — completes the tap-to-pair / tap-to-connect decode tree in `ndef_decode` (no new tool — recursive NDEF, like Smart Poster). A real handover tag's top-level record is a **Handover Select** (`Hs`) or **Handover Request** (`Hr`): a 1-byte version (major high nibble / minor low) + a nested NDEF message that the decoder now **recurses**, so the **Alternative Carrier** (`ac`) records and the **Wi-Fi WSC / Bluetooth OOB carrier-config records they reference by ID** (shipped v0.461/v0.462) are decoded in place — previously the `Hs` payload was opaque hex and the carriers were never reached. The `ac` record decodes the Carrier Power State (0=inactive / 1=active / 2=activating / 3=unknown), the carrier-data reference (the carrier record's ID), and the auxiliary-data references; `cr` (Collision Resolution) the 2-byte random number; `err` the error-reason byte. Values verified verbatim against the **nfcpy/ndeflib reference** (CPS tuple, `ac` byte layout, version-byte nibbles, `Hs`=version+message) — not recalled. **Also hardened a latent stability bug**: nested-message decode (the pre-existing Smart Poster path and these new handover paths) had **no recursion-depth cap** — a maliciously self-nesting record could exhaust the stack; added a depth=16 guard that leaves the payload raw with a warning past the limit. No confidently-wrong output: reserved error reasons surfaced as `reserved(0xNN)`, truncated `ac` references reported. Verified against a full hand-constructed `Hs → ac → referenced BT-OOB carrier` tree + aux-refs / cr / err / depth-guard cases; fuzz-clean (1.7M execs, 0 panics). Native — recursive descent, stdlib only (`internal/ndef`).
- `bluetooth_oob_decode` (Bluetooth OOB pairing / NFC handover record decode) — ✅ shipped v0.462.0 — decodes the **tap-to-pair** payload: the Bluetooth Secure Simple Pairing OOB data block carried by an NFC handover tag, in both MIME forms — `application/vnd.bluetooth.ep.oob` (BR/EDR "Easy Pairing": a 2-byte little-endian OOB Data Length + 6-byte little-endian BD_ADDR + EIR) and `application/vnd.bluetooth.le.oob` (Bluetooth LE: a bare AD-structure sequence). Recovers the peer's Bluetooth address, device class, LE role, local name, and the SSP OOB key material a tag offers. Both variants run through the **shared `internal/ble` EIR/AD walker**, which this work extended with value decoders for the OOB-critical AD types it previously only named: **LE Role** (0x1C → Peripheral / Central / preferred, per Bluetooth CSS Part A §1.17), **LE Bluetooth Device Address** (0x1B → 6 LE address bytes + public/random type flag), and **Class of Device** (0x0D → 24-bit value + named Major Device Class) — so `ble_gap_decode` benefits too. The BR/EDR framing and the LE value formats are taken **verbatim from the Bluetooth Core Specification Supplement and the NFC Forum "Bluetooth Secure Simple Pairing Using NFC" application document (as implemented in the ndeflib reference)** — verified, not recalled. No confidently-wrong output: the declared OOB length is checked-not-asserted (mismatch noted), reserved LE-role values are surfaced as `reserved(0xNN)`, and the opaque OOB key material (Hash C / Randomizer R / LE SC confirmation+random / SM TK) is left as raw hex. The same record is decoded automatically when it appears as an `ndef_decode` MIME payload (the tap-to-pair sibling of the v0.461 tap-to-connect WSC wiring). Verified against hand-constructed BR/EDR + LE vectors (BD_ADDR endianness, CoD major class, LE role/address, length-mismatch, reserved role) + an NDEF-wrapped integration test; fuzz-clean (btoob 4.1M + new GAP harness 3.3M execs, 0 panics). Native — thin BR/EDR framing over the shared EIR walker, stdlib only (`internal/btoob` + `internal/ble`).
- `wifi_wsc_decode` (Wi-Fi Simple Config / WPS credential decode) — ✅ shipped v0.461.0 — decodes the `application/vnd.wfa.wsc` credential blob: the payload behind a **tap-to-connect Wi-Fi NFC tag** (an NDEF MIME record) and the Credential carried in WPS Registrar messages (M7/M8). Recovers the provisioned network's **SSID**, authentication type (Open / WPA-PSK / WPA2-PSK / WPA-Enterprise / WPA2-Enterprise, decoded as bit flags), encryption type (None / WEP / TKIP / AES), MAC, and — the operative field for an authorized engagement — the **network key (the PSK)**. Walks the WSC TLV grammar (big-endian type:2 / length:2 / value) and recurses into the Credential attribute (0x100E). The attribute IDs and auth/encr flag values are taken **verbatim from the Wi-Fi Simple Config spec as published in hostap's `src/wps/wps_defs.h`** (verified against that source, not recalled). The 0x1018 Encrypted-Settings attribute (AES under an unavailable session key) is reported present-but-encrypted rather than guessed; WSC v2 vendor-extension subelements (0x1049) are surfaced raw; unknown auth/encr flag bits are surfaced, never dropped — no confidently-wrong output. The same blob is decoded automatically when it appears as an `ndef_decode` MIME payload (closing the long-standing "MIME pass-through surfaces type + size only" gap in `internal/ndef`). Verified against hand-constructed WSC vectors (SSID/auth/encr/PSK/MAC TLVs, mixed-mode 0x0022 auth, unknown-bit handling, truncated TLV) + an NDEF-wrapped integration test; fuzz-clean (6.6M execs, 0 panics). Native — fixed TLV parsing, stdlib only. Offline, no hardware (`internal/wsc`).
- `nfc_iso14443b_decode` (ISO 14443 Type B ATQB identity) — ✅ shipped v0.460.0 — decodes the **Type B answer to REQB/WUPB**, the proximity air interface behind most **ePassports (ICAO 9303)**, several national eID cards, and some transit/payment cards — the Type B complement to the project's long-standing `nfc_iso14443a_identify` (it had Type A + ISO 15693 but no Type B). The 12-byte ATQB → 4-byte PUPI, 4-byte card-specific application data (surfaced raw — its meaning is issuer-defined), and the 3-byte protocol-info group decoded into documented ISO 14443-3/-4 fields: max frame size (FSCI → the same 16…256-byte table as Type A), ISO 14443-4 support, frame waiting time (FWI → `FWT = 0.302064·2^FWI` ms), the bit-rate capability byte (PICC↔PCD 212/424/848 kbit/s), and NAD/CID support. The **0x50 leading byte is the hard anchor** — a non-0x50 ATQB is flagged, not mis-decoded; a trailing CRC_B is tolerated. Verified against spec-anchored constructed vectors (FSCI/FWI/FO bit fields, bit-rate decode) + non-standard/short/non-hex cases; fuzz-clean (7.1M execs, 0 panics). Offline, no hardware (`internal/iso14443b`).
- `nfc_iso15693_decode` (ISO 15693 vicinity-card identity) — ✅ shipped v0.455 — decodes the **second major HF NFC standard** alongside ISO 14443 (the project had `iso14443a` but no ISO 15693): the 8-byte UID (0xE0 prefix **validated** as the hard anchor — a non-0xE0 UID is flagged, not mis-decoded; IC manufacturer from the ISO 7816-6 table **shared with `internal/iso14443a`**; 6-byte serial) plus the optional AFI application-family byte (documented ISO 15693-3 table). Seen on library / access / medical / industrial tags (NXP ICODE, TI Tag-it, ST LRI). DSFID and the flag-gated Get-System-Information response are deferred (manufacturer-specific / unverified-format). Verified against NXP/ST UID vectors + non-standard/unknown-manufacturer cases; fuzz-clean. Offline, no hardware (`internal/iso15693`).
- `jwk_to_pem` (JWK / JWKS → PEM) + `jwt_verify` JWK input — ✅ shipped v0.454 — converts a JSON Web Key or JWK Set (the form an issuer publishes at `/.well-known/jwks.json`, RFC 7517) into PKIX PEM public keys (RSA / ECDSA P-256/384/521 / Ed25519), closing the **capture-JWKS → verify-token** workflow that v0.452's all-algorithm `jwt_verify` left open (it accepted only PEM). `jwt_verify`'s `public_key` now also accepts a JWK/JWKS directly, selecting the key by the token's `kid`. Verified by round-tripping RSA/EC/Ed25519 keys against the Go stdlib (the authoritative reference) + an end-to-end RS256-token-via-JWKS test; `e="AQAB"` pinned to 65537; fuzz-tested (no panics). Native — base64url + crypto/x509 PKIX marshalling, stdlib only (`internal/jwtsig`).
- `magstripe_decode` (ISO 7813 Track 1/2 swipe parser) — ✅ shipped v0.453 — parses a **raw magnetic-stripe swipe** (the ASCII track data a card reader / MSR / skimmer emits) into structured fields: PAN (+ masked), cardholder name (Track 1 only — surname/given), expiry (MM/YY), the 3-digit service code with its decoded ISO 7813 meaning (chip-preferred, PIN-required, …), and discretionary data. The **offline half of magspoof (rank #3)** and the swipe-side complement to `nfc_emv_track2_decode` (which handles the EMV *chip's* tag-57 BCD Track-2-Equivalent — a different encoding, no cardholder name). The PAN's Luhn check digit is the verification anchor (a misframed swipe is flagged, never asserted as a real card); the bit-level LRC is surfaced raw but not validated (a wrong verdict is worse than none). Reuses `internal/emv`'s Luhn + ISO 7813 service-code table. Verified against Luhn-valid test PANs (Visa 4111…, Mastercard 5555…). Offline, no hardware (`internal/emv`).
- `argon2` (Argon2id/Argon2i compute + verify) — ✅ shipped v0.451 — the **OWASP-recommended modern password hash** (the Password Hashing Competition winner, bcrypt's successor), completing the modern-hash compute/verify coverage. `hash_identify` recognises `$argon2id/$argon2i/$argon2d` but nothing could generate/verify one. Compute (password + optional variant/memory/time/parallelism/salt; random salt via crypto/rand) or verify a candidate against a captured PHC hash (constant-time; params read from the hash). `hash_crack_dictionary` also gained the `argon2` algorithm (memory-hard → workers capped). **`Wrap-vs-native: wrap`** (the second documented exception, after bcrypt) — Argon2 is a memory-hard BLAKE2b KDF; the audited `golang.org/x/crypto/argon2` (the already-required x/crypto module) is used, with our own PHC string parse/encode around it, plus a 2 GiB memory-cost cap that rejects a hostile `m=` before it can OOM the host. Verified against real argon2id/argon2i hashes from the reference **argon2-cffi** library (an independent oracle, confirmed to match x/crypto byte-for-byte) plus a compute→verify round-trip. argon2d (data-dependent) is out of scope. Offline compute, no network/device (`internal/tools/argon2.go`).
- `hash_crack_dictionary` crypt(3) + MySQL coverage — ✅ v0.450 extended the dictionary cracker with the **`crypt` algorithm** (auto-detects `$1$` md5crypt / `$apr1$` / `$5$` sha256crypt / `$6$` sha512crypt from the hash prefix via the oracle-verified `unixcrypt.Verify`) and the **`mysql`** algorithm (mysql_native_password, SHA1∘SHA1). The cracker could already do md5/sha1/sha256/sha512/ntlm/bcrypt; this closes the **detect-but-can't-crack** gap for modern Linux `/etc/shadow` ($6$ is the default) and MySQL/MariaDB dumps — the hashes `hash_identify` already flags. Each crack path reuses primitives gated against independent oracles (OpenSSL `passwd`, the published MySQL vector). No new tool (`hash_crack_dictionary` enhanced); `internal/tools/security.go` + `internal/unixcrypt`.
- `bcrypt` ($2a/$2b/$2y compute + verify) — ✅ shipped v0.449 — the **dominant modern web-application password hash** (hashcat 3200), completing the credential cluster's compute/verify coverage: `hash_identify` recognises bcrypt and `hash_crack` runs a dictionary attack, but neither generates a hash nor does a single-shot verify. Compute (password + optional cost 4-31, default 10) or verify a candidate against a captured `$2…$` hash (constant-time, the bcrypt design); bcrypt's 72-byte limit is surfaced as an error. **The one deliberate `Wrap-vs-native: wrap` in the credential cluster** — bcrypt is Blowfish-based and a faithful native port would require vendoring the un-hand-verifiable 1024-entry S-box table (the hex digits of pi), so the audited `golang.org/x/crypto/bcrypt` (already a project dependency, used by `hash_crack`) is used instead; no new dependency, native genuinely infeasible. Verified against the canonical OpenBSD/jBCrypt published vectors plus a generate→verify round-trip. Offline compute, no network/device (`internal/tools/bcrypt.go`).
- `sha_crypt` (Unix $6$ sha512crypt / $5$ sha256crypt compute + verify) — ✅ shipped v0.448 — the **modern Linux `/etc/shadow`** credential primitive (`$6$` sha512crypt is the current default), the highest-frequency offline-crack target. `hash_identify` recognises these (hashcat 1800 sha512crypt / 7400 sha256crypt) and `hash_crack` attacks them; this computes/verifies them, alongside `md5crypt`'s older `$1$/$apr1$` family. Compute (password + optional salt/scheme/rounds; random salt via crypto/rand, default 5000 rounds, `rounds=N$` emitted when set) or verify a candidate against any crypt hash ($6$/$5$/$1$/$apr1$, constant-time). Ulrich Drepper's SHA-crypt (the dual-context digest mixing + the two final base64 permutation tables) is implemented natively over `crypto/sha256`+`crypto/sha512`. Strongest verification class — gated against the **OpenSSL `passwd -6` / `passwd -5` oracle** across password/salt lengths and the `rounds=` form, matching byte-for-byte. Offline compute, no network/device (`internal/unixcrypt`).
- `md5crypt` (Unix $1$ / Apache $apr1$ compute + verify) — ✅ shipped v0.447 — the compute/verify side of the crypt(3) MD5 family: `hash_identify` recognises these (hashcat 500 md5crypt / 1600 apr1) and `hash_crack` attacks them, but neither could produce or check one. The same `$1$` algorithm is **Cisco IOS "type 5"** (complementing `cisco_type7_decode` and the type-8/9 detection); `$apr1$` is the **Apache htpasswd** format. Compute (password + optional salt/scheme; random salt via crypto/rand if omitted) or verify a candidate against a captured hash (constant-time). md5crypt (Poul-Henning Kamp's MD5 scramble over 1000 rounds + the custom crypt base64) is implemented natively over `crypto/md5`. Strongest verification class — gated against the **OpenSSL `passwd -1` / `passwd -apr1` oracle** across several password and salt lengths (empty password, 16-char password, 2/4/8-char salts), matching byte-for-byte. The SHA-crypt family ($5$/$6$) shipped as the follow-up `sha_crypt` (v0.448). Offline compute, no network/device (`internal/unixcrypt`).
- `nt_hash` (Windows NT/NTLM hash compute) — ✅ shipped v0.445 — the compute side of the credential cluster: `hash_identify` recognises an NTLM hash and `hash_crack` attacks one, but neither produces one. Computing the NT hash of a known/candidate password confirms a cracked password, prepares a pass-the-hash value (the output includes the `blankLM:NT` pwdump / hashcat-1000 line), or builds test data. The NT hash is MD4(UTF-16LE(password)); MD4 (RFC 1320) is implemented natively in-tree rather than taken from the discouraged `x/crypto/md4`, consistent with the other owned crypto primitives. Strongest verification class — native MD4 gated against the **complete RFC 1320 Appendix A.5 suite** (7 vectors) and NTHash against the universal NTLM vector (`NTHash("password")=8846f7ea…`); no external oracle needed. **v0.446 — now also computes the legacy LM hash** (DES of "KGS!@#$%" under each 7-byte half of the uppercased 14-char password, via `crypto/des`), completing the full pwdump `LM:NT` line (hashcat -m 3000 LM / -m 1000 NT). LM is gated against three cross-confirming references (the universal empty-LM, the published `LM("password")=e52cac67…` pair, and the hashcat -m 3000 example `HASHCAT→299bd128c1101fd6`), every value independently reproduced via an OpenSSL DES oracle. LM is emitted only for ASCII passwords ≤ 14 chars (Windows stores no LM otherwise — the disabled-LM placeholder + a note are shown; non-ASCII is OEM-codepage dependent and rejected, not guessed). Offline compute, no network/device (`internal/nthash`).
- `wpa_pmk_derive` (WPA/WPA2-PSK PMK derivation) — ✅ shipped v0.444 — the offline Wi-Fi primitive that turns a candidate passphrase + target SSID into the 256-bit Pairwise Master Key: PBKDF2-HMAC-SHA1(passphrase, ssid, 4096, 32) per IEEE 802.11i. That PMK is the value precomputed to test a guess against a captured 4-way handshake / PMKID (hashcat 22000 / 16800), so this supplies the derivation step that `wifi_pmkid_hc22000` (a hashcat-line formatter) and `internal/rsn` (a PMKID/beacon parser) do not. PBKDF2 is implemented natively in-tree (not pulled from x/crypto) to keep the crypto owned, consistent with `internal/otp` / `internal/hmacutil` / `internal/jwtsig`. Strongest verification class — gated against the RFC 6070 PBKDF2 vectors **and** the IEEE 802.11i WPA-PSK vectors (passphrase "password" / SSID "IEEE" → `f42c6fc5…`). Input validated to the spec (passphrase 8-63 printable ASCII, SSID 1-32 bytes). The 64-hex raw-PSK form and PMKID computation are deferred (PMKID held back until gated against a confidently-sourced vector — a wrong PMKID is worse than none). Offline compute, no network/device (`internal/wpa`).
- `hmac_compute` (HMAC compute / verify) — ✅ shipped v0.440 — the keyed-MAC tier and the webhook/API-auth analogue of `jwt_verify`: verify or forge an HMAC signature (GitHub X-Hub-Signature-256, Stripe-Signature, generic API request signing) with a known/leaked secret, or check a protocol HMAC auth tag. HMAC-SHA1/256/512, compute + verify, text/hex inputs. Strongest verification class — gated against the RFC 4231 published vectors. Complements `crc_compute` / `checksum_compute` (unkeyed). Offline (`internal/hmacutil`).
- `cisco_type7_decode` (Cisco IOS type-7 password) — ✅ shipped v0.439 — decode the weak reversible `service password-encryption` obfuscation (fixed-key XOR + salt index) straight to plaintext; ubiquitous in router/switch config loot. The reversible complement to hash_identify's Cisco type 8 ($8$, PBKDF2 → 9200) / type 9 ($9$, scrypt → 9300) detection (those are cracked; type 7 is decoded). Key pinned to published vectors (02050D480809 / 060506324F41 → "cisco"); offline (`internal/ciscopw`).
- `jwt_forge` (JWT forging) — ✅ shipped v0.438 — completes the JWT decode/verify/forge trio. Forge a token from operator-supplied claims for authorized web-pentest: claim escalation (`{"admin":true}`), `alg:none` crafting, and the RS→HS algorithm-confusion token (HS256 with the issuer's public-key bytes as the secret). HS256/384/512; offline payload builder (generation only, like pacs_encode/uds_encode); round-trip-verified against `jwt_verify` and reproduces the canonical jwt.io token (`internal/jwtsig`).
- `jwt_verify` (JWT signature verify) — ✅ shipped v0.437 — the verification counterpart to `jwt_decode` (decode-only by design). Verifies HS256/384/512 against a candidate secret or a list (the weak-secret test — a top web-pentest primitive), confirms the `alg:none` vulnerability, and (v0.441) verifies **RS256/384/512** against a PEM public key (Auth0/Okta/JWKS). **v0.452 — completes the asymmetric coverage**: now verifies **PS256/384/512** (RSA-PSS — Azure AD / Microsoft Entra), **ES256/384/512** (ECDSA P-256/384/521 — Apple and many IdPs; the raw r‖s JWS encoding, not DER), and **EdDSA** (Ed25519), so any captured signed JWT can be checked against the issuer's published key (`jwtsig.VerifyPublicKey` auto-parses RSA/ECDSA/Ed25519 PEM/cert). Every algorithm is round-trip-verified against the Go stdlib signer (the authoritative reference), plus the canonical jwt.io HS256 token; offline, no network/device (`internal/jwtsig`).
- `totp_generate` (RFC 6238 TOTP / RFC 4226 HOTP) — ✅ shipped v0.436 — offline OTP derivation: a 2FA seed recovered from captured loot (secrets file / config dump / otpauth:// payload) → the live codes, complementing the credential tooling (hash_identify / jwt_decode / kerberos_decode). SHA1/256/512, 6-8 digits, TOTP+HOTP. **v0.442 — also consumes a full `otpauth://` key URI** (the 2FA-enrolment QR / authenticator-export artifact) in the `secret` field: `ParseURI` takes the algorithm / digits / period / counter / mode straight from the URI, so the right parameters are applied automatically — feeding only the raw base32 from a SHA256/8-digit enrolment while the defaults stay SHA1/6 would otherwise yield valid-looking but wrong codes. Strongest verification class — gated in-tree against the RFC 4226/6238 published test vectors (HOTP counter 0 → 755224, TOTP SHA-1 T=59 → 94287082), and a URI carrying that seed reproduces the same vectors. **v0.443 — adds Steam Guard** (`mode=steam`): Steam's mobile authenticator is RFC 6238 over HMAC-SHA1 / 30s but maps the 31-bit truncated value to a fixed 5-character alphabet (`23456789BCDFGHJKMNPQRTVWXY`) instead of decimal digits, from a base64 `shared_secret` (the maFile / loot form — `encoding=base64`). Verifiable without an external vector: the truncation is anchored to the RFC value 1284755224 (the RFC publishes HOTP=755224 for counter 0, and 1284755224 % 1e6 = 755224), which maps to `GG5F5` — gated in-tree. A general `encoding` (base32/base64) param was added alongside. Offline compute from an operator-supplied seed, no network/device (`internal/otp`).
- `checksum_compute` (non-CRC checksums) — ✅ shipped v0.433 — the companion to `crc_compute` for frame trailers that aren't CRCs (common on cheap RF remotes / sensors / serial devices): SUM-8/16, XOR-8 (LRC), Modbus LRC, Fletcher-16/32, with compute + identify modes. Fletcher verified in-tree against its published reference vectors; sum/XOR/LRC are definitional. Identify makes no guesses (empty = no match → try `crc_compute`). Offline (`internal/checksum`).
- `manchester_decode` (line-code RE) — ✅ shipped v0.431 — decode a raw '0'/'1' bitstream as standard Manchester: the reverse-engineering layer between a raw OOK/FSK/RFID capture and the protocol decoders. Tries both bit alignments and returns both conventions (IEEE 802.3 / G.E. Thomas), gated on the 01/10-pairs validity rule (illegal 00/11 pairs flagged, not mis-decoded); the convention ambiguity is surfaced (both shown), never guessed. Complements `crc_compute` as protocol-RE tooling (`internal/linecode`).
- `crc_compute` (CRC compute / identify) — ✅ shipped v0.430 — a protocol reverse-engineering aid over the standard reveng catalogue (CRC-8/16/24/32 models; v0.432 added CRC-24 for Bluetooth LE PDU / OpenPGP / FlexRay). Computes a frame's CRC under any/all models, and the identify mode reports which model(s) reproduce an observed CRC over the data (reveng's fingerprinting trick) — the constant question when bringing up a decoder for a new RF/wired protocol. Every model verified in-tree against its published check value (CRC of "123456789"); identify makes no guesses (empty = no match). Offline (`internal/crc`).
- `iso7816_apdu_decode` (smart-card APDU) — ✅ shipped v0.427 — offline ISO 7816-4 APDU decoder: response SW1SW2 status word (9000 / 61XX more-data / 6CXX wrong-Le / 63CX PIN-retries-remaining / 69xx security / 6A82 file-not-found, plus the parameterised families) and command CLA/INS/P1/P2 length-case parsing with interindustry INS naming. v0.428 added the DESFire wrapping-mode status family (SW1 0x91 + SW2 → NXP DESFire status: ADDITIONAL_FRAME / AUTHENTICATION_ERROR / PERMISSION_DENIED / FILE_NOT_FOUND / …), since most DESFire exchanges are ISO 7816 wrapped. v0.429 added the DESFire command side too (CLA 0x90 → INS named as the DESFire command: SELECT_APPLICATION / AUTHENTICATE_AES / READ_DATA / GET_VERSION / …), completing DESFire APDU decoding in both directions. The analysis complement to `nfc_apdu` (which sends one); status words are the headline for smart-card interaction triage. Bounded tables, raw always surfaced (`internal/iso7816`).
- `imei_decode` (GSM device identity) — ✅ shipped v0.422 — IMEI (15-digit, Luhn-validated) / IMEISV (16-digit) decoder + TAC/RBI/serial breakdown. The cellular-identity complement to `gsmtap_decode` (an IMEI is disclosed in a GSM/LTE Identity Response, the message an IMSI-catcher forces). Luhn check digit is the anchor (advisory note on mismatch); TAC-to-manufacturer/model deliberately not guessed (proprietary GSMA registry). Offline read, no hardware (`internal/imei`).
- `ir_raw_decode` (raw infrared timing → protocol) — ✅ shipped v0.413 — the IR analogue of `subghz_decode` and the complement to `ir_decode_file` (which only reads a .ir file's already-parsed entries). Decodes the NEC family (standard / extended / repeat) gated on NEC's address & command bitwise-inverse checksum, Samsung32 (v0.419) gated on its command-byte inverse (addr·addr·cmd·~cmd), and Sony SIRC (12/15/20-bit, v0.418) gated structurally on the 2400µs leader + exact bit count + per-bit timing (no confidently-wrong output); dispatched by leader pulse, every bit tolerance-matched. RC5/RC6 (Manchester) deferred. Offline read, no IR hardware (`internal/ir`).
- `combo_cracker` (3-wheel padlock — niche physical primitive)
- `m2_lin_capture` / `m2_j1850_decode` (Macchina M2 — superset of CANable)
- `ghostesp_pwnagotchi_friend` (rolled into §2c #4 backend Specs)
- `mayhem_pocsag_decode` / `mayhem_aprs_decode` (PortaPack-side
  variants of HackRF Specs — same protocols, different backend)
- `nrf52_sniff_advertising` (custom-FW sub-variant of CatSniffer)
- `wch_swio_flash` (CH32V flasher; niche)
- `bruce_lora_scan` extension to LoRaWAN replay (audit Q4 deferred
  pending shared `esp32backend`)

---

## 4. Recommended additions to the v0.8 roadmap

A PR-able patch for `docs/refactor/v0.8-team-audit.md`. File:line
anchors below reference that file as it stands at HEAD
(`a911fcb` 2026-04-25, 257 lines).

> ⚠️ This section produces a patch only. Do **not** edit the audit doc
> as part of this gap-analysis task — the user applies these inserts.

### 4.1 Insert into §2a (attack Specs) — after line 140

Add three rows below the existing five-row §2a table. Anchor: insert
between `dronid_receive` (line 140) and `### 2b. New Flipper tool Specs`
(line 142):

```markdown
| `tpms_anomaly_detect` | Tesla CVE-2025-2082 BLE VCSEC defensive classifier | Medium |
| `iclass_dummy_mac_emulate` | bettse/Flipper picopass app (2024) | Low |
| `wifi_peap_downgrade_audit` | CVE-2023-52160 wpa_supplicant phase-2 bypass | Medium |
```

Rationale: all three sourced from `attacks.md` Top-15 (rows 9, 13, 15);
all three are 2024-2026 in-window with public PoC and no new hardware.

### 4.2 Insert into §2b (Flipper tool Specs) — after line 153

Add five rows below the existing eight-row §2b table. Anchor: between
`nfc_flippernested_run` (line 153) and `### 2c. New hardware backends`
(line 155):

```markdown
| `nfc_relay_start` + `nfc_relay_stop` | Two-Flipper ISO14443A proxy — apps `nfc_relay`, `ulc_relay` | Medium |
| `magspoof_emulate` | Mag-stripe T1/T2/T3 wireless coil — apps `magspoof_flipper` | Medium |
| `gpio_sentry_safe_open` | Factory-backdoor sequence — apps `flipperzero-sentry-safe-plugin` | Low |
| `subghz_pocsag_decode` | Paging dragnet — apps `pocsag_pager` | Low |
| `nfc_apdu_script_run` | Stored APDU sequence runner — splits from existing `nfc_apdu_run` | Low |
| `swd_dump` + `avr_isp_read` | Chip-dump primitives — apps `swd_probe`, `avr_isp` (blocks §2d `workflow_glitch_chip_dump`) | Medium |
```

Rationale: all six come from `apps.md` Top-20 (#9, #10, #11, #13, #14,
#16) and `firmware.md` cross-cutting gaps. The `swd_dump` row is the
**most consequential**: §2d's `workflow_glitch_chip_dump` is currently
listed without naming the Specs that produce the captured bytes;
without those Specs the workflow has no data path.

### 4.3 Insert into §2c (hardware backends) — after line 163

Add two rows below the existing five-item §2c list. Anchor: between
item 5 (`USB Rubber Ducky`, line 163) and `### 2d. New workflows`
(line 167):

```markdown
6. **Proxmark3 (Iceman)** — `internal/containerbridge/pm3` + ~10 Specs.
   Containerbridge to the `pm3` CLI exactly the way mfoc/mfcuk are
   wired today. Fills the LF EM4x sniff + HID-downgrade gap the
   ChameleonUltra (§2c #2) cannot reach. Pairs with §2d
   `workflow_iclass_pickup`.
7. **CatSniffer V3 / Sniffle dongle** — `internal/sniffle/` (or
   containerbridge to `sniff_receiver.py`) + ~6 Specs. Sole commodity
   tool that follows BLE 5 connections through PHY/CSA hops. Closes
   the gap behind `ble_findmy_emulate` (§2b) and the new
   `ble_proximity_audit` defensive Spec.
```

Rationale: both come directly from `hardware.md` §11 ("Top-7 backends
to add in v0.8"), explicitly marked **NEW** there. Proxmark3 was
omitted from §2c because the original audit treated it as
federation-only; on second pass the containerbridge route is days of
work. CatSniffer/Sniffle was missed entirely; given accelerating BLE
attack research (nRootTag, Stealtooth, BLE proximity-tracking 2025),
the absence of a sniffer backend is a real gap.

### 4.4 Adjust §2d (workflows) — line 168

No insertion; tighten the existing `workflow_glitch_chip_dump` row to
record its dependency on §4.2's new `swd_dump`/`avr_isp_read` Specs.
Suggested replacement for line 170:

```markdown
- `workflow_glitch_chip_dump` — Faultier sweep + Bus Pirate UART
  listener concurrently (depends on `swd_dump` / `avr_isp_read` Specs
  added in §2b)
```

### 4.5 Reprioritise §2b ordering (no insertion, reordering only)

`attacks.md` Top-15 row #1 puts `subghz_tpms_decode` ahead of every
other §2b row on ROI grounds (S effort, ~150 LoC per per-vendor
decoder, no new HW). Recommend moving the existing `subghz_tpms_decode`
row in `docs/refactor/v0.8-team-audit.md` (currently line 147) to the
**top** of the §2b table so sequencing follows ROI, not the original
order. Mechanical reorder; no content change.

### 4.6 Optional: §6 "What NOT to do" minor expansion

Two items from the catalogs warrant explicit no-go calls so future
contributors don't re-research them. Insert after line 251:

```markdown
- **Original Z3BRO Sub-GHz Jammer** — repo 404 since 2025; only
  survives in leaked CFW. Do not implement; use `subghz_jammer_detect`
  (offensive symmetric not in scope).
- **Pacs-Pwn** — original repo 404; lives only in RogueMaster bundle.
  Capability is captured by the `rfid_pacs_decode` Spec
  (gap-analysis §3 row 19); do not chase the standalone PoC.
```

---

## 5. Honest limits of this analysis

- **Volume is misleading.** §3 caps at 30 because below that the gap
  list is a long tail of marginal items (UV meter, Geiger counter,
  niche AC remotes). The **first 12 rows are where the real value is**;
  rows 13-30 are filler that some operator will eventually want.
- **The audit is mostly right.** Of 24 audit Phase-2 rows the catalogs
  reviewed, **0** look weakened on second pass. Two get **stronger**
  citations (`ble_findmy_emulate` ⟵ nRootTag; `mifare_fm11rf08_backdoor`
  ⟵ confirmed Quarkslab strongest 2024 source). The audit's strategic
  shape is sound.
- **One stale researcher claim found.** `firmware.md` §4.2 #2 lists
  `subghz_chat` as a missing handler. It exists at
  `internal/tools/subghz.go:123`. Treat as already-resolved.
- **Cuyler36 typo** lives only in the task system / draft notes; the
  audit doc itself is clean. No audit patch needed.
- **Out-of-scope by policy** (do not gap-flag): RollBack offensive
  replay, Apple BLE-spam offensive, DroneID transmit, KeeLoq
  manufacturer-key replay, Mifare Plus SL3 / DESFire EV2/EV3, 5G
  test-gear (5Ghoul / SNI5GECT / 5G-SPECTOR), TETRA:BURST exploit
  reproduction.
- **What this analysis did not do.** Did not re-verify the four
  catalogs' GitHub URLs (trusting the researchers' 2026-04-25
  fetches). Did not crawl `lab.flipper.net/apps` directly (apps.md §
  ecosystem-health flagged its API as cloudflare-fronted). Did not
  attempt any hands-on hardware verification.

---

## 6. Sources

- `/home/michael/projects/promptzero/docs/catalog/firmware.md`
- `/home/michael/projects/promptzero/docs/catalog/apps.md`
- `/home/michael/projects/promptzero/docs/catalog/attacks.md`
- `/home/michael/projects/promptzero/docs/catalog/hardware.md`
- `/home/michael/projects/promptzero/docs/refactor/v0.8-team-audit.md`
  (HEAD `a911fcb`, 257 lines)
- `/home/michael/projects/promptzero/internal/tools/` Spec registry
  (230 `Name:` registrations / 221 unique;
  `grep -rohE 'Name:\s+"[a-z0-9_]+"' --exclude="*_test.go" | sort -u`
  on 2026-04-25)
- `/home/michael/projects/promptzero/internal/flipper/capabilities.go`
  (per-fork bitmap)
